/** Mirrors `apps/server/src/modules/federation/federation.constants.ts`'s value — see
 * `delivery-client.ts`'s doc comment on why this is a small deliberate duplication rather
 * than a cross-app import. */
export const ACTIVITY_JSON_CONTENT_TYPE = 'application/activity+json';
