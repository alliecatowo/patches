/** Injected by `vite.config.ts` `define` — `<package version>+<short git sha>`. */
declare const __PATCHES_WEB_VERSION__: string;
/** ISO timestamp of the build that produced this bundle. */
declare const __PATCHES_WEB_BUILT_AT__: string;
