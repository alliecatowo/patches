# site/public/media

Populated by P9-002 (TUI screenshots + GIFs). Expected filenames, referenced from
`site/index.md`:

- `hero.gif`
- `compose.gif`
- `search-follow.gif`
- `cli.gif`
- `home.png`
- `profile.png`
- `thread.png`
- `notifications.png`

If a file listed above is missing, the corresponding `<img>` on the site just renders
broken until the next build after P9-002 lands — the build itself does not depend on
these existing.
