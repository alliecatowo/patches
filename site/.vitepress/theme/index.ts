import DefaultTheme from 'vitepress/theme';
import './custom.css';

// Default theme is enough for a docs/marketing site — custom.css carries the
// dark-first, purple-accent look described in the P9-001 brief.
export default DefaultTheme;
