# Issue #215 plan

1. [x] Compare the TUI screen/keymap inventory with the web routes and shell.
2. [x] Close one coherent parity gap: make implemented web keyboard interactions discoverable in a grouped help surface.
3. [x] Make the help surface usable at desktop and mobile widths and add route-shell regression coverage.
4. [x] Run the scoped web checks when the workspace toolchain is available; otherwise record static validation and the exact environment blocker.

## Scope boundary

React Native parity is out of scope. This slice does not advertise TUI commands that the web client does not implement.
