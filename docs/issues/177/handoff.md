# Issue #177 handoff

Implementation is complete locally: MCP metadata and resource-server verification are in `apps/server/src/modules/mcp/`, with API/MCP audience separation in `TokenService`. Formatting and diff checks pass. Commit/PR publication is blocked by the managed workspace's read-only `.git` and pnpm store paths; see `run-log.md`.
