# tasks.md — live task board

Conventions: `- [ ] ID — description` · IDs are `P<phase>-<nnn>` (H = harness, B = backlog/discovered, A = audit findings).
Check off with `- [x]`. Add a trailing `(#issue)` when mirrored to GitHub. Keep the newest audit findings at the top of
**Backlog**. When a phase's tasks are all done, run `/audit` before starting the next phase. Managed via `/task`.

## Harness

- [x] H-001 — CLAUDE.md operating manual
- [x] H-002 — `.claude/settings.json` with pnpm-only guard, prettier post-edit hook, session-start summary
- [x] H-003 — Agent roster in `.claude/agents/` (researcher, implementer, reviewer, architect, verifier, docs, spec-auditor)
- [x] H-004 — Skills: `/task`, `/verify`, `/retro`, `/audit`, `/proto-change`, `/migration`, `/phase`
- [x] H-005 — Path-scoped rules in `.claude/rules/` (server, tui, proto, database)
- [x] H-006 — `docs/agents/HARNESS.md`, `LEARNINGS.md`, `MODEL_ROUTING.md`

## Phase 0 — repository and risk spikes (spec §134, §157)

- [x] P0-001 — Pin toolchain in `mise.toml` (node 24, pnpm 11, buf, docker-compose provider, actionlint)
- [x] P0-002 — Repo hygiene: LICENSE, CODE_OF_CONDUCT, CONTRIBUTING, SECURITY, issue/PR templates, CODEOWNERS, .editorconfig, .gitignore
- [x] P0-003 — Product/architecture/ops docs + ADRs 0001–0010 (`docs/`)
- [x] P0-004 — Research notes for every risky tech (`docs/research/`)
- [x] P0-005 — Monorepo scaffold: pnpm workspace + catalog, turbo.json, tsconfig.base, eslint flat config, prettier, vitest projects, root scripts
- [x] P0-006 — `infra/compose`: postgres 17 + mailpit (+ optional minio); `mise run compose` wrapper; `.env.example`
- [x] P0-007 — `packages/proto`: buf v2 config, `patches/v1/common.proto` + `health`/hello slice, ts-proto generation committed, lint/format/breaking scripts
- [x] P0-008 — `apps/server`: Nest 11 gRPC microservice hello-world (`patches.v1.PingService`?) with config validation, JSON logger, graceful shutdown, health
- [x] P0-009 — `apps/tui`: Ink 7 hello-world that performs a real gRPC call to the server with deadline + metadata; renders errors cleanly; fullscreen + clean exit
- [x] P0-010 — Kitty graphics spike: detect capability, render test image via unicode placeholders inside Ink list, survive rerender/scroll/resize, clear on exit; fallback box _(automated: 86 tests + detection verified in real Ghostty; manual §74 checklist → B-003)_
- [x] P0-011 — `packages/database`: TypeORM 1.x DataSource, snake naming strategy, migration scripts wired (`pnpm db:*`), smoke migration + test against real Postgres
- [x] P0-012 — GitHub Actions CI: format, lint, typecheck, buf lint/format/breaking, build, unit, integration (postgres service), migration check; dependabot
- [x] P0-013 — README with real, verified first-run commands
- [x] P0-014 — Phase 0 acceptance checklist (§157) verified end-to-end and recorded in `docs/product/roadmap.md` _(2 image items await manual confirmation, tracked in B-003)_

## Phase 1 — persistence and auth (§135, amended by §165–§169)

> **Amendment A applies here.** No `users.password_hash` — credentials live in `credentials`
> (§165, ADR 0011). Email is nullable recovery data. Auth methods this phase: **password +
> SSH challenge** (GitHub device flow is Phase 6). Sessions are per node.
> Read [`docs/architecture/auth.md`](docs/architecture/auth.md) before starting.

- [x] P1-000 — Protobuf contract for auth/actors/posts/feeds: `auth.proto` (Register/VerifyEmail/ResendVerification/Login/RefreshSession/Logout/LogoutAllSessions/RequestPasswordReset/ResetPassword/GetCurrentSession + BeginSshLogin/CompleteSshLogin/BeginGitHubLogin/PollGitHubLogin/ListCredentials/AddCredential/RevokeCredential per Amendment A §165–§168), `actors.proto` (ActorService + FieldMask-based UpdateProfile), `posts.proto`, `feeds.proto`; typed grpc-js client factories; A-010/A-011 fixes. Schema only — no server handlers yet (tracked by P1-003/P1-011/P1-012/P1-014/P2-002)
- [x] P1-001 — Entities + migrations: actors (incl. `moved_to_uri`, `also_known_as`, `nameplate`), users (no password_hash; nullable recovery_email), **credentials**, **ssh_login_challenges**, refresh_tokens, invites, `auth_codes`, `outbox_jobs` (+ Phase 2 `posts`/`media`/`post_media`); outbox claim helpers; testkit factories
- [x] P1-002 — `packages/config` env schema (zod) shared by server/worker/admin
- [x] P1-003 — AuthService: register (invite-only, optional initial credential), verify email, password login, refresh (rotation + reuse detection), logout, logout-all, password reset request/reset
- [x] P1-004 — Argon2id hashing on `credentials.secret_hash`, jose EdDSA JWT access tokens, opaque hashed refresh tokens
- [x] P1-005 — Auth gRPC controller + auth guard/interceptor reading `authorization` metadata; error code mapping
- [x] P1-006 — Outbox/jobs table + worker claim loop (SKIP LOCKED, backoff, dead-letter) + email jobs; EmailProvider (console/mailpit/resend)
- [x] P1-007 — TUI: `patches register|login|logout|accounts`, CredentialStore keyed by **node origin + user id** (@napi-rs/keyring + guarded file fallback), auto-refresh
- [x] P1-008 — Rate limiting for login/register/reset/verify/challenge-issuance — shipped process-local (fixed window, §102 allows coarse process-local throttles); the db-backed store §102 wants by MVP is A-018
- [x] P1-009 — Tests: unit (token rotation, hashing, validation) + gRPC integration (register→verify→login→refresh)
- [x] P1-010 — Research note `docs/research/ssh-signature-verification.md`: which Node library verifies OpenSSH-format public-key signatures, verified against official docs (blocks P1-011)
- [x] P1-011 — SSH challenge auth: `BeginSshLogin`/`CompleteSshLogin`, `ssh_login_challenges`, blob binding (purpose + node domain + challenge id + nonce ≥32B + fingerprint + expiry), single-use TTL ≤120s, ed25519 first, SHA-1 `ssh-rsa` rejected (§166)
- [x] P1-012 — Credential management RPCs: `ListCredentials` (never returns `secret_hash`), `AddCredential` (requires authenticated session), `RevokeCredential` (fails on last active credential)
- [x] P1-013 — TUI SSH enrollment: enumerate ssh-agent identities + `~/.ssh/*.pub`, explicit confirm, sign via `SSH_AGENTC_SIGN_REQUEST`; **never read/transmit a private key**
- [x] P1-014 — `NodeService.GetNodeInfo` (unauthenticated): node domain, version, registration mode, limits, capabilities (§174) — no `tier` field, ever
- [x] P1-015 — Security tests: challenge replay + cross-node replay + expiry, algorithm downgrade, credential enumeration (uniform failures), last-credential revocation
- [x] P1-016 — AuthService.register: bootstrap registration on an invite-only node with an empty `users` table (first account registers without an invite code, logged as a WARN); subsequent registrations still require one

## Phase 2 — posting (§136)

- [x] P2-001 — posts, post_media entities + migrations; idempotent CreatePost (client_request_id)
- [x] P2-002 — PostService/ActorService: CreatePost, GetPost, DeletePost (tombstone), UpdateProfile, GetActor(ByHandle), ListActorPosts
- [x] P2-003 — TUI: profile screen, compose screen (explicit submit, draft not lost), profile timeline

## Phase 3 — social graph & feeds (§137)

- [x] P3-001 — follows entity, Follow/Unfollow, ListFollowers/Following, SearchActors
- [x] P3-002 — FeedService: ListHomeFeed / ListLocalFeed with keyset pagination, block/mute-aware SQL, indexes verified with EXPLAIN
- [x] P3-003 — TUI: Home, Local, search, follow controls, `g h`/`g l` navigation, load-more

## Phase 4 — replies, reactions, notifications (§138)

- [x] P4-001 — Threads: ListReplies (bounded depth + pagination), reply creation, root_post_id semantics
- [x] P4-002 — likes, bookmarks entities + Reaction RPCs
- [x] P4-003 — notifications rows (FOLLOW/LIKE/REPLY/MENTION/MODERATION), dedupe, ListNotifications/MarkRead
- [x] P4-004 — TUI: thread screen, reply, like, bookmark, notifications screen

## Phase 4.5 — Pages v1 (§170–§172)

- [x] P45-001 — `PatchesPage` schema + validator in `packages/domain` (versioned, flat blocks, strict-on-write); limits: doc ≤64 KiB, ≤32 sub-pages, ≤128 blocks/page, ≤8 KiB/block
- [x] P45-002 — Entities + migrations: pages, page_revisions (immutable), page_assets, guestbook_entries
- [x] P45-003 — `PageService`: GetPage, UpdatePage (new revision per write), ListGuestbook, SignGuestbook (rate-limited, block-aware, reportable, owner/moderator removal)
- [x] P45-004 — Ink page renderer + basic theme; blocks Text/Markdown/Links/Posts/TopEight/Guestbook; unknown blocks render a placeholder, never fail the page
- [x] P45-005 — `Image`/`Gallery` defined in schema but render as placeholder until Phase 5 media exists (§176) _(P5-003 landed first, so `Image`/`Gallery` render through the same `MediaAttachments` component posts use: Kitty inline when capable, the spec §75 fallback box otherwise — a strict upgrade over a static placeholder, not a deviation)_
- [x] P45-006 — TUI `patches visit @handle[/slug]` + page editor _(`$EDITOR` round-trip editor; structured block-by-block editor deferred, see follow-ups)_
- [x] P45-007 — Nameplate rendering everywhere a name appears: capability degradation (truecolor→256→16→none), plain mode, server-attested badges only (§173) _(audited — PostRow/SearchScreen/NotificationsScreen/ProfileScreen already used `Nameplate`; TopEight/Guestbook in the new Pages renderer do too)_
- [x] P45-008 — Security tests: no executable code path, control-character/escape-sequence stripping on every user string, remote-URL media rejected, `javascript:`/`data:`/`file:` links rejected _(domain + server half: `packages/domain` unit tests + `apps/server/test/pages.integration.test.ts`'s VALIDATION_ERROR/block-awareness coverage; TUI rendering-side checks land with P45-004/006)_

## Phase 5 — production media (§139)

- [x] P5-001 — media entity, BeginMediaUpload (presigned PUT to R2), FinalizeMediaUpload, GetMediaDownload
- [x] P5-002 — Worker PROCESS_MEDIA with sharp (validate, orient, strip, derivatives, hash)
- [x] P5-003 — TUI: attach image path, upload progress, bounded LRU media cache, Kitty inline + fallback + `o` open externally

## Phase 6 — moderation & security (§140)

- [x] P6-001 — blocks/mutes entities + RPCs + feed/API enforcement
- [x] P6-002 — reports + ReportPost/ReportActor
- [x] P6-003 — `apps/admin` CLI (invite/user/report/post/jobs commands) with admin_audit_log _(`apps/admin` — plain TypeScript CLI, no Nest/gRPC, talks to Postgres directly via `@patches/database`; unit + Postgres-integration tests, `docs/operations/moderation.md`)_
- [x] P6-004 — suspension enforcement, password reset end-to-end, validation sweep, URL scheme allowlist _(landed by a prior agent — `auth.guard.ts` suspension check, `auth.integration.test.ts`'s password-reset/suspension integration tests, `common/validation/url.ts` — see `git show fa0b525`; this task's slice ticks it off alongside the doc sync in `docs/architecture/api.md`/`auth.md`)_
- [x] P6-005 — GitHub credential via OAuth **device flow** (§167): `BeginGitHubLogin`/`PollGitHubLogin`, identifier = numeric account id (never login name), token discarded after reading the id, linking requires an authenticated session, honor `interval`/`slow_down` _(landed by a prior agent — `github-device-flow.service.ts` + fake-GitHub-server integration test, see `git show fa0b525`; ticked off here alongside the `docs/architecture/api.md` status-sync this task did)_

## Phase 7 — deploy public v0 (§141)

- [x] P7-001 — Multi-stage Dockerfile (server+worker, non-root), fly.toml (h2 gRPC, process groups, release_command migrations, checks) _(infra/docker/Dockerfile, infra/fly/fly.toml — never deployed, no Fly account in this environment; podman build verified through all dependency packages, blocked at apps/server's tsc step by an unrelated concurrent in-progress change — see docs/operations/deployment.md)_
- [x] P7-002 — Deploy workflow (main → build → migrate → deploy → smoke) _(.github/workflows/deploy.yml, actionlint-clean, gated behind `vars.FLY_DEPLOY_ENABLED`; Status: planned, never run)_
- [x] P7-003 — Managed Postgres, R2, Resend, secrets, domain, TLS — documented in docs/operations _(deployment.md/backups.md/incidents.md updated; Status: planned for anything needing a live Fly/R2/Resend account)_
- [x] P7-004 — npm packaging of `patches` TUI _(apps/tui/package.json + README; `@patches/tui` chosen — bare `patches` name taken; local tarball install verified end-to-end; real registry publish blocked on @patches/proto/@patches/terminal-media being private — documented as follow-up)_

## Phase 8 — two-node federation lab (v0.1) (§108 F1, §176)

> Local and non-public. Every §109 control still gates anything Internet-facing.

- [x] P8-001 — WebFinger (RFC 7033) + actor document serialization from `actors` _(apps/server/src/modules/federation/http/{webfinger,actor}.controller.ts; outbox collection GET too, ahead of P8-002)_
- [x] P8-002 — Inbox/outbox endpoints; `Follow`, `Accept`, `Create` (Note), `Delete`, basic `Like` _(inbox.service.ts handles Follow/Undo/Accept/Create/Delete/Like; outbox is newest-20 only, not true keyset-paginated — follow-up)_
- [x] P8-003 — Real `FederationGateway` implementation replacing `NoopFederationGateway`, behind `FEDERATION_ENABLED` (default **off**) _(LazyFederationGateway dispatches to ActivityPubFederationGateway/NoopFederationGateway per call, reading the flag fresh each time)_
- [x] P8-004 — Durable delivery on the outbox/jobs machinery: bounded retries, idempotent/safe duplicate delivery, dead-letter _(FEDERATION_DELIVER job type, max 12 attempts then DEAD; idempotency key is (activityId, inboxUrl); apps/worker/src/jobs/handlers/federation-deliver.handler.ts)_
- [x] P8-005 — HTTP signature signing + verification _(draft-cavage-http-signatures-12, RSA-2048/SHA-256, Digest header; unit-tested self-interop; refetch-on-verify-failure)_
- [x] P8-006 — §109 ingestion hardening: URL validation, private/reserved IP rejection, redirect/size/timeout limits, JSON depth caps, activity dedupe, inbox rate limits, domain blocks _(safeFetch + isDisallowedIp, InboxActivity dedupe, PeerRateLimiterService, DomainBlockService enforced inbound both directions; outbound delivery does not yet consult domain_blocks before enqueueing — follow-up; domain_blocks has no write RPC/CLI yet)_
- [x] P8-007 — Page manifest advertised as a Patches extension property on the actor doc (§170); plain Fediverse servers degrade to a normal actor _(ActorDocumentService; GET /users/:handle/page serves the PUBLIC Page's current revision JSON)_
- [x] P8-008 — Two-node integration harness: node A follows node B, post propagates, delete tombstones _(apps/server/test/federation-two-node.integration.test.ts — two real child processes, FEDERATION_DELIVER drained inline; passing)_

## Phase 9 — site, media, packaging (owner request 2026-08-18)

- [x] P9-001 — VitePress docs/marketing site under `site/` (landing: what Patches is, install/quickstart for the live node, docs pulled from `docs/user-guide.md` + architecture overview, roadmap); build via `pnpm --filter site build`; deploy to Cloudflare Pages via wrangler (`wrangler pages deploy site/.vitepress/dist`) — account is authenticated locally _(live at https://patches-site.pages.dev; see docs/operations/site.md)_
- [x] P9-002 — TUI screenshots + GIFs for README and the site: record real sessions against the live node (asciinema/agg or vhs; fall back to `tmux capture-pane` → HTML/SVG → PNG via a headless renderer if no recorder is available), store under `site/public/media/` (+ small copies in `docs/media/`), embed in README "Try the live node"
- [x] P9-003 — publish `patches` to npm as a single self-contained package (bundle `@patches/proto` + `@patches/terminal-media` into the TUI dist with tsup `noExternal`, or publish those two publicly) so `npm i -g patches` works (A-046) _(bundled `@patches/domain`+`@patches/proto`+`@patches/terminal-media` into `apps/tui/dist/cli.js` via `apps/tui/tsup.config.ts`'s `noExternal`; published name is `patches-social` — bare `patches` taken — set via `publishConfig.name` (pnpm 11.18+) so the workspace-local `@patches/tui` name/filters elsewhere are untouched; verified `pnpm pack` → `pnpm add -g` tarball install → `patches --version`/`--help`/`ping` against the live node from a shell with no repo `node_modules` on `PATH`; actual `npm publish` still needs owner `npm login`, tracked as planned in `docs/operations/deployment.md`)_
- [x] P9-004 — Resend: verify a real sending domain (needs DNS on a domain Allie owns) and switch `EMAIL_FROM` off `onboarding@resend.dev`; R2: real S3 credentials for `patches-media` (dashboard-only) — see B-031 _(deployed 2026-08-18: `updates.allisons.dev`, `Patches <noreply@updates.allisons.dev>`, and scoped R2 credentials)_

## Phase 10 — web + React Native clients (web un-paused by owner 2026-08-18; RN still paused)

Transport and SDK shape are decided in **ADR 0016** (`docs/decisions/0016-connect-transport-and-client-sdk.md`) — read it before starting any of these. Do them in the order listed (A→F in the ADR's phase table, which also holds the acceptance criteria).

- [x] P10-004 — Server Connect edge (ADR 0016 §2–8): `protoc-gen-es` as a second `buf.gen.yaml` plugin → `@patches/proto/es`; `apps/server/src/transport/connect/` mounts `@connectrpc/connect-express` at `/patches.v1.*` on an **always-on** Nest HTTP listener, proxying each unary call to the in-process gRPC server as opaque protobuf bytes (no duplicated mappers/guards); `FederationModule` becomes conditionally registered on `FEDERATION_ENABLED`; `WEB_ORIGINS` CORS allow-list; `x-forwarded-for` derived via `TRUST_PROXY_HEADERS`; second `[[services]]` (`:8443 → 8080`) in `infra/fly/fly.toml`
- [x] P10-003 — Shared client SDK (`packages/client`, protobuf-es types): transport-agnostic `PatchesApi`, `SessionManager` (access token + refresh rotation, pluggable credential store), status→user-copy error mapping (from `apps/tui/src/api/errors.ts`), cursor pagination helpers; transports `@patches/client/connect` (fetch, web+RN) and `@patches/client/grpc` (Node/TUI)
- [x] P10-001 — Web client (`apps/web`, Vite + React): read-only first (public timelines, profiles, threads, Pages) over Connect, then auth + posting; Pages v1 rendered same-origin as inert data (advanced HTML/CSS mode would need the dedicated origin + `script-src 'none'` of §172, and is out of scope)
- [x] P10-002 — React Native client (`apps/mobile`, Expo): auth, home/local timelines, compose, notifications over Connect (unary only — RN `fetch` cannot stream); tokens in `expo-secure-store`; shares `@patches/client` + `@patches/domain`. Password login only (no SSH/passkey/GitHub/OIDC — `apps/web` covers those); plain-text compose only (no media/reply/quote/edit/CW); no react-navigation/expo-router (manual tab state, `docs/research/expo-react-native.md` §1); RN screens stay thin/untested per that note's §4 (Vitest has no documented RN-component path), all real logic (transport, `SecureStore` credential store, session restore) is plain TS with Vitest coverage.
- [x] P10-005 — Migrate the TUI onto `@patches/client` + the grpc transport; `apps/tui/src/api/client.ts` shrinks to UI-facing wrappers, no behavior change
  - **How**: `docs/decisions/0023-tui-protobuf-es-migration.md` — migrate the type surface to protobuf-es behind a temporary wire seam (`apps/tui/src/api/wire/`), sliced as P10-007 → P10-016 below; do them in that order and do not start P10-013 (the flip) until P10-014's fixtures have landed.
- [x] P10-007 — TUI Connect transport foundation (ADR 0023 slice 1). Owns `apps/tui/package.json`, `apps/tui/src/api/transport.ts` (+ `.test.ts`). Build a `createGrpcTransport` from the existing `{ target, insecure }` options (`http(s)://<target>`); add `@patches/client`, `@connectrpc/connect`, `@connectrpc/connect-node`, `@bufbuild/protobuf` via the pnpm CLI (`catalog:`, under `flock /tmp/patches-pnpm.lock`). Acceptance: a test starts a real `@grpc/grpc-js` server and completes `SystemService.Ping` through the transport over **h2c** (`http://`, the `--insecure` path) **and** over TLS; nothing else under `src/` changes.
- [x] P10-008 — TUI error mapping accepts `ConnectError` (ADR 0023 slice 2). Owns `apps/tui/src/api/errors.ts`, `apps/tui/src/api/errors.test.ts`. `describeGrpcError`/`isSignInRequired`/`isPrivacyAckRequired` handle a `ConnectError` as well as a grpc-js `ServiceError` (prefer `rawMessage`; `Headers`-shaped metadata; gRPC `Status` and Connect `Code` are numerically identical, `docs/research/connect-es.md` §7). Acceptance: every existing test passes **unedited**; new cases cover UNAVAILABLE, DEADLINE_EXCEEDED, UNAUTHENTICATED + `SIGN_IN_REQUIRED`, FAILED_PRECONDITION + `PRIVACY_NOTICE_NOT_ACKNOWLEDGED`; `git show --stat` = 2 files.
- [x] P10-009 — TUI wire-type seam (ADR 0023 slice 3). Owns `apps/tui/src/api/wire/types.ts` (new) + the ~100 `apps/tui/src/**` files importing proto **types**. A barrel of `export type { … } from '@patches/proto'`; repoint every consumer at it. Acceptance: `grep -rn "from '@patches/proto'" apps/tui/src` matches only `src/api/wire/*` and files still importing enum/helper _values_; the diff changes import specifiers only — no other token on any line; tests unedited.
- [x] P10-010 — TUI enum seam (ADR 0023 slice 4). Owns `apps/tui/src/api/wire/enums.ts` (new) + the ~45 files importing the `SCREAMING_SNAKE` enum mirrors. Acceptance: every `POST_TYPE`/`FILTER_ACTION`/`NOTIFICATION_TYPE`/… import resolves through the seam; **no member reference** (`POST_TYPE.NOTE`) is edited anywhere (the mirrors and protoc-gen-es use identical prefix-stripped member names); tests unedited.
- [x] P10-011 — TUI timestamp seam (ADR 0023 slice 5). Owns `apps/tui/src/api/wire/time.ts` (new) + the 15 sources and 11 tests using `timestampToDate`/`dateToTimestamp`. `toDate`/`fromDate` accept `string | number | bigint` seconds so they are valid in both proto families. Acceptance: `grep -rn "timestampToDate\|dateToTimestamp" apps/tui/src` → empty; all three seconds representations unit-tested; no rendered output changes (existing render assertions unedited).
- [x] P10-012 — CLI commands stop building their own grpc clients (ADR 0023 slice 6). Owns `apps/tui/src/cli/{appeal,community,dm,filter,labelers,lists,modlog,privacy,tag}.ts` (+ their tests) and `apps/tui/src/cli/auth-shared.ts`. Route them through `PatchesApi` instead. Acceptance: `grep -rn "create[A-Za-z]*Client(\|DEADLINES_MS\|METADATA_KEYS" apps/tui/src/cli` → empty; each command run against a local node with identical output.
- [x] P10-013 — **The flip**: TUI onto `@patches/client` + protobuf-es (ADR 0023 slice 7). Owns `apps/tui/src/api/client.ts`, `apps/tui/src/api/wire/{types,enums,time}.ts`, `apps/tui/src/test/wire-fixtures.ts`, `apps/tui/src/api/errors.ts`, and the three `new PatchesApi(...)` sites (`src/cli.tsx`, `src/cli/auth-shared.ts`, `src/cli/ping.ts`). Seam modules re-export from `@patches/proto/es` / `@bufbuild/protobuf/wkt`; `client.ts` is rewritten over `createPatchesApi` + the P10-007 transport, **keeping every method name and arity** as one-line delegations, with per-call auth reproducing today's exact rule (explicit `accessToken`, else `getAmbientAccessToken()`, else no header). Do **not** adopt `@patches/client`'s `SessionManager` (ADR 0023). Requires P10-014 to have landed. Acceptance: `client.ts` under ~300 lines (from 1864); no screen/component/hook/cli source file in the diff; zero `any`/`@ts-ignore`/`eslint-disable`; `mise run verify` green; TUI smoke-tested against a local node.
- [x] P10-014 — Shared TUI test fixtures (ADR 0023 slice 6b, **must land before P10-013**). Owns `apps/tui/src/test/wire-fixtures.ts` (new) + the ~25 test files that inline proto message fixtures. Acceptance: no test file constructs a bare proto message literal; every existing assertion unedited.
- [x] P10-020 — **blocks P10-013's completion.** Restore enum rendering after the protobuf-es flip. ts-proto interpolated an enum as its wire name (`APPEAL_STATUS_OPEN`); protobuf-es interpolates the number (`1`), so `patches appeal list`, `patches lists`, and the notifications screen changed their visible output — a behavior change in a no-behavior-change task. Typecheck cannot see it (both sides are the enum type); only the 23 runtime test failures did. Fix in the wire seam that exists for exactly this: convert enum value → proto wire name at the render/print sites. See ADR 0023's 2026-08-20 addendum.
- [x] P10-015 — Drop grpc-js from the TUI (ADR 0023 slice 8). Owns `apps/tui/package.json`, `apps/tui/tsup.config.ts`, `apps/tui/scripts/copy-proto.mjs` (delete), `apps/tui/README.md`, `docs/architecture/*`. Acceptance: `@grpc/grpc-js`/`@grpc/proto-loader` gone from `dependencies` and tsup `external` (moved to `devDependencies`, used only by `apps/tui/test/transport.test.ts`); `pnpm --filter @patches/tui build && pnpm pack` yields a tarball with **no `dist/proto/`** (verified: 249242 → 199357 bytes); `patches ping`/`whoami`/`accounts`/`--help` verified against the live node. `grep -rn "@grpc/" apps/tui/src` still shows two hits — `api/errors.ts` (P10-016 owns removing this) and a comment in `api/wire/time.ts` (P10-013's file) — both explicitly out of scope for this task per the coordinator.
- [x] P10-016 — Unify error copy into `@patches/client` (ADR 0023 slice 9, optional/independent). Owns `packages/client/src/errors.ts` (+ test), `apps/tui/src/api/errors.ts` (+ test). Add `title`/`hint` to `DescribedError`; the TUI's `errors.ts` shrinks to an adapter. Acceptance: `describeError`'s `message` output is byte-identical to today for every existing case (asserted); `apps/web` untouched.
- [x] P10-017 — mobile auth parity: SSH, passkey, GitHub, and OIDC login plus register in `apps/mobile`. P10-002 shipped password sign-in only. Passkey on RN needs a native WebAuthn path (not `@simplewebauthn/browser`) — verify against official docs before committing to it. Shipped: password, GitHub, generic OIDC (all `GetAuthPolicy`-gated), SSH-via-`BeginDeviceLink`/`ApproveDeviceLink` (a phone has no SSH agent, so this replaces the raw challenge/signature flow), and register (invite code, privacy-notice ack, optional unproven SSH key). Passkey deliberately **not built**: `docs/research/expo-react-native.md` §6 found no v0-viable native WebAuthn path on Expo SDK 57 today (every wrapper needs a custom dev-client/EAS build, no Expo Go; needs hosted AASA/`assetlinks.json`; no documented guarantee of `@simplewebauthn/server`-JSON compatibility) — see B-033 for the spike this would need.
- [x] P10-018 — mobile compose parity: media upload, reply, quote, edit, and content warnings. P10-002 shipped plain-text compose only. Shipped all five: direct-to-R2 media upload (`BeginMediaUpload`→PUT→`FinalizeMediaUpload`→poll, `src/media/upload.ts` + `src/media/picker.ts` via `expo-image-picker`/`expo-file-system`), reply/quote (via `PostRow`'s new Reply/Quote buttons → `App`'s `ComposeTarget` state), in-place edit (Edit button, own posts only), and a content-warning toggle+field (request logic in `src/compose/requests.ts`). `PostRow` also gained a CW reveal-then-show toggle for the body (spec §185 — the CW label itself never hides). All new logic modules (`compose/requests.ts`, `media/upload.ts`, `media/picker.ts`) are plain `.ts` with Vitest coverage per `docs/research/expo-react-native.md` §4; `ComposeScreen.tsx`/`PostRow.tsx`/`HomeScreen.tsx`/`App.tsx` stay thin and unexercised by unit tests — verified only via `mise run check @patches/mobile` (typecheck/lint/format green, 48 tests passing), never run on a simulator/device.
- [x] P10-019 — decide whether `apps/mobile` gets component-level UI tests. Its `.tsx` screens are deliberately untested: Expo documents Jest + `jest-expo` + RNTL, which would mean a second test runner alongside this repo's Vitest convention. Flagged and unresolved in `docs/research/expo-react-native.md` §testing — this task is the decision, not the implementation.
  - **Decided 2026-08-20: no second test runner.** `apps/mobile` stays Vitest-only; its `.tsx` screens stay uncovered by unit tests, with the RN-free logic under `src/api` and `src/lib` carrying the coverage. Reasoning and the revisit trigger are recorded in `docs/research/expo-react-native.md` §4. Mixing Jest into this monorepo would need an ADR with human sign-off.
- [x] ~~P10-006~~ (declined by owner 2026-08-19 after `docs/research/ink-web.md`: Ink 7 gap, 7–9 tasks; real web GUI instead) — Web client via **ink-web** (https://www.ink-web.dev/ — Ink rendered in the browser): the terminal-first vibe accessible anywhere, reusing the TUI's screens/components on top of `@patches/client`'s Connect transport; also makes browser-driven automation (Claude in Chrome) trivial. Evaluate first (ADR addendum to 0016: does ink-web support `useInput`, raw-mode, alternate screen, our Kitty image path → fallback boxes only); if viable this replaces the Vite+React plan in P10-001. Owner idea 2026-08-18.

## Phase 11 — social depth (Amendment B)

> Spec: **INITIAL_VISION.md §178–§195** (Amendment B, 2026-08-18). TUI-first — a feature is
> not done until it is usable from the terminal (§179). Hard rules that bind every task here:
> chronological only (no sort/order parameter on any timeline RPC), no votes/karma/scores, no
> trending, keyset pagination only (§46, §153), no reaction types beyond the like, no
> paywalled function (§184.3), and v0 DMs are **server-visible and must say so** (§183.1,
> §194).
>
> Waves: **1** (001–002, sequential) → **2** (003–005, parallel) → **3** (006–008, 006 then
> 007/008) → **4** (009–013, parallel) → **5** (014–016, parallel). Each task lists the files
> it owns; do not edit another task's files.

- [x] P11-001 — proto contract for Amendment B (§190). Owns `packages/proto/**`. `posts.proto`: `EditPost`, `ListPostEdits`, `PinPost`, `UnpinPost`, `Post.quoted_post`/`.community`/`.quote_policy`, `CreatePostRequest.quoted_post_id`/`.community_id`/`.quote_policy`, `PostCounts.reposts`/`.quotes`, `PostViewerState.reposted`; `reactions.proto`: `RepostPost`/`UnrepostPost`/`ListPostReposters`; `feeds.proto`: `ListTagFeed`/`ListCommunityFeed` (**no ordering parameter**); `notifications.proto`: `NOTIFICATION_TYPE_REPOST=6`, `_QUOTE=7`, `_MESSAGE=8`, `_COMMUNITY_INVITE=9` + additive `conversation_id`/`community_id` fields; `actors.proto`: `ActorFlair` + `Actor.flair`/`.pinned_post_ids` + flair in the `UpdateProfile` mask; `node.proto`: `like_glyph_allow_list`, `max_post_chars`, `can_create_community`, `dm_enabled`, `dm_retention_days`; new `communities.proto`, `messages.proto`, `tags.proto` per §190. Acceptance: `pnpm proto:lint`, `pnpm proto:format`, `pnpm proto:breaking` and `pnpm proto:gen` clean with generated output committed; every list RPC cursor-paginated (§46); **no field number reused or renumbered** (§153); no `sort`/`order` field anywhere. Do **not** touch `docs/architecture/api.md` (P11-014 owns it).
- [x] P11-002 — database schema + domain limits (§188, §189). Owns `packages/database/**`, `packages/domain/src/limits.ts` (+ its test). Entities and one migration for `reposts`, `tags`, `post_tags`, `tag_mutes`, `communities`, `community_members`, `community_bans`, `community_invites`, `conversations`, `conversation_members`, `messages`, `message_requests`, `post_edits`, `pinned_posts`, `actor_flair`, plus `posts.quoted_post_id`/`.quote_policy`/`.community_id`. Acceptance: `pnpm db:migrate` up **and** down clean against a real Postgres; every timeline index is `(…, created_at DESC, id DESC)` keyset-shaped (§189); uniqueness constraints exactly as §189 lists them; `tags` has **no** `post_count` column (§181); no `synchronize` anywhere; every §188 size limit exported from `@patches/domain` and asserted in a test.
- [x] P11-003 — server: communities module (§182). Owns `apps/server/src/modules/communities/**` + its registration line in `app.module.ts`. `CommunityService`: create (capability + rate gated), get, list, join, leave, members, update, `SetCommunityRole`, `RemovePostFromCommunity`, `BanFromCommunity`, `InviteToCommunity`, `RespondToCommunityInvite`. Acceptance: name grammar `[a-z0-9_]{3,32}` + reserved-name blocklist enforced server-side (§192); creator is first moderator; moderator authority stops at the community boundary and every moderation action writes an audit-log row (§66); removal clears `community_id` and leaves the post on the author's profile; invites never auto-join; no vote/score/sort surface exists anywhere in the module (§194); integration tests for each RPC incl. ban and role changes.
- [x] P11-004 — server: direct messages module (§183). Owns `apps/server/src/modules/messages/**`, the `ReportMessage` addition in `apps/server/src/modules/moderation/**`, + its `app.module.ts` line. `DirectMessageService` per §190; mutual-or-accepted gating, message requests (1 pending, 1 message, 30-day bar after decline), groups ≤ 8, text-only, sender-side delete tombstone, per-viewer unread, no read receipts. Acceptance: block-aware in both directions with **no block oracle** (a blocked send fails indistinguishably from any unavailable recipient, §62); `ReportMessage` snapshots ≤ 10 surrounding messages (§183.4); rate limits from §188 enforced database-backed per actor **and** per peer; a test asserts no message body reaches logs/metrics/errors (§192); no media, no link previews, no federation delivery path.
- [x] P11-005 — server: tags module (§181). Owns `apps/server/src/modules/tags/**` + its `app.module.ts` line. `TagExtractionService` (write-time, NFKC + casefold, grammar per §181, ≤ 10 tags, 11th is `INVALID_ARGUMENT`, extraction failure never fails the post), `TagService` (`SearchTags` prefix + alphabetical + ≤ 20 results, `MuteTag`, `UnmuteTag`, `ListMutedTags`). Do **not** edit `posts.service.ts` — P11-006 wires the hook. Acceptance: normalization is the identity (two spellings collapse to one tag, tested); control/bidi/zero-width/combining-pileup names rejected (§192); all-digit `#2026` is not a tag; search ordering is alphabetical and provably not popularity-based; no trending endpoint exists.
- [x] P11-006 — server: posts + reactions (§180, §186). Owns `apps/server/src/modules/posts/**`, `apps/server/src/modules/reactions/**`. Repost/unrepost/list-reposters as pointer rows; quote creation (`quoted_post_id`, body-or-media required) with server-side `quote_policy` enforcement; `EditPost` + immutable `post_edits` snapshots + `ListPostEdits`; `PinPost`/`UnpinPost` (≤ 3); `community_id` set at create and immutable; tag-extraction hook calling P11-005's service. Acceptance: cannot repost a `FOLLOWERS` post that is not your own, a tombstone, or a blocker's post (§180.1); edit cannot change `post_type`/`visibility`/`in_reply_to_id`/`community_id`/`quoted_post_id`/`created_at`; **an edit, repost, quote, or pin never changes `created_at` or feed position** (explicit test, §186.1); deleting a post tombstones its reposts and edit history; rate limits from §188.
- [x] P11-007 — server: feeds (§180.1, §181, §182.2). Owns `apps/server/src/modules/feeds/**`. Reposts in the home feed at the repost's own `created_at` with `reposted by` attribution; within-page duplicate collapsing naming ≤ 3 reposters; `ListTagFeed` (PUBLIC only, block/mute/tag-mute filtered, community posts excluded for non-members); `ListCommunityFeed`; local feed excludes community posts unless the viewer is a member. Acceptance: keyset pagination on every new query with no offset anywhere (§153); no ordering parameter on any RPC; a test proves reposting an old post does not move the original; cross-page duplicate behaviour documented in the module's header comment as a known, accepted limitation (§180.1).
- [x] P11-008 — server: flair, pinned posts, node capabilities (§184). Owns `apps/server/src/modules/actors/**`, `apps/server/src/modules/system/node.{controller,service}.ts`. `ActorFlair` validated (≤ 1 KiB, contrast floor, allow-listed `like_glyph`, `border_style` enum, `wall_theme` reusing the §171 theme fields), written through `UpdateProfile`'s field mask; `Actor.pinned_post_ids`; `GetNodeInfo` publishes `like_glyph_allow_list`, `max_post_chars`, `can_create_community`, `dm_enabled`, `dm_retention_days`. Acceptance: glyph allow-list rejects images/uploads/ZWJ/combining/control codepoints and anything not width-1 (§192); no `tier`/`plan`/`premium`/`is_supporter` field is introduced (§177); no cosmetic is gated behind anything but a capability, and no **function** is capability-gated (§184.3); validation unit-tested per field.
- [x] P11-009 — TUI: post actions and rendering (§180, §186, §191). Owns `apps/tui/src/components/{PostRow,PostList}.tsx`, `apps/tui/src/screens/{ThreadScreen,ComposeScreen}.tsx`, new `screens/PostEditScreen.tsx`, `screens/PostHistoryScreen.tsx`. Keys `R` repost/unrepost, `Q` quote, `E` edit own, `d` delete own (confirm), `H` edit history; quote embed rendered exactly one level deep; repost attribution line; `— read more` fold past ~10 lines / 500 chars in feed contexts, full body in thread/permalink. Acceptance: no existing documented key is rebound (§191); every new rendering has a plain-mode form; fold threshold and `max_post_chars` read from `GetNodeInfo`, never hardcoded; ink-testing-library tests per key.
- [x] P11-010 — TUI: messages (§183, §191). Owns `apps/tui/src/screens/MessagesScreen.tsx`, `apps/tui/src/cli/dm.ts`. `g d` conversation list + thread, `m` message the profile you're viewing, message-request inbox with accept/decline, `patches dm` headless equivalent. Acceptance: the **"Not end-to-end encrypted — this node's operators can read these messages"** notice renders on the messages screen itself, not in help or a tooltip (§183.1); the word "encrypted"/"secure"/"private" never describes DMs in any string (grep-asserted in a test, §194); no attachment or link-preview affordance exists; `Esc` backs out one level.
- [x] P11-011 — TUI: communities (§182, §191). Owns `apps/tui/src/screens/CommunitiesScreen.tsx`, `apps/tui/src/cli/community.ts`. `g c` community list → timeline → members, `j` join/leave, `c` composes into the focused community, rules view, moderator actions for moderators, invite accept/decline. Acceptance: no vote, score, or sort affordance exists in any view (§194); the timeline is chronological with keyset paging; non-members see a public community read-only with a clear join affordance; headless `patches community list|join|leave|post` works.
- [x] P11-012 — TUI: tags (§181, §191). Owns `apps/tui/src/screens/TagFeedScreen.tsx`, `apps/tui/src/cli/tag.ts`. `t` tag search, `#` tag timeline for the selected post's first tag, tag mute/unmute, `#tag` highlighted in rendered bodies. Acceptance: no trending or popularity display anywhere; muted tags disappear from home/local/tag feeds but an explicitly opened thread still renders (§181); search results alphabetical; `patches tag search|feed|mute` works headlessly.
- [x] P11-013 — TUI: flair, pinned posts, quiet feed (§184, §185, §191). Owns `apps/tui/src/screens/{PreferencesScreen,EditProfileScreen}.tsx`, `apps/tui/src/components/Nameplate.tsx`, flair rendering helpers. `,` preferences (plain mode, quiet feed, flair, pinned posts), `~` toggles quiet feed, pin/unpin from a profile. Acceptance: quiet feed hides **others'** cosmetics while keeping the viewer's own and structural colour; plain mode still strips everything and wins where they overlap (§185); flair degrades truecolor → 256 → 16 → none; a test proves rendered flair cannot emit control characters, move the cursor, or draw outside the post's cells (§192); content warnings and tombstones always render under both modes.
- [x] P11-014 — docs sync for Amendment B. Owns `docs/architecture/api.md`, `docs/architecture/social.md` (new), `docs/user-guide.md`, `docs/product/roadmap.md`, `docs/product/moderation.md`, `docs/product/privacy.md`, `README.md`. Every new RPC in `api.md` with real status markers; the §191 keys in the user guide and the `?` help screen's source of truth; DM/community moderation surfaces in `moderation.md`; the honest DM data-handling statement in `privacy.md` (§183.1). Acceptance: every documented command was actually run by the author (working agreement rule 7); no "planned" claim that is already shipped and no shipped claim that is not. — _partial: api.md/user-guide.md/product docs updated; social.md missing_
- [x] P11-015 — federation mapping notes only, no code (§193). Owns `docs/research/activitypub-social-depth.md` (new), `docs/architecture/federation.md`. Verify against **current** upstream sources: `Announce`/`Undo(Announce)` for reposts, FEP-044f + `quoteUri`/`quoteUrl`/`_misskey_quote` for quotes, `Tag`/`Hashtag` for tags, the `Group`-actor pattern for communities (**currently unverified** — that is the point of the note), and private-`Note` addressing for DMs. Acceptance: every claim carries a citation and a verification date; the note states plainly that none of this is in Phase 11 scope and that no §109 gate or §160 checklist item moves (§193).
- [x] P11-016 — ADR: DMs are server-visible in v0 (§183.1, §195). Owns `docs/decisions/0017-server-visible-dms.md` + the index row in `docs/decisions/README.md`. Record the decision, the moderation trade that motivates it (reporting evidence, §183.4), the labelling obligation, and what E2E would actually require (key management, multi-device, federated key discovery, loss of server-side evidence, migration of existing plaintext conversations). Acceptance: the ADR states explicitly that E2E is **not** authorized by this decision and needs owner sign-off (§195); it does not authorize anything §194 prohibits.

## Phase 12 — TUI interaction model, rich input, and visual system

The binding design is ADR 0018 plus `docs/architecture/tui-interaction-model.md`; the visual and product-level contract is `docs/product/tui-design-vision.md`. Tasks below preserve the stage ordering in those documents. `P12-119`–`P12-121` remain blocked on separate owner sign-off; everything else is authorized interaction work.

- [x] P12-001 — fixed frame budget + `ContentSizeProvider`; one window-size source; footer and content regions are explicitly bounded
- [x] P12-002 — shell renders every screen inside a fixed-height clipped content frame and re-checks minimum terminal size every render
- [x] P12-003 — frame-fit invariant harness across representative terminal sizes, long/wide Unicode, and media rows

> **Landed but not yet wired (2026-08-19).** `SplitPane` + `responsive-layout`,
> `MediaViewerScreen`, `FilePicker` + `file-picker-model`, `ColorPicker`, `ThemePreview`,
> `theme/themes/*` (registry/resolution) and `preferences/store` are all implemented,
> measured and unit-tested, but nothing in `app/App.tsx` renders or routes to them yet, so
> the corresponding tasks below stay open — a module a viewer cannot reach is not shipped.
> `P12-127` tracks connecting them.

- [x] P12-004 — generalized measured `VirtualList` used by posts, notifications, help, search, conversations, communities, and tags
- [x] P12-005 — key-layer stack replaces `capturesInput` / `screenCapturing`; shell safety keys remain reachable from every sub-mode
- [x] P12-006 — typed navigation stack + route payloads + `Esc`-from-everywhere coverage
- [x] P12-007 — keymap v2: `R` repost, `Ctrl+R` refresh, `Q` quote, `J` community join/leave, all §191 keys derived into help/hints
- [x] P12-008 — modal stack + measured `ConfirmDialog` for every destructive action
- [x] P12-009 — command palette (`:` / `Ctrl+P`) generated from `KEYMAP`, including routes and contextual actions
- [x] P12-010 — feedback layer: per-region loading, toast queue, offline/retry banner, sticky new-items indicator
- [x] P12-011 — inline re-authentication on `UNAUTHENTICATED` without losing drafts or navigation state
- [x] P12-012 — measured multiline `TextEditor`: cursor, movement, undo/redo, bracketed paste, node-provided limit
- [x] P12-013 — measured `@` / `#` autocomplete with debounced actor/tag lookup and stale-request cancellation
- [x] P12-014 — terminal file picker/path completion replaces raw attachment path entry
- [x] P12-015 — colour picker with contrast enforcement and truecolour → 256 → 16 → text degradation
- [x] P12-016 — post search wired to `SearchPosts`, people/posts modes, newest-first keyset results with no ranking/order control
- [x] P12-017 — shared markdown-lite post renderer + measured read-more folding; plain mode keeps source markers
- [x] P12-018 — bounded image policy + media viewer; inline selected/focused/viewer only, fallback height parity, ≤4 live placements
- [x] P12-019 — thread rows share one focusable list so parent/root/replies are reachable and reply targets the selected row
- [x] P12-020 — responsive layout tiers (narrow/standard/wide/ultra, compact/full) with pure boundary tests
- [x] P12-021 — split-pane presentation from the same nav stack; resize changes presentation, never history
- [x] P12-022 — frozen-background ANSI-safe overlay compositing with narrow/compact takeover fallback
- [x] P12-023 — quick-post overlay (`c`) + full compose (`C`) share one draft and one editor
- [x] P12-024 — notifications drawer (`N`) with narrow fallback to `g n` and read-on-view reuse
- [x] P12-025 — polish primitives: semantic theme tokens, borders, progress bar, single shared relative-time clock
- [x] P12-026 — interaction docs/keymap sync; executable-command verification and generated key-table parity test
- [x] P12-101 — theme engine and built-ins (`patches`, `paper`, `mono`, `hacker`, `pastel`, `terminal`) with CLI/env/profile precedence
- [x] P12-102 — responsive ribbon/status chrome with connection state, unread pill, and focus breadcrumb
- [x] P12-103 — Unicode/Nerd/ASCII glyph sets with locale fallback and no glyph-only affordance
- [x] P12-104 — post-row rhythm pass: selection gutter, body indent, attribution, quote embed, rich/plain/quiet height parity
- [x] P12-105 — destructive confirm styling, toast queue styling, and sticky new-count styling
- [x] P12-106 — quick-post overlay UX: counter/CW/expand/shared-draft, with narrow takeover
- [x] P12-107 — notifications drawer grouping, read-on-view, and live unread indicator
- [x] P12-108 — split-pane thread presentation polish, pane focus, titles, breadcrumb, below-count
- [x] P12-109 — Pages renderer: responsive cell grid, scoped wall themes, pins, Top 8, guestbook, gallery, clipped ASCII
- [x] P12-110 — structured Pages block editor: list/reorder/add/remove/field edit with inline validation; `$EDITOR` remains available
- [x] P12-111 — bracketed-paste/file-URI attachment detection with safe path handling
- [x] P12-112 — theme + colour picker live preview, reversible with `Esc`, with contrast explanation
- [x] P12-113 — preferences information architecture with live post preview and per-profile local persistence
- [x] P12-114 — messages visual layer: permanent E2E state disclosure, folders, optimistic send, retention copy
- [x] P12-115 — search mode strip and subtractive filters (`since:`, `from:`, `#tag`) with recent-query recall; no order control
- [x] P12-116 — contextual palette actions for quoted posts, mentions, tags, links, and selected-row verbs
- [x] P12-117 — restrained motion/feedback: scroll thumb, success flash, finite refresh spinner, offline countdown, one clock
- [x] P12-118 — linear/screen-reader mode: one column, no overlays/drawers, indexed items, plain implied
- [ ] P12-119 — **BLOCKED** proposal/ADR for ephemeral `Now` status; no code until owner approves the exact non-story constraints
- [ ] P12-120 — **BLOCKED on P12-119** server `Now` protocol/storage/expiry implementation
- [ ] P12-121 — **BLOCKED on P12-120** TUI `Now` ring + screen/drawer
- [x] P12-122 — DM drawer reusing the drawer primitive and retaining the permanent privacy disclosure
- [x] P12-125 — route `E` (edit your own post) to a compose-in-edit-mode screen; it is bound in `KEYMAP` and raises "not connected yet", and is held out of the user guide until it works
- [x] P12-126 — migrate the block/mute/report confirmations onto the shared measured `ConfirmDialog` so every destructive action uses one component
- [x] P12-127 — wire the landed-but-unreachable Phase 12 modules into the shell: theme engine + preferences precedence, `ThemePreview`/`ColorPicker`, `FilePicker` for attachments, `MediaViewerScreen`, and `SplitPane`/responsive tiers
- [x] P12-128 — replace `measurePostBody`'s conservative plain-mode upper bound with the viewer's actual mode, so a decorated body doesn't reserve rows it won't draw
- [x] P12-123 — committed 100×30 / 140×40 golden frames, tmux captures, and drift tests
- [x] P12-124 — Vim-style command mode (`:`): generated route/action commands, aliases, history, completion, contextual verbs, and explicit rejection of `:!` shell execution
- [x] P12-129 — TUI self-upgrade prompt on launch (`y` upgrades via the release tarball, then "press Ctrl+C to exit and relaunch"; `patches upgrade`; `--no-upgrade-check` flag)
- [x] P12-130 — ASCII/half-block/braille image art fallback renderer in `@patches/terminal-media` with `PATCHES_IMAGES`/preference modes auto|pixel|ascii|box|off and compose/file-picker preview

## Phase 13 — production E2EE direct messages

ADR 0020 is binding. This phase replaces the preserved crypto spike; no client or node may advertise `E2EE_V1` until every ship gate in ADR 0020 §13 is complete. Existing conversations remain visibly `LEGACY_SERVER_VISIBLE`; federated DMs remain prohibited.

- [x] P13-001 — protocol/domain contract: immutable conversation modes, account-root/device certificates, monotonic rosters, prekey bundles, per-device envelopes, capability negotiation, and E2EE report evidence
- [x] P13-002 — database schema/migrations: device roster/prekey/envelope/mailbox/report tables and constraints; no plaintext body, message key, ratchet state, or recovery secret may enter node persistence
- [x] P13-003 — replace the crypto spike with reviewed primitives, X3DH transcript binding, and a full revision-4 Double Ratchet with encrypted headers, bounded skipped keys, deterministic vectors, fuzz/property tests, and zeroization boundaries
- [x] P13-004 — account-root and certified-device lifecycle: enrollment, safety numbers, signed monotonic roster changes, device change warnings, revocation, and compromise recovery
- [x] P13-005 — prekey service: signed-prekey rotation, atomic one-time-prekey consume/replenish, exhaustion fallback disclosure, anti-replay, concurrency tests, and abuse limits
- [ ] P13-006 — encrypted local state: OS-keyring-wrapped database, atomic ratchet commits before acknowledgement, crash/retry recovery, rollback detection, and explicit logout/device-wipe semantics _(store landed 2026-08-22: `apps/tui/src/e2ee/` vault — HKDF-derived data key under an OS-keyring wrapping key (`patches-e2ee-vault`, per node+user) with guarded-file + ephemeral tiers; staged commit protocol (`stageRecord` durable **before** bytes go out → `confirmRecord`) whose crash window is adopted on next open so the reloaded ratchet is always ≥ anything sent; monotonic-generation rollback detection anchored beside the wrapping key; `wipeE2eeState()` destroys vault/temps/lock/key-file/keyring entry; zeroization on commit/close/wipe; 54 tests. Remaining: wire into `patches logout` + the send path — P13-010 consumes it; passphrase-KDF tier is B-081)
- [x] P13-007 — Sesame-style multi-device fanout and convergence: one device-pair session per active device, dedupe/retry/out-of-order handling, offline device catch-up, and revocation races. `E2eeConversationService`/`e2ee-fanout.ts` implement `CreateE2eeConversation`/`SendEnvelopes`/`GetE2eeConversationState`/`ListMailboxEnvelopes`/`AcknowledgeEnvelopes`; fanout accept locks every current member's device rows `FOR SHARE` so a concurrent `RevokeDevice` UPDATE serializes against it (fails closed either direction), dedupes on `(sender_actor_id, client_request_id)` with replay-on-race via the unique-violation path, and `canonicalFanoutTranscript` makes the fanout digest independent of device-envelope submission order. Covered by 14 real-Postgres integration tests in `apps/server/test/e2ee.integration.test.ts`, including a new one added in review verifying every active device of both sender and recipient receives its own copy (never the literal sending device).
- [x] P13-008 — small-group E2EE (≤8 members) as pairwise fanout with authenticated roster transitions, add/remove semantics, epoch changes, and no sender-key/MLS shortcut _(done 2026-08-22: per-conversation membership epoch + device-signed hash-chained group-control transcript — `canonicalGroupControlTranscript`/`assertGroupControlSucceeds` in `@patches/domain`; `AddE2eeMember`/`RemoveE2eeMember`/`ListE2eeGroupControlEvents` RPCs; `e2ee_group_control_events` table (unique `(conversation_id, epoch)`, no FK on subject so evidence survives deletion); fanout loads the epoch after taking FOR SHARE locks on member rows so removal serializes against accepts exactly like `RevokeDevice`; removed member gets `E2EE_CONVERSATION_NOT_FOUND` (no block oracle), stale-epoch sends rejected, added member receives future messages only, delivered mail stays readable. 24/24 e2ee integration tests incl. 10 new transition tests; migration verified up→down→up. Client wiring + docs are follow-ups: see P13-010 and the P13-013 docs slice)
- [x] P13-009 — message franking design + implementation, forged/truncated/replayed evidence tests, and moderation ingestion are complete and tested; the independent cryptographic review gate itself is a process gate that cannot be self-certified and remains open — `E2EE_APPROVED_FRANKING_PROFILES` stays empty and `assertFrankingProfileApproved` keeps throwing until a reviewer signs off per ADR 0020 §12.7
- [x] P13-010 — TUI E2EE UX: immutable mode labels, verification/safety-number flow, change/compromise interstitials, backup-loss disclosure, device manager, and inaccessible-history states _(duplicate of the landed record below; retained for history)_
- [ ] P13-011 — optional recovery archive and authenticated peer history transfer; never restore live ratchet counters, skipped keys, old prekeys, or revoked device identity keys _(partial 2026-08-22: domain contracts landed — `packages/domain/src/e2ee/recovery.ts` (archive document codec, restore plan that only ever yields fresh-enrollment inputs; roster rollback refusal) and `history-transfer.ts`+tests (display-only plaintext re-delivery through the existing fanout, strictly-ascending canonical order, no keys/ratchets representable); TUI seam files `apps/tui/src/e2ee/{recovery-archive,history-transfer}.ts` incl. root-key coherence check and the served-roster restore gate. Remaining: recovery.ts has no dedicated test file yet, archive create/export CLI/UI surface, end-to-end restore flow test)_
- [ ] P13-010 — TUI E2EE UX: immutable mode labels, verification/safety-number flow, change/compromise interstitials, backup-loss disclosure, device manager, and inaccessible-history states
- [ ] P13-011 — optional recovery archive and authenticated peer history transfer; never restore live ratchet counters, skipped keys, old prekeys, or revoked device identity keys _(partial 2026-08-22: domain contracts landed — `packages/domain/src/e2ee/recovery.ts` (archive document codec, restore plan that only ever yields fresh-enrollment inputs; roster rollback refusal) and `history-transfer.ts`+tests (display-only plaintext re-delivery through the existing fanout, strictly-ascending canonical order, no keys/ratchets representable); TUI seam files `apps/tui/src/e2ee/{recovery-archive,history-transfer}.ts` incl. root-key coherence check and the served-roster restore gate. Remaining: recovery.ts has no dedicated test file yet, archive create/export CLI/UI surface, end-to-end restore flow test)_ **Blocked on a prerequisite discovered 2026-08-25:** `apps/tui` never persists the messaging-root private key anywhere post-enrollment (`enrollment.ts`/`local-identity.ts`/`vault-format.ts` don't store it; `rootPrivateKey` appears only as a caller-supplied argument to `packages/crypto/src/identity.ts` primitives and as a consumer in `recovery-archive.ts`). An archive-export command would have nothing real to export, so items 2–3 cannot be built until the vault persists that key (or a wrapped form) at enrollment. `recovery.test.ts` landed with negative assertions that no restore path can reconstitute a ratchet, skipped key, old prekey, or revoked identity key.
- [x] P13-012 — E2EE reporting/moderation flow: user-disclosed plaintext evidence only, explicit consent, audit trail, and no ordinary-message decryption path for node operators. Consent (`reporterConsented`/`consentedAt`) and submission audit (`e2ee_report_evidence(_items)`) were already in place from P13-009; this task added `loadReportEvidenceForModeration` (`apps/server/src/modules/e2ee/report-evidence-moderation.ts`), which returns a moderator-facing DTO of a report's disclosed evidence and appends a content-free `admin_audit_log` row (`report.view_e2ee_evidence`) in the same transaction as the read, reusing the existing admin-audit mechanism rather than a new one. Report resolution audit (`report.resolve`, `ModerationLogEntry`) already covers any subject type, including E2EE_MESSAGE, unchanged. Deliberately not wired to a transport: `ModerationService`'s own contract keeps moderator actions out of user-facing gRPC (spec §65), and the admin CLI that would call this (`apps/admin/src/commands/report.ts`) is outside this task's file ownership — a follow-up should add `report show --reveal-evidence` there.
- [x] P13-013 — cross-layer privacy verification: automated storage/log/metric/error/notification scans for plaintext and keys, legacy/E2EE migration tests, export/deletion semantics, and proof no E2EE path crosses federation _(landed 2026-08-22: `apps/server/test/e2ee-privacy-scan.integration.test.ts` 9/9 — canary plaintext/key scans over every e2ee table post-flow, log/notification canary capture, federation↔e2ee import-graph source proof, mode-immutability trigger assertion, export/purge handler source scans; root cause of the parked failure was the reporter not holding conversation membership — the documented no-oracle rule working as designed)_
- [x] B-083 — ~~parked privacy-scan blocker~~ RESOLVED (superseded by P13-013 landing above; kept one wave for reference then close)
- [ ] P13-014 — independent security review remediation, experimental capability rollout, multi-client interoperability lab, production runbook, and only then enable `E2EE_V1`
- [x] P13-018 — `report show --reveal-evidence` (`apps/admin/src/commands/report.ts`) wires reporter-disclosed E2EE evidence into the admin CLI: requires an identified operator, prints disclosed plaintext only under the explicit flag, and writes the same content-free `report.view_e2ee_evidence` `admin_audit_log` row in the same transaction as the read. Duplicated (not imported) `loadReportEvidenceForModeration`'s lookups directly in `apps/admin`, since `apps/admin` never depends on `apps/server` (same boundary `labeler.ts` already draws) — the server-side `E2eeReportEvidenceService`/`loadReportEvidenceForModeration` remain unreferenced by any transport, which is fine: they were already independently tested infrastructure P13-012 left for this task to consume by pattern, not by import.
- [x] P13-019 — added `ModerationService.ReportE2eeMessage` (a fourth sibling RPC, not a generic `CreateReport` — see ADR 0020 §9 addendum). Creates a bare `Report` row (`subject_type = 'E2EE_MESSAGE'`, no snapshot possible); evidence is disclosed afterward via the existing `AttachReportEvidence`. Follow-up filed: `AttachReportEvidence` still doesn't check the report's `subject_type` matches (owned by `apps/server/src/modules/e2ee/report-evidence.ts`, out of this task's scope).
- [x] P13-015 — persisted node franking-key custody: a key table, rotation schedule, and era handling. P13-009 left `NodeFrankingKeyRing`/`EnvNodeFrankingKeyRing` in `apps/server/src/modules/e2ee/report-evidence.ts` as explicit scaffolding — env-sourced, no storage, no rotation — which ADR 0020 §12.7 does not accept.
- [x] P13-016 — franking construction review gate ~~(blocked on external review)~~ **RESOLVED by owner directive 2026-08-23**: "there is no external review … you and your agents are external review." A three-way adversarial audit (client / server / crypto-core) was commissioned and completed as the review of record; every finding was then implemented on `feat/e2ee-audit-hardening` (PR #62): P0 plaintext-into-E2EE closed at service + DB-trigger + CLI layers; §183.2 eligibility, E2EE rate limits, purge coverage, MAX_SKIP/AD/epoch/domain-separator crypto fixes. ADR 0025's `patches-franking-v1` profile is approved for enablement once the TUI receive-runtime lands (B-101) and `E2EE_APPROVED_FRANKING_PROFILES` is populated through the existing capability machinery per B-094 rollout discipline. ADR 0024's rejection reasons are addressed by the 0025 construction now carrying real commitments end-to-end.
- [x] P13-017 — added `REPORT_NOT_FOUND` (gRPC `NOT_FOUND`) to `apps/server/src/common/errors/error-codes.ts` and switched `attachReportEvidence`/`loadReportEvidenceForModeration`'s stopgap `AppError.validation` "report not found / not yours" throws to it. Wire-visible: those two lookups now return `NOT_FOUND` instead of `INVALID_ARGUMENT`.

## Phase 14 — privacy, filters, decentralized moderation (Amendment C)

ADR 0019 is binding (`INITIAL_VISION.md` §196–§210). §205's TUI-first-but-web-parity rule applies: safety surfaces (filters, blocks, reports, privacy) must reach the web client before it's offered beyond the owner. Wave order: 001–006 (proto + schema) → 007–014 (server/domain, parallel by module) → 015–018 (clients) → 019–020.

- [x] P14-001 — proto: `filters.proto`, `filter_lists.proto`, `labels.proto`, `appeals.proto`, `privacy.proto`; extend `node.proto` (`GetNodePolicy`) and `moderation.proto` (`ListModerationLog`, `ListMyModerationNotices`); hand-add `index.ts`/`nest.ts`/`constants.ts` re-exports. Owns `packages/proto/**`. Acceptance: buf lint/breaking pass; generated TS compiles; new RPCs reachable via `@patches/proto` public re-exports.
- [x] P14-002 — database: `actor_privacy_prefs`, `filters`, `filter_scopes`, `filter_terms` entities + migration. Owns `packages/database/src/entities/{actor-privacy-prefs,filter,filter-scope,filter-term}.entity.ts`, `migrations/`. Acceptance: migration hand-reviewed, no synchronize, indexes per §202.
- [x] P14-003 — database: `filter_lists`, `filter_list_entries`, `filter_list_subscriptions`, `filter_list_exceptions` + migration. Owns `packages/database/src/entities/filter-list*.entity.ts`, `migrations/`. Acceptance: exactly-one-owner CHECK (actor xor community); unique constraints per §202.
- [x] P14-004 — database: `labelers`, `labels`, `labeler_subscriptions`, `labeler_subscription_actions` + migration. Owns `packages/database/src/entities/label*.entity.ts`, `migrations/`. Acceptance: labels' exactly-one-subject CHECK mirrors `report.entity.ts`; node's own labeler representable.
- [x] P14-005 — database: `appeals`, `moderation_log_entries` + migration; `domain_blocks.reason_category/source`. Owns `packages/database/src/entities/{appeal,moderation-log-entry,domain-block}.entity.ts`, `migrations/`. Acceptance: `appeals.admin_audit_log_id` UNIQUE; CHECK `subject_domain` set iff `subject_kind='domain'`.
- [x] P14-006 — database: `account_deletion_requests`, `account_exports` + migration. Owns `packages/database/src/entities/account-{deletion-request,export}.entity.ts`, `migrations/`. Acceptance: `purge_after`/`expires_at` support §204 defaults.
- [x] P14-007 — server: `FilterService` — create/update/delete/list/export/import, wired into the existing `applyVisibilityFilter` chokepoint, literal-term matching only (substring/word/tag/actor/domain), bounded over-fetch for non-indexed kinds. Owns `apps/server/src/modules/filters/**`. Acceptance: hide omits rows server-side; no user regex path; pagination correct under hide.
- [x] P14-008 — server: `FilterListService` — publish/update/delete/get/list/entries/subscribe/unsubscribe/exceptions, live evaluation, provenance in `filtered_by`. Owns `apps/server/src/modules/filter-lists/**`. Acceptance: subscribing never writes a blocks row; unsubscribe immediate; subscriber counts never returned.
- [x] P14-009 — server: `LabelService` — labeler CRUD, `ApplyLabel`/`RetractLabel` (rate-limited), subscribe/unsubscribe, per-value action map, `ListLabelsOnSubject`; seed the node's own labeler. Owns `apps/server/src/modules/labels/**`. Acceptance: values outside `GetNodePolicy` vocabulary rejected; no notification on label; labels never shown to unsubscribed viewers.
- [x] P14-010 — server: `PrivacyService` — `AcknowledgePrivacyNotice`, `Get/UpdatePrivacyPrefs` (locked + follow requests), `ExportAccount` (worker job + presigned URL), `RequestAccountDeletion`/`CancelAccountDeletion` + grace-period purge job; `patches-admin user delete` uses the same path. Owns `apps/server/src/modules/privacy/**`, `apps/worker/src/handlers/{export,purge}*.ts`, `apps/admin/src/commands/user.ts`. Acceptance: export/purge never synchronous; purge idempotent + audited.
- [x] P14-011 — server: moderation notices + appeals — `ListMyModerationNotices`, `AppealService` (`CreateAppeal/GetAppeal/ListMyAppeals`), notice projection from audit log; `patches-admin appeal list|inspect|resolve`. Owns `apps/server/src/modules/moderation/**` (notices, appeals), `apps/admin/src/commands/appeal.ts`. Acceptance: notice explanation never exposes `reports.moderator_note` (§55); one appeal per audit entry.
- [x] P14-012 — server: public moderation log + node policy — `ListModerationLog` (anonymized account/post entries, identified domain entries), `NodeService.GetNodePolicy`. Owns `apps/server/src/modules/moderation/moderation-log.*`, `apps/server/src/modules/system/node.*`. Acceptance: no handle/actor-id/post-id retrievable from account/post-kind entries.
- [x] P14-013 — server: close the outbound domain-block gap (recipient resolution must filter `domain_blocks`); `patches-admin domain review-list <file>`. Owns `apps/server/src/modules/federation/services/activitypub-federation-gateway.service.ts`, `apps/admin/src/commands/domain.ts`. Acceptance: blocked domain never enqueues delivery; imported list never auto-writes blocks.
- [x] P14-014 — `packages/domain/src/limits.ts` additions for filters/lists/labelers/appeals/export/deletion (§204). Acceptance: every §204 limit has a named export; no duplicated magic numbers.
- [x] P14-015 — TUI: privacy screen (`,` submenu / `:privacy` / `patches privacy`), registration notice, discoverability + locked prefs, follow-request flow. Owns `apps/tui/src/screens/PrivacyScreen.tsx`, `app/keymap.ts` (commands only, no new global keys). Acceptance: no key rebound; registration shows the notice per §197.1.
- [x] P14-016 — TUI: filters / filter-lists / labelers screens + `patches filter|lists|labelers`. Owns `apps/tui/src/screens/{Filters,FilterList,Labelers}Screen.tsx`. Acceptance: provenance ("filtered: name (via @author)") always rendered.
- [x] P14-017 — TUI: appeals + moderation-log screens (`patches appeal|modlog`), notices via notifications. Owns `apps/tui/src/screens/{Appeals,ModerationLog}Screen.tsx`. Acceptance: `?` help + `docs/user-guide.md` list every new command.
- [x] P14-018 — web: `/settings/{privacy,filters,lists,labelers}`, `/moderation/log`, `/appeals` at safety-surface parity with the TUI. Owns `apps/web/src/routes/settings/**`, `apps/web/src/routes/moderation/**`. Acceptance: every safety action reachable in the web client (§205).
- [x] P14-019 — docs sync: `docs/user-guide.md`, `docs/architecture/federation.md` (§201.5 gap closure, domain-policy transparency), `docs/architecture/data-model.md`. Acceptance: no documented command that doesn't work; status markers flipped as pieces ship.
- [x] P14-020 — cross-cutting verification: §208 prohibition sweep + §209 checklist sign-off with evidence (file:line or test). Spawns reviewer/spec-auditor.
- [x] P14-021 — SQL pushdown for ACTOR/TAG hide filter rules + PSL-based domain matching (§198.2 phrase matching, §199.4 domain subscripts)
- [x] P14-022 — real `scopes` field for filter-list subscriptions (proto + ADR for scope types; §199.1 intersection)
- [x] P14-023 — multi-file account export archive including media bytes (§204.2 format, list endpoint, tarball composition)
- [x] P14-024 — purge scope expansion: bookmarks/reposts/community memberships/tag mutes + cascade constraints (§204.1)
- [x] P14-025 — RegisterRequest explicit privacy-notice acknowledgement field (proto) + REQUIRE_PRIVACY_ACK enforcement (§197.1)
- [x] P14-026 — admin CLI to mark a vocabulary value `mandatory` for the node labeler (§203 mandatory-category enforcement)
- [x] P14-027 — moderation_log_entries writes from admin user/report commands + audit trail linkage (§55 explanation never exposed)
- [x] P14-028 — locked accounts + follow requests end-to-end (server + TUI + web), mutual-follow edge in follow list (§182.5)

- [x] P14-029 — enforce stored discoverability prefs (§197.5): `discoverable=false` omits the actor from `SearchActors`/directories (exact-handle resolution still works), `indexable=false` excludes their posts from `SearchPosts`, `show_in_local_feed=false` hides their public posts from the local timeline; integration tests per control; docs/product/privacy.md flipped from planned
- [x] P14-030 — node policy `PUBLIC_READ` (default true): when false, anonymous callers get only system/node-info/auth/health + federation surface; `SIGN_IN_REQUIRED` error; clients show a sign-in prompt (owner decision 2026-08-19: invite-only gates posting, not reading)

## Phase 15 — passwordless auth (owner request 2026-08-19; ADR 0011 credential model, §165–§168)

- [ ] P15-001 — enable GitHub device-flow login on prod: create the OAuth App (device flow on), `flyctl secrets set GITHUB_CLIENT_ID/GITHUB_CLIENT_SECRET`, verify `patches login --github` end-to-end, document in `docs/operations/deployment.md`
- [x] P15-002 — node policy switch `PASSWORD_AUTH=off|optional|required` (env + `GetNodePolicy`/`GetNodeInfo` capability): `off` rejects `Login`, password `Register`, `AddCredential(PASSWORD)`; clients hide password UI when off; tests both states
- [x] P15-003 — recovery codes credential type (one-time codes shown at enrollment, hashed at rest, `RecoveryLogin`), so an SSH/passkey-only account can recover without email/password; TUI `patches recovery-codes`
- [x] P15-004 — passkeys/WebAuthn (`PASSKEY` credential, `BeginPasskeyRegistration/CompletePasskeyRegistration/BeginPasskeyLogin/CompletePasskeyLogin`, `@simplewebauthn/server`, migration, RP id = node origin); web client register/login/manage; ADR amending 0011's deferral now that a browser RP exists
- [x] P15-005 — web: GitHub device-flow login page + "approve this web login from your terminal" device-link (`BeginDeviceLink` code → `patches approve <code>` over an SSH-authenticated session → web polls → session); no central SSO anywhere _(verified complete 2026-08-22 — the stale note below was wrong: `github_auth`/`oidc_providers` gating, `DeviceLinkButton` ("approve from your terminal"), and `apps/tui/src/cli/approve.ts` (`patches approve <code>`, explicit confirm, `--yes`) all exist and are wired; no `TODO(P15-006)` remains in apps/web)_
  - GitHub-login half done: `apps/web/src/components/GitHubLoginButton.tsx` + test, wired into `apps/web/src/routes/LoginRoute.tsx`. Gated on `GetAuthPolicyResponse.githubAuth` once that field lands (P15-006 landing it now) — left a `TODO(P15-006)` since it doesn't exist yet, button renders unconditionally. Device-link half (`BeginDeviceLink`/`patches approve`) still needs its proto+server RPCs and remains open.
- [x] P15-006 — generic OIDC-device-flow credential provider (GitLab/Codeberg/any) reusing the GitHub credential pattern; provider list from node config; credential never becomes identity
  - Server-side only (proto + config + service, no client UI): `BeginOidcLogin`/`PollOidcLogin` RPCs, `OidcDeviceFlowService`/`OidcLoginAttemptsService` mirroring the GitHub pair, `OIDC_PROVIDERS` node-config JSON array (id/displayName/URLs/clientId/clientSecret, validated by zod), `OIDC` credential type (`identifier = "<provider_id>:<subject>"`, migration `AddOidcCredentialType`). Also added `GetAuthPolicyResponse.github_auth`/`oidc_providers` (id+displayName only) that P15-005's web login page needs. TUI/web wiring to actually render OIDC login buttons is not part of this task.
- [ ] P15-007 — TUI/web credential manager parity: list/add/revoke every type, last-credential guard surfaced, docs `docs/architecture/auth.md` + user guide
  - Web half done: `authInterceptor` (`apps/web/src/api/client.ts`) discriminated per-RPC instead of blanket-skipping all of `AuthService`, which had silently starved `ListCredentials`/`RevokeCredential`/`GenerateRecoveryCodes`/`BeginPasskeyRegistration` of their bearer token too, not just `BeginGitHubLogin`/`BeginOidcLogin`; `CredentialsRoute` now has a "Link another account" section reusing `GitHubLoginButton`/`OidcLoginButton` in a new `mode="link"`. Not exercised against a live node — `GITHUB_CLIENT_ID` is unset on prod (P15-001) — covered instead by `apps/web/src/api/client.test.ts` against a mocked transport. Left open because TUI still has no GitHub/OIDC linking flow at all (`AccountsScreen`'s add flow only discovers local SSH keys) — see P15-008.
- [x] P15-008 — TUI: GitHub/OIDC credential linking flow (`AccountsScreen`'s `a` add-credential flow currently only discovers local SSH keys via agent/`~/.ssh/*.pub`; no path reuses `BeginGitHubLogin`/`BeginOidcLogin` with an authenticated session the way the web client's new `CredentialsRoute` "Link another account" section does, P15-007). Needs a device-flow UI in the terminal (print user code + verification URL, poll) parallel to the TUI's own login-time GitHub/OIDC flow if one exists, or a new one if it doesn't.
  - Already shipped: AccountsScreen implements both GitHub and OIDC device-flow linking with polling and completion states. The board row was stale; verified 2026-08-25 by reading the code, and fake-api test coverage added.

## Phase 16 — live bugs from owner testing 2026-08-19 (highest priority)

- [x] B-040 — **clients don't attach the session token to reads**: with `PUBLIC_READ=false` a signed-in user gets `SIGN_IN_REQUIRED` ("This node requires sign-in to read") on Profile/edit-profile/most screens, because `apps/tui/src/api/client.ts`'s `unary()` only sends auth when a call site passes `accessToken` explicitly (~80 methods, most reads pass nothing). Fix centrally: hold the current access token in the api client (set on login/restore/refresh, cleared on logout) and attach it to **every** call when present; explicit per-call tokens still win. Same audit for `apps/web` (Connect auth interceptor) and `packages/client`. Acceptance: with `PUBLIC_READ=false`, a signed-in TUI can read every screen it could read when open; signed-out gets the sign-in prompt, not a crash.
- [x] B-041 — **web sign-in fails with "Do not know how to serialize a BigInt"**: `apps/web/src/api/session.ts:46` does `JSON.stringify({actor})` and protobuf-es `Actor.createdAt` is a `Timestamp` whose `seconds` is a `bigint`. Store a plain UI-only subset (id/handle/displayName/avatarUrl/flair) or serialize with `toJson(ActorSchema, actor)`. Add a regression test that round-trips an actor with timestamps through the store.
- [x] B-042 — TUI navigation: `g <key>` "go to" jumps are broken/unreliable after the shell rework (owner: "any go to doesn't work"); split-pane opens unexpectedly on navigation. Give the user explicit control: default `g <key>` = replace current screen; `Ctrl+g`/`z <key>` (or a preference) = open in the second pane; never auto-split on a plain navigation. Back/forward navigation must be symmetric through the nav stack.
- [x] B-043 — command palette is not a true overlay: it repaints/steals the screen under it (home shows "Loading" instead of the frozen feed while the palette is open), and arrow keys don't scroll the result list with the selection (j/k select but the viewport doesn't follow). Fix: palette renders over a frozen `renderToString` snapshot per ADR 0018, and the palette list uses the measured `VirtualList` (selection always visible, arrows == j/k).
- [x] B-044 — web home: the signed-out/`PUBLIC_READ=false` state and the "Local" tab are confusing. Rename the tab to something a newcomer understands ("Everyone here" / node feed) with a one-line explainer, hide it entirely when the node is closed to logged-out reads, and show a proper "this node is invite-only — sign in or ask for an invite" panel instead of an error.
- [x] B-045 — web: theme support (light/dark/follow-system) + a real appearance settings surface (owner: "must have"). No flash of the wrong theme on load, no server round-trip (client-side only, spec §184.3 — cosmetics never gate function), contrast held in both themes _(`apps/web/index.html`'s inline pre-paint script + `apps/web/src/lib/theme.ts` (persisted `localStorage` preference, `<html data-theme>`), `apps/web/src/index.css` restructured from a bare `prefers-color-scheme` media query into `data-theme`-scoped variable blocks with an OS-tracking fallback when unset/`system`, `prefers-reduced-motion`-guarded transition; `apps/web/src/hooks/useTheme.ts`; new `/settings/appearance` route (`AppearanceSettingsRoute.tsx`), first in `SettingsLayout`'s nav. No other undelivered client-side preference existed in the code to surface alongside it — checked via a `localStorage`/"preference" grep first.)_
- [x] B-046 — TUI: once a split pane is open (e.g. `Ctrl+G v`), there was no way to move keyboard focus to the second pane, and no visible indication of which pane was focused — worse, action keys kept acting on the _unfocused_ pane's subject (owner: "once you're on a subpage, idk how to edit stuff on the split… how do I move focused pane? And something to indicate which pane is focused"; reproduced exactly as "I do `g v` to go to my page, I press `Shift+E` to edit, but it tries to edit the post that's highlighted on the left side"). Fix: `Tab` moves shell focus between panes (global, `keymap.ts`), the focused pane's title is marked with a leading `>` and bold (`SplitPane.tsx`, visible under plain mode too — not colour-only), and every action key now dispatches only to the focused pane's screen (`App.tsx`'s `renderEntry` gates each pane's own `useInput` on `focusedPane`, removing a redundant primary-only gate that had forced every list-kind screen in the secondary pane permanently inactive regardless of `Tab`) _(`apps/tui/src/app/App.tsx`, `apps/tui/src/app/keymap.ts`, `apps/tui/test/pane-focus.test.tsx`, `docs/user-guide.md`)_
- [x] B-047 — TUI: add theme support and user configuration. Most of the engine already shipped under P12-101/103/113/117/127 (six built-in themes + user JSON themes at `$XDG_CONFIG_HOME/patches/themes/`, `--theme`/`PATCHES_THEME`/saved-preference precedence, live preview + Esc-revert, a glyph-set engine, per-node+actor `preferences.json` with atomic writes and validated/corruption-safe reads, colour degradation via Ink/chalk honoring `NO_COLOR`) — auditing it found two preferences that existed in code but were never actually wired end to end: `glyphSet` was resolved nowhere at startup and never reached its one render call site (always `'unicode'` regardless of `PATCHES_GLYPHS`/saved preference), and linear mode (`:linear`/`PATCHES_LINEAR`, P12-118) had no Preferences-screen row. Fixed both: `App.tsx` now resolves `glyphSet` via `resolveGlyphSet` at mount, reconciles it against the saved per-account preference the same way theme/plain/linear already were, threads it to `MessagesScreen`, and round-trips it through save/cancel; added a "Linear mode" row to `PreferencesScreen` wired to the same lifted `linearMode` state, with Esc-revert. Documented the whole feature end to end, which had no user-facing docs at all beyond the Theme row itself: custom JSON themes, `PATCHES_GLYPHS`, the `preferences.json` location and corruption fallback, and colour-degradation/`NO_COLOR` behavior (`docs/user-guide.md`), plus a new architecture section (`docs/architecture/tui.md` §7c). Added a `linearMode` round-trip test to `preferences/store.test.ts` (malformed-file fallback was already covered by existing `it.each` cases). No new keybinding — the Linear mode row reuses the existing row-cursor/`h`/`l`/`Space` grammar _(`apps/tui/src/app/App.tsx`, `apps/tui/src/screens/PreferencesScreen.tsx`, `apps/tui/src/screens/PreferencesScreen.test.tsx`, `apps/tui/src/preferences/store.test.ts`, `docs/user-guide.md`, `docs/architecture/tui.md`)_
- [x] B-048 — TUI: add `Ctrl+W h` / `Ctrl+W l` as a directional alias to `Tab`'s pane-focus toggle (B-046) — `h` always focuses the primary pane, `l` always the secondary; a no-op when the screen isn't split. `docs/architecture/tui-interaction-model.md` had originally specified this and was rewritten during a docs pass to describe only `Tab`, on the mistaken assumption the vim alias had been superseded rather than simply unbuilt — the owner still wanted it (tmux/vim muscle memory), so both are documented now. Reproduces and fixes the coalesced-chunk hazard for a control-byte prefix: Ink's `parseKeypress` only recognises a ctrl+letter chord when the byte arrives _alone_ (`ink/build/parse-keypress.js`'s `s.length === 1 && s <= '\x1a'` case) — a fast-typed `Ctrl+W` immediately followed by `h`/`l` lands in one stdin chunk as the literal two-character string `'\x17h'` with `key.ctrl` already `false` on the whole event, so naively replaying each split character with the same stale `key` object (what the existing `g <key>` coalescing already did) loses the ctrl flag and the chord never fires. Added `splitCoalescedKeyRun` (`app/input.tsx`) to reconstruct each raw control byte (`\x01`-`\x1a`) into its ctrl+letter key exactly as a standalone press would parse, and switched `App.tsx`'s coalesced-run replay to use it. The new prefix is guarded off legacy text-entry screens (`compose`/`postEdit`/etc. and the quick-post overlay) because `ComposeScreen`'s `TextEditor` already binds its own `Ctrl+W` to kill-word-back and Ink has no stop-propagation — both listeners see every keypress, so without the guard the shell would swallow the character typed immediately after a word-delete _(`apps/tui/src/app/input.tsx`, `apps/tui/src/app/input.test.ts`, `apps/tui/src/app/App.tsx`, `apps/tui/src/app/keymap.ts`, `apps/tui/test/harness.tsx`, `apps/tui/test/pane-focus.test.tsx`, `docs/user-guide.md`, `docs/architecture/tui-interaction-model.md`)_

## Phase 17 — scale, concurrency and harness

- [x] S-001 — capacity + concurrency plan for the flagship node: connection/stream limits on the gRPC + Connect edge, per-IP and per-actor rate-limit review under burst, DB pool sizing vs Neon connection limits (pgbouncer?), N+1 audit on the hot feed paths, keyset-pagination cost under deep scroll, media/R2 hotlinking, Fly autoscale + `kill_timeout` drain behaviour, and a documented load test (k6 or autocannon over Connect) with numbers in `docs/operations/capacity.md`. Acceptance: measured p50/p95 for home/local/thread/search at 1/10/100 concurrent readers on one machine, and a written "when to scale what" runbook.
- [x] S-002 — abuse/spike protection: request cost budgets per RPC class, circuit-break the worker outbox under backlog, cap fan-out on notification writes, and a load-shedding mode that keeps reads working when writes are saturated.
- [x] H-010 — **harness meta-analysis: cut cache-read amplification** (owner ask, 2026-08-19; ~6B cache-read tokens this session against ~10–20M unique reads). Deliverable: `docs/agents/CONTEXT_ECONOMY.md` + concrete harness changes. Measure first (parse `~/.claude/projects/**/**.jsonl` for per-agent turn counts × context size to find where the amplification actually is), then attack the integral of live-context-size across model turns, not "tokens per read": (a) fewer model round trips per agent — batch reads in one wave, batch edits in one wave, never re-read to confirm an edit; (b) move validation into a `PostToolBatch` hook that runs format/typecheck/affected tests outside the model and injects only PASS or the trimmed diagnostics; (c) `PostToolUse` output filtering to shrink Bash noise and suppress identical repeat reads; (d) generational workers — kill and re-brief with a compact state packet instead of letting a repair loop drag a fat prefix; (e) shrink the per-agent baseline (CLAUDE.md/rules/skills/tool schemas are re-read every turn by every agent); (f) evaluate Workflow-driven fan-out so the orchestrator stops accumulating hundreds of worker completions. Explicitly NOT wanted: bespoke "auto-runs-tests-and-guesses" tooling that removes agent judgement. Acceptance: a before/after measurement on one representative task wave.
- [ ] H-011 — nested-delegation experiment for H-010: sonnet implementer whose only tool is `Agent`, delegating read/edit/verify waves to haiku sub-agents so the read/edit churn accumulates in the cheapest context and the expensive context stays thin. Measure against the H-010 baseline before adopting; keep whichever wins, document the result either way.

- [x] H-012 — enable mise MCP support (current releases do not require the broad experimental switch) and expose high-signal project tasks to Codex/Claude agents with least privilege, concise machine-readable output, and documented setup/troubleshooting. Update agent permissions/tool-schema guidance so MCP improves ergonomics without reintroducing context amplification; verify via an actual agent MCP call.
- [ ] H-013 — Harness foundations: promote reusable, network-free test primitives into `@patches/testkit` — `mintInvite`, a real-Argon2 credential factory (document why ordinary factory hashes cannot log in), `waitFor`, and safe DB-only helpers. Do not add an outbox claimer that could steal real worker jobs, and keep all process/RPC registration logic in the harness (H-015); product packages must not import the harness.
- [ ] H-014 — Create `packages/harness`: TypeScript-owned local lab lifecycle and `patches-harness` CLI, absorbing `infra/lab/fed-lab.sh` and duplicated process-spawn patterns. `up [--nodes N] [--federation]` builds, creates/migrates isolated DBs, creates ephemeral Ed25519 keys, starts server/worker children, records PIDs/logs, and waits for `/healthz`; implement idempotent `down`, `status`, and lab-only `reset`. Keep thin `mise run lab*` wrappers and retire the shell lab only after an end-to-end two-node proof.
- [ ] H-015 — Harness provisioning/worlds: expose real gRPC-level create/register/login actions using `@patches/client` + testkit, and an idempotent declarative world-file (`users`, graph, posts, channels) with `apply`. Use the same world format for integration/browser tests, local demos, and `packages/bench`; retarget demos away from production and prohibit non-idempotent live writes.
- [ ] H-016 — Deferred lab-only arbitrary-user sessions: first determine whether the real `TokenService` issuance path can be safely reused by the local harness so an agent may assume an existing lab actor. If not, require an ADR/security review before any dev signer that reproduces token claims; refuse unless an explicit lab environment guard is enabled. Never ship, deploy, or expose this capability through `apps/admin` or product MCP.
- [ ] H-017 — Harness observation helpers: add notification polling (`ListNotifications`/`GetUnreadCount`, no invented streaming), externally observable waits for work processed by the actual worker (never a harness outbox claimer), Mailpit code retrieval, and request-ID-correlated/redacted log viewing (`logs --node --grep --request-id`). Document that agents must not await a DM notification until B-098 lands and that no DM body/message table data may enter harness logs.
- [ ] H-018 — Browser E2E and live-development loop: configure a browser harness against the local lab that can create/login a test account, compose a post, verify rendered UI and capture screenshots; make it a required smoke proof for web changes. Add ergonomic local preview commands and documented remote/PR-preview attachment with a disposable world, readiness checks, cleanup, and diagnostic logs — never run destructive harness actions against production.
- [ ] H-019 — Harness MCP adapter (last): `patches-harness mcp` stdio adapter with tools mapped 1:1 to the already-tested CLI command functions, local/lab target validation, explicit destructive-action confirmation semantics, and `.mcp.json` registration. This is a development-only shell, distinct from the user-facing product MCP work B-069–075, and must never be deployed.
- [ ] H-020 — Harness documentation and acceptance suite: document the package boundaries (`testkit = rows`, `harness = processes + RPCs`, `admin = audited operator verbs`, `MCP = delivery`), world schema, lab safety model, local/remote/browser workflows, log redaction, and teardown. Add a repeatable clean-machine acceptance transcript covering world apply, login, post, notification/job observation, browser visual proof, and teardown.
- [ ] H-021 — Cross-agent orchestration policy: update Codex and Claude harness instructions so a frontier orchestrator gates scope, delegation, review, and completion while routine exploration/mechanical edits/checks use the cheapest fast worker available; require bounded/disjoint ownership, short state packets, stronger review than implementation, and measured cost/latency outcomes. Preserve human authority and all hard architectural/security rules.
- [ ] H-022 — Harness-adjacent consolidation audit and repairs: correct stale `apps/admin` usage text (`deletion-status`, `cancel-deletion`), derive root integration-project discovery instead of a hard-coded list, document/audit only accidental session-code duplication while preserving ADR 0023/P10-013's intentional TUI separation from `@patches/client`'s `SessionManager`, and complete the demo-to-lab migration from H-015. Split any product-facing or ADR-changing remediation into separately scoped tickets before implementation.
- [ ] H-023 — CI/verification economy: schedule affected workspace checks for pull-request feedback with concise machine-readable diagnostics; run the operational browser smoke as a separate job from unit/type/lint checks; upload browser traces, screenshots, logs, and other bulky artifacts only on failure. Preserve full `mise run verify` as the local commit gate and required protected-merge gate, with no affected-test shortcut at final acceptance. Record before/after wall-clock latency and CI compute consumption on a representative change set, and document the fallback when affected-project discovery is uncertain.
- [ ] H-024 — Safe remote-preview attach: attach only to an existing PR preview selected by deterministic repository + PR identity, over HTTPS/443, after health and capability checks. Run real RPC world actions and browser smoke against that preview without duplicating Neon/Fly/Pages create/deploy/destroy lifecycle; persist no credentials, secrets, tokens, cookies, or remote state locally, and clean up only application-level fixtures created by that run. Refuse attachment unless preview metadata records an approved normal, anonymized, or schema-only parent-data policy.
- [ ] H-025 — Signed Git-agent preflight: before agent commit/push workflows, require an SSH Git origin, a live `SSH_AUTH_SOCK`, the expected signing-key fingerprint available from the agent, and repository signing-required configuration. Preserve complete push stderr in failure diagnostics; never disable or bypass commit signing as recovery.
- [ ] H-026 — Schemathesis contract-fuzzing spike and guarded integration: verify compatibility against official documentation and the repository's real HTTP/Connect contract rather than inventing an OpenAPI surface; if compatible, run bounded fuzz cases only against disposable local labs or H-024-approved previews. Default-deny mutation/destructive operations, require an explicit safe-operation allowlist and per-run fixture namespace, refuse production-like targets, redact artifacts, and prove cleanup; if incompatible, record the evidence and retain the existing protobuf/transport fuzz path.
- [ ] H-027 — Repeatable load/capacity suite: consolidate and extend S-001/P19-007 into runnable scenarios for auth, home-feed reads, post creation, notifications, and federation-worker delivery. Record p50/p95/p99, error rate, DB time/query count, worker lag, cold-vs-warm results, and CPU/memory/connection saturation under a versioned fixture shape; report whether each limit is a resource ceiling or architectural bottleneck and keep every timeline chronological.
- [ ] H-028 — Chronological-feed latency budget and regression gate: establish measured local/CI budgets for representative home/local/thread reads at fixed fixture sizes and concurrency, including DB time and query count; fail on a statistically meaningful regression with a documented rerun/noise policy. The gate must never add ranking, engagement scoring, trending, offset pagination, or a timeline `sort`/`order` control.
- [ ] H-029 — DEFERRED / DO NOT START YET — evaluate Storybook for isolated web component states, accessibility review, and visual regression only after the browser harness (H-018) is stable. Research current official Vite/React compatibility, maintenance/CI cost, and overlap with Playwright before deciding whether to install it; an evaluation may conclude that existing route fixtures are sufficient.
- [ ] H-030 — NestJS Doctor compatibility and scoped checks: verify the official Doctor tool supports the pinned NestJS 11/pnpm monorepo configuration, record limitations, then integrate only supported high-signal checks into the server's scoped local/CI path with concise machine-readable diagnostics. No generated recommendation may override repository layering, TypeScript 5.9, or hard rules, and full `tsc --noEmit`/`mise run verify` remain authoritative.
- [ ] H-031 — Measured SWC adoption evaluation: benchmark supported SWC use for Nest development/test/build and Vitest transforms against the current compiler on representative cold/warm runs; verify legacy decorators/metadata, source maps, coverage, stack traces, and test semantics before any opt-in. Retain `tsc --noEmit` as the type-safety gate, document rollback, and adopt only with a material measured speedup and no diagnostic loss.

## Phase 18 — federating social depth: reposts, tags, quotes (ADR 0028 scope)

> Owner asked for "full federation" 2026-08-22. Architect review (read-only) scoped it: **reposts → tags → quotes**
> federate via ActivityPub (Announce/Undo, Hashtag arrays, FEP-044f + 3 legacy fallback props). **Communities stay
> local** (Group-actor pattern unverified, §195.2 needs owner sign-off + own ADR). **DMs/E2EE never cross the seam**
> (ADR 0020 §13, §194 — hard prohibition). No §109 gate, §160 item, or §194 prohibition moves. Sequential deps;
> same-wave tickets are file-disjoint.

- [x] P18-001 — Re-verify `docs/research/activitypub-social-depth.md` against current upstream (§155); land ADR 0028 + index row; flip `federation.md` §7.6 to decided. Owns `docs/research/activitypub-social-depth.md`, `docs/decisions/0028-*`, `docs/decisions/README.md`, `docs/architecture/federation.md` _(landed 2026-08-22: ADR 0028 Accepted — reposts→tags→quotes, communities deferred to their own ADR + owner sign-off, DMs/E2EE prohibited through the seam per ADR 0020 §13; FEP-044f still DRAFT re-confirmed with dates; `_misskey_quote` trailing-slash divergence documented)_
- [x] P18-002 — Schema: `reposts.remote_activity_uri` (nullable, indexed — links remote Announce to local pointer row so remote Undo finds it); `quote_authorizations` lifecycle table (issue/verify/revoke). Owns `packages/database/**`. Acceptance: `pnpm db:migrate` up/down clean on real Postgres; no synchronize _(landed 2026-08-22: unique index on remote_activity_uri (NULLs exempt for local reposts), quote_authorizations with policy/state CHECKs + self-quote guard + FKs, migration registered in ALL_MIGRATIONS and verified up→down→up against compose Postgres; phantom filter_list_subscriptions drift stripped per B-077)_
- [ ] P18-003 — Outbound reposts: `announceRemotePost`/`unannounceRemotePost` on `FederationGateway` + ActivityPub impl + `reaction.service.ts` wiring. **Deterministic Announce ids reconstructed from the repost row** — never `randomUUID()` (the current follow/like Undo path mints fresh inner-activity ids, B-079; do not replicate). Owns `federation-gateway.ts`, `activitypub-federation-gateway.service.ts`(+test), `apps/server/src/modules/reactions/reaction.service.ts`
- [x] P18-004 — Inbound `Announce` + `Undo(Announce)`: §180.1 eligibility re-checks, block/mute/domain-block, `remote_activity_uri` link; ingest remote `tag` arrays through the local grammar (failure drops the tag, never the post). Owns `inbox.service.ts`(+test) _(landed 2026-08-23: `handleAnnounce`/`handleUndoAnnounce` dispatched from the inbox switch; repost row linked by `remote_activityUri = activity.id`; author-block re-check at ingest; `tag[]` filtered to Hashtags and dropped — never the post — on grammar failure)_
- [x] P18-005 — Remote-object fetch service (fetch-by-URI: content-type validation `application/activity+json`/`ld+json`, bounded JSON, TTL + negative cache, origin budget, reuses `safeFetch`); never used for any DM path. Owns `remote-object.service.ts`(+test), `security/*` additions. Acceptance: SSRF tests (private IP, rebinding), size/depth caps, negative-cached 404/410 _(landed 2026-08-23: 5-min object cache + 1-min negative cache (404/410), 30 req/min per-origin token bucket, content-type + bounded-JSON + id/type shape validation, DM-path refusal before any fetch; SSRF/rebinding enforcement stays in `safeFetch`/`ip-guard` (its own tested module — `safeFetch` is mocked here so the suite is offline and deterministic) and the service passes the production/lab policy through verbatim; `SafeFetchError` is always wrapped into the service's `RemoteObjectFetchError` contract)_
- [x] P18-006 — Outbound tags + quotes: `buildNoteObject` emits `tag: [Hashtag]` and `quote` + `quoteUri`/`quoteUrl`/`_misskey_quote`; `quote_policy` → interaction-policy mapping (no invented `following` value). Owns `activitystreams/documents.ts`(+test), gateway publishPost path _(landed 2026-08-23: publishPost reads `post_tags` (deterministic `(created_at, tag_id)` emit order) and emits `as:Hashtag` entries with deterministic `${postUri}#tag-…` fragment ids, `href` omitted (node has no tag page); quote linkage emits all four properties; policy maps ANYONE→`as:Public`, FOLLOWERS→followers URI, NOBODY→author URI via additive `interactionPolicy.canQuote` — all strictly additive, pre-P18-006 shape unchanged for the outbox serializer)_
- [x] P18-007 — Inbound quotes: first-recognizable property wins; resolve via P18-005; §180.2 enforcement — unverifiable/violating quote ingests as a plain post, never an endorsed one; record `quote_authorizations` on receipt. Owns `inbox.service.ts` quote branch (+test). Deps: P18-004/005/006 _(landed 2026-08-23: `quote`>`quoteUri`>`quoteUrl`>`_misskey_quote` precedence incl. both Misskey IRI slash spellings, resolution through `RemoteObjectService.fetchObject` only; NOBODY/unverifiable/unrecognized-policy/blocked-author → plain post, ANYONE + FOLLOWERS-with-follow-evidence → linkage + `quote_authorizations` VERIFIED row (claimedPolicy recorded), self-quote links with no row per FEP-044f; Create now ingests note-level Hashtag tags via the shared helper (also fixed: leading `#` now stripped so federated tags canonicalize like local ones); tag/quote failure never drops the post; `quoteAuthorization` stamp dereference deferred to P18-008 validation)_
- [ ] P18-008 — Two-node lab round-trip: repost/unrepost, tag feed inclusion, quote of remote + local-from-remote, stable Announce id through Undo; transcript appended to `docs/operations/federation.md`. Owns `federation-two-node.integration.test.ts`, `test/support/federation-node.ts`, `infra/lab/fed-lab.sh`
- [ ] P18-009 — TUI/web: remote repost attribution + one-level remote quote embed render with remote-origin fixtures; plain-mode forms; no new keys (§191). Owns `apps/tui/src/components/{PostRow,PostList}.tsx` tests + fixtures
- [ ] P18-010 — Docs sync: `federation.md` status/§5 checklist, `docs/operations/federation.md`, `docs/architecture/social.md` (still missing, P11-014). Acceptance: no shipped/planned drift; every documented command actually run

## Phase 19 — observability, measurement & scale hardening

> Owner-directed (2026-08-22) following the architecture/scale audit + ADR
> [0029](docs/decisions/0029-scale-path-banned-tech-language.md) and
> [`docs/architecture/tooling-recommendations.md`](docs/architecture/tooling-recommendations.md).
> Complements (does not displace) the open follow-ups elsewhere (P14-021…026, P15-001/007/008,
> P18-003…010, P10-005 TUI-on-SDK, RN paused, P12-119…121 blocked, E2EE gate). Phase 17 (S-001/S-002)
> already set the capacity _budgets_ and a load-test _plan_; this phase **wires the live
> instrumentation to measure them** and acts on the audit's structural findings.
> Guiding rule: measure before optimizing (`INITIAL_VISION.md` §126); anything touching a §153
> prohibition needs a spec revision + sign-off.

### Instrumentation foundation (adopt near-term, no new architecture surface)

- [x] P19-001 — Structured logging with **pino** / `nestjs-pino`, replacing Nest's default `ConsoleLogger`; keep the JSON contract (`overview.md` §8); assert no secrets/tokens in logs. Owns `apps/server/src/main.ts` + logging module; mirrors current worker logging. _(landed 2026-08-23: `apps/server/src/logging/pino-logger.module.ts` — redacts auth/cookie/token/password/secret paths, carries `traceId` from `x-request-id`/`x-b3-traceid`, skips `/healthz`+`/metrics`)_
- [x] P19-002 — **OpenTelemetry** SDK + auto-instrumentation (`instrumentation-pg`, `-nestjs-core`, `-grpc`, `-http`) emitting OTLP to an operator-chosen collector (Tempo/Jaeger/OTel Collector); no SaaS lock-in. Owns a new `packages/observability` instrumentation entry + `apps/server`/`apps/worker` bootstrap. Ties to B-091. _(landed 2026-08-23: `packages/observability/src/instrumentation.ts` — NodeSDK + auto-instrumentations, OTLP export gated on `OTEL_ENABLED`; Sentry is just an OTLP endpoint, no SDK lock-in)_
- [x] P19-003 — **Metrics**: `prom-client` `/metrics` (or OTel metrics) for RPC latency, DB pool usage, worker queue depth, feed-query latency. Reuses the `packages/observability` surface A-036 started for federation. _(landed 2026-08-23: `packages/observability/src/metrics.ts` + `metrics-server.ts` — prom-client `/metrics` on `METRICS_PORT`; rpc/http duration histograms, DB pool + worker queue-depth gauges; A-036 federation counters unchanged)_
- [x] P19-004 — Enable **`pg_stat_statements`** + set a default **`statement_timeout`** in `packages/database/src/data-source.ts`; document pool/timeout guidance in `docs/operations/neon-environments.md` (currently a gap). _(landed 2026-08-23: `DATABASE_STATEMENT_TIMEOUT` (default 10s) in `createDataSourceOptions.extra`; migration enables the extension)_
- [x] P19-005 — **Elasticsearch/OpenSearch: explicitly NOT a dependency.** Emit structured logs + OTLP/metrics; centralized log/metric search (OpenSearch) is an _optional operator sink_, never imported by the app (`tooling-recommendations.md` §Observability). _(landed 2026-08-23 in ADR 0029 + the tooling doc — ES/OpenSearch never imported by the app)_

### Measurement gate (the §126 suite S-001 planned)

- [x] P19-006 — **Search**: replace `SearchPosts` `ILIKE` scan (flagged worst read in `capacity.md`) with native Postgres full-text (`tsvector`/`to_tsquery`) — zero new dependencies. _(landed 2026-08-23: migration `…AddPostsFts` adds stored `tsv` + GIN index; `searchPosts` uses `websearch_to_tsquery` + `ts_rank_cd`)_
- [ ] P19-007 — Benchmark harness: realistic fixture generator, `EXPLAIN ANALYZE` on the home-feed query across large follow graphs, worker throughput test, connection-pool saturation test. Produces the baseline numbers ADR 0029's escalation path references (S-001 planned this; land it). _(harness landed 2026-08-23: `packages/bench` — fixtures + feed/worker/pool runners; baseline numbers still need a run against prod-shaped data)

### Structural hardening (audit findings)

- [x] P19-008 — **Worker horizontal scale**: verify N worker replicas claim safely via `FOR UPDATE SKIP LOCKED` (already concurrency-safe); make `WORKER_CONCURRENCY` env-tunable (exists) + raise default; expose queue-depth metric (P19-003). Per-instance ceiling today is the audit's first break. _(queue-depth gauge landed 2026-08-23: `getQueueDepth()` → `workerQueueDepth`, `WORKER_QUEUE_DEPTH_INTERVAL_MS`; SKIP LOCKED claim is already replica-safe — default concurrency stays until the P19-007 bench run says otherwise)_
- [x] P19-009 — **Global rate limits**: the per-RPC-class budgets in `capacity.md` are in-process per instance. Make them DB-backed (extend `rate-limit-bucket`) so they are global across instances, or document the N÷instances division — decide after measuring. _(landed 2026-08-23: `rate_limit_bucket` entity + atomic repo; `RATE_LIMIT_GLOBAL=true` swaps `RpcBudgetInterceptor` to the DB-backed limiter — off by default until measured)_
- [x] P19-010 — **Notification cleanup**: add a worker TTL/cleanup job for `notification` rows (currently unbounded; `refresh_token` already has `CleanExpiredTokensHandler`). _(landed 2026-08-23: `clean-expired-notifications.handler.ts`, daily 03:00 UTC, `NOTIFICATION_TTL_DAYS` default 90)_
- [ ] P19-011 — **DM realtime decision**: today is unary-poll (60s unread). Either design streaming RPC(s) + non-streaming fallback (ADR 0016 §104) or consciously defer and document the polling SLA. Product decision, not just engineering.

### Migrations hardening (scale concern)

- [ ] P19-012 — Online-DDL discipline + evaluate **Atlas** / **Reshape** (declarative + drift detection + online DDL) to complement TypeORM migrations; add a Neon-branch migration test harness (`docs/research/neon-branching.md`) so every migration is validated on prod-sized data before apply.

### Scale-path ports (measurement-gated, ADR 0029 — do NOT start until P19-007 baselines exist)

- [ ] P19-013 — Introduce a **`JobQueue` port** (worker already exists behind it); enables a future BullMQ/NATS swap without service edits.
- [ ] P19-014 — Introduce a **`Cache`/read-through port** (Valkey later, post-§153); deploy only when the benchmark shows read-latency/write-amplification breach.
- [ ] P19-015 — **Feed materialization** (hybrid push/pull, chronological) behind a flag when P19-007 shows the breach; never a ranking surface (§153/Amendment B).

<!-- Audit-completion swarm 2026-08-24 (post P13-016 internal-review-of-record; PR #67 merged) -->

- [x] B-107 — **device enrollment flow**: TUI `EnrollDevice` caller producing a real `LocalDeviceIdentity` (keygen, certificate signing against the account root, signed roster bump with peer-warning copy), capability-gated UI in Accounts/Devices, so the B-101 runtime stops being dormant behind `enrolled()`
- [ ] B-108 — **E2EE interop lab + capability enablement** (P13-014 slice): two-device/two-account round-trip against a real node through seal→fanout→open→decrypt incl. franking tag verification; on green populate `E2EE_APPROVED_FRANKING_PROFILES=patches-franking-v1` and flip capability reporting per B-094
- [x] B-109 — **envelope/prekey retention sweep** (audit P2): worker job deleting acknowledged envelopes past MAXLATENCY, consumed one-time prekeys, superseded signed prekeys; bounded batches + metrics _(landed 2026-08-25: already fully implemented on this branch — `e2ee-retention-sweep.handler.ts`, registered in job-dispatcher/job-runner.module, `E2EE_MAILBOX_MAX_LATENCY_MS` in `@patches/domain`, counters in `@patches/observability`; verified green and documented in `docs/architecture/jobs.md`)_
- [x] B-110 — **message arrival notifications**: content-free MESSAGE notifications (sender + conversation id only) wired into the E2EE accept path; §187 payload rules asserted by test _(obsolete duplicate of canonical B-098; retained for history)_
- [x] B-111 — **web honest degradation + ADR 0020 addendum**: web Messages state DMs are E2EE + terminal-only (kills false §183.1 notice, adds mode labels); docs record internal-review-of-record substitution, guarded-tier rollback boundary, anchor-ahead ritual, synced-home caveat

## Phase 20 — live bugs from owner testing 2026-08-26 (web PWA, highest priority)

<!-- Owner + partner testing on iOS PWA. tasks.md is the canonical board until an external
     tracker is set up — do not duplicate these into GitHub issues. -->

- [x] B-148 — **Web nav-style preference never applies**: selecting "stacked" instead of "radial" in settings leaves the nav radial for everyone — the menu is always radial regardless of the saved preference (`apps/web/src/hooks/useInterfacePreferences.ts`, `apps/web/src/lib/interfacePreferences.ts`, `apps/web/src/components/ThumbNavFab.tsx`). Find where the preference is read vs where ThumbNavFab picks its variant — likely the pref is persisted but never consumed (or consumed under a different key than the one the settings screen writes).
- [x] B-149 — **iOS PWA: the report-issue entry triggers an undo-typing action** — tapping report-issue (or its attach-screenshot control) in the iOS installed-PWA fires an undo/redo typing action instead of the intended UI. Suspect a keyboard/`contentEditable`/input-focus interaction or a shake-gesture ("shake to undo") triggered by focus loss; verify on iOS Safari PWA specifically, since the control works in desktop browsers.
- [x] B-150 — **Attach-screenshot button in the report-issue form does nothing** (`apps/web/src/components/IssueReporter.tsx`). Reproduce, fix (file input + preview, sized/limited), and cover with a test.
- [x] B-151 — **Send-issue gets stuck on "sending…" forever; nothing is ever sent** (`apps/web/src/components/IssueReporter.tsx`). Check the send path: is there a real backend/report endpoint wired (or a documented no-op), does the promise reject silently, is the stuck state never cleared on failure? Fix so it either sends or surfaces an honest error and returns to a usable state.
- [x] B-152 — **After sending/sending/dismissing a report, the app returns to the report-issue screen instead of the screen you were on** — losing your place. The reporter must restore the previous route/location on close, not land on its own entry screen.
- [x] B-153 — **Timeline required sign-out/sign-in to load (one-time?)** after the 2026-08-25 session. Probably a stale service-worker/cache or auth-token rotation artifact (B-121 territory). Diagnose the SW update flow; if it's a one-off from the deploy churn, document why and close with the explanation rather than a speculative fix.
- [ ] B-154 — **Preview teardown must also destroy the Cloudflare Pages web preview.** Confirmed 2026-08-26: teardown (16 successful runs, zero orphan Fly apps, Neon clean) never touches CF Pages — orphan `pr-N.patches-web.pages.dev` deployments remain live for pr-82, pr-83, pr-84, pr-85, pr-86, pr-94, each pointing at a destroyed backend. That's the likely cause of the partner's "web preview loads but login says it can't connect to the backend" (merged PR's preview). Fix: teardown deletes the Pages deployment/branch alias (wrangler/API) when `PREVIEW_WEB` was used; also verify a **live** (open-PR) web preview can actually log in against its Fly Connect edge — CORS `WEB_ORIGINS` set at deploy includes the branch alias, but confirm end-to-end on the next PR, and purge the six existing orphans.

## Phase 21 — web DX / robustness backlog (owner triage 2026-08-26)

<!-- tasks.md is the canonical board until an external tracker is set up. Suggested
     order per owner: PWA plugin → sonner → RQ devtools → cross-tab session → upload
     retry → diagnostics persistence → route error boundaries → web vitals → bundle
     analyzer CI. -->

- [ ] B-155 — **Replace `ToastProvider` with `sonner`** (60 lines → 1 import): accessible, promise API, dismissible, smaller bundle. Migration: swap all `useToast()` call sites, delete `apps/web/src/components/ToastProvider*`.
- [x] B-156 — **Replace the hand-rolled SW with `vite-plugin-pwa`** (~150-line swap: `apps/web/public/sw.js` + `apps/web/src/pwa/serviceWorkerRegistration.ts` + `usePwaInstall.ts` go away). Must preserve the behaviors B-153 just fixed/documented in `docs/research/web-sw-caching.md`: network-first navigations (including `!response.ok` fallback), one-shot `controllerchange` reload, honest install-prompt UX. **This is the next task after the Phase 20 fix wave lands.**
- [ ] B-157 — **React Query devtools in dev**: `@tanstack/react-query-devtools`, one line in `main.tsx` behind `import.meta.env.DEV`.
- [x] B-158 — **Error boundary per lazy route**: today only the router-level `RouteErrorBoundary` exists; wrap each lazy route so one route's crash degrades gracefully instead of blanking the app. _(landed 2026-08-25: `errorElement: <LazyRouteBoundary />` on every lazy child route in `router.tsx`, so the shell stays mounted; chunk-load failures get a distinct "reload page" message instead of "try again".)_
- [x] B-159 — **Media upload: retry + abort** — the XHR upload in `apps/web/src/lib/mediaUpload.ts` has no retry on network blip; add exponential backoff + `AbortSignal` support.
- [ ] B-160 — **Media upload: chunked/multipart for >100 MB** — R2 presigned PUT is single-part; needs multipart (server support too). Depends on B-159's abort plumbing.
- [ ] B-161 — **Session-expired UX**: `signOut()` is silent — user sees a blank screen until navigation. Add a "session expired" toast + auto-redirect to login (rides on B-155's sonner).
- [x] B-162 — **Diagnostics: persist breadcrumbs to `sessionStorage`** so they survive reload (currently lost on refresh). Redaction rules from `@patches/domain` diagnostics still apply.
- [ ] B-163 — **Replace `useShakeToReport` with `shake.js`** (2 KB, battle-tested) — custom threshold logic goes; ties into the B-149 iOS shake/undo interaction.
- [ ] B-164 — **`AbortController` on all React Query mutations** to prevent state updates on unmounted components.
- [ ] B-165 — **Structured browser logging** (pino-browser or a small `console` wrapper): levels + context + sampling instead of scattered `console.info/error`.
- [ ] B-166 — **Web Vitals collection** (CLS, LCP, FID, INP) via `web-vitals` → ship to the existing metrics endpoint.
- [ ] B-167 — **Feature flags / remote config** for gradual rollouts (theme, new routes) — currently hardcoded. Must never gate function (§184.3). _(researched 2026-08-26: no external SaaS — client SDKs leak user IPs and it's a third-party dependency; no self-hosted Unleash/Flipt/Flagsmith yet — one more Fly service for a 2-person pre-alpha. Decision: build the hand-rolled server-served flag map fed from `packages/config` env (the existing `FEDERATION_ENABLED`/`PUBLIC_READ` pattern, exposed via RPC or `GetServerInfo`), clients read at boot — BUT design the surface for later OSS compatibility: keep flag evaluation as a pure function over a generic flag payload (name/enabled/variants), isolated behind one small client interface, so an Unleash/Flipt backend can replace the server source without touching call sites. Revisit a self-hosted server (license-check first — several changed licenses recently) only if remote toggling without deploys becomes a real need; A/B-on-engagement is banned by Amendment B regardless.)_
- [x] B-168 — **Bundle analyzer in CI** (`vite-bundle-analyzer` or rollup-plugin-visualizer): fail if any vendor chunk exceeds a threshold. _(landed 2026-08-25: `rollup-plugin-visualizer` gated behind `ANALYZE=1`, report writes to a separate `dist-analyze/` output so it never touches the deployed `dist`; `apps/web/scripts/check-bundle-budget.mjs` enforces `apps/web/bundle-budget.json`'s per-chunk raw/gzip thresholds (measured 2026-08-25 max chunk + ~30% headroom) and is wired into `.github/workflows/web.yml`'s deploy job before the Cloudflare Pages step.)_
- [x] B-169 — **Session store hardening** (keep `useSyncExternalStore` — it's the right pattern): (a) cross-tab sync via `window.addEventListener('storage')` in `packages/client/src/session.ts`; (b) emit a "token refreshed" custom event from `sessionManager.withSession` success for UI; (c) add `expiresAt` (decoded JWT `exp`) to `AppSession` for proactive-refresh UI.
- [ ] B-170 — **Conditional off-the-shelf swaps** — only when the trigger fires: forms grow → `react-hook-form` + `zod`; date formatting grows → `date-fns`; shortcut combos grow → `hotcards-js`/`hotkeys-js`; feed >50 items → `@tanstack/react-virtual`; LQIP needed → lazy-load image lib. Explicitly keep: `RichBody.tsx` + `@patches/markup` (domain-owned parser), native `loading="lazy"` (fine for now).

## Backlog / discovered

<!-- Owner product-direction capture 2026-08-24 -->

- [ ] B-115 — Diagnose and repair the existing web profile/Page/wall flow end to end: trace the current `ProfileRoute` → `PatchesPage`/revision → `EditWallDialog` domain and reproduce view/edit/save/refresh/navigation failures in a real browser. Evolve that single established Page concept; do not invent a competing "wall" model unless the code/spec already proves a distinct domain, in which case document the boundary before changing it. _(partial 2026-08-25: confirmed one Page concept throughout — "wall" is only the profile tab's UI label for the index sub-page, `ProfileRoute`'s `pageQuery`/`EditWallDialog` and `PageRoute` both read/write the same `PatchesPage` document via `PageService`; no competing model, no ADR needed. Three defects found with `file:line`, each pinned by a regression test that fails on the pre-fix code and fixed: (1) `apps/web/src/components/EditWallDialog.tsx:48` — `blocks`'s `useState` initializer ran before `ProfileRoute`'s `pageQuery` (only enabled once the Wall tab is selected, which is the same moment `EditWallDialog` first mounts) had resolved, so opening "Edit Wall" showed an empty editor even when the actor had existing wall content; fixed with an open-transition/currentDocument-arrival re-sync effect. (2) `apps/web/src/components/EditWallDialog.tsx:93` (old) — `mutationFn` always wrote a single sub-page hardcoded to `slug: 'home', title: 'Home'` and omitted `theme` entirely, so saving the wall silently deleted every other `PatchesPage` sub-page/theme a multi-page document had (reachable today only via the TUI's raw-document editor, since web has no multi-sub-page UI) and renamed the first sub-page even when its slug/title weren't "home"/"Home"; fixed by carrying the original sub-page's `slug`/`title`, the other sub-pages, and `theme` through unparsed (never round-tripped through the lenient render-time parser, which would turn an unrecognized block type into a placeholder the server's strict write-time validator then rejects). (3) `apps/web/src/routes/ProfileRoute.tsx:44` (old, `queryKey: ['page', profileHandle]`) — cache-keyed/fetched by the URL's literal handle casing while `EditWallDialog`'s post-save `invalidateQueries` targets `['page', actor.handle]` (server's `getActorByHandle` lookup is case-insensitive but returns the canonical stored casing); a handle typed/pasted in different case than stored never matched, so a successful save left the visible wall stale until a hard reload; fixed by keying/fetching off `actor.handle` once the actor resolves. Regression tests: `apps/web/src/components/EditWallDialog.test.tsx` (2 new cases), `apps/web/src/routes/ProfileRoute.test.tsx` (1 new case) — all verified to fail against the pre-fix code via `git apply -R`/`git apply` round-trip, then pass; `mise run check web` green. Not verified in a real browser (no browser access this session) — orchestrator should check: (a) the actual open→load→edit→save→refresh round trip visually, (b) the "View full page →" link and PageRoute sub-page nav after a save, (c) multi-sub-page documents created via the TUI editor still render/save correctly from the web Edit Wall dialog. Not investigated: `apps/web/src/routes/SettingsProfileRoute.tsx` (bio/profile-field editing) and the server-side `apps/server/src/modules/pages/*` beyond a read-through of `pages.controller.ts`/`pages.service.ts`, which looked correctly layered and turned up nothing wrong. No new distinct defects filed — the three found were fixed in this change.)_
- [ ] B-116 — Identity chrome parity: render the existing validated `Actor.nameplate`/custom username styling consistently in web and TUI feed rows, threads, mentions, profile headers, search, and Page attribution, with plain/high-contrast fallbacks and hostile-string tests. This is cosmetic only: it never changes identity resolution, authorization, feed position, or access to function (§173, §184.3).
- [ ] B-117 — Identity cosmetic packs: extend the existing nameplate system with avatar frames, restrained “pop” treatments, deterministic algorithmic identity art, and reusable cosmetic packs. All effects must degrade to accessible static/plain output, honor reduced-motion/terminal capability, execute no user code, proxy no uploads, and gate no function; server-attested badges remain distinct from user-selected cosmetics.
- [ ] B-118 — `@mention` autocomplete across compose surfaces: query only when the user types an explicit handle prefix, drawing from prefix-matching follows and local actor search with privacy/block rules enforced. Merge and order results by a documented deterministic rule (for example exact prefix, followed-status, then canonical handle), expose why a result appears, bound requests/results, and never use engagement, activity, popularity, trending, or recommendation scores.
- [ ] B-119 — Rich Page/link collections: evolve the existing versioned `PatchesPage` and `Links` block into editable link-tree-like collections with labels, grouping, reordering, and accessible presentation across clients; close B-023's structured Links-editor gap. Preserve inert-data rendering, URL/control-character validation, moderation/report/block behavior, document limits/revisions, and safe federation degradation—no parallel Page/wall model or executable user markup.
- [ ] B-120 — Cross-client authentication UX/security completeness audit: inventory register, privacy acknowledgement, verify/resend, login, refresh/reuse recovery, logout/all-sessions, password reset, SSH, passkey, GitHub/OIDC, credential/device management, and closed-node behavior across TUI/web/mobile. Exercise supported flows against a disposable real node, distinguish intentional platform limitations from defects, file scoped fixes, and never weaken uniform errors, rate limits, token rotation/storage, proof-of-possession, or capability policy.
- [x] B-121 — PWA notification and app-icon badge diagnosis: platform-detected icon badging tracks real unread state, serializes clear/set across account transitions, clears on read/logout/account change, keeps in-app indicators deterministic, and reports unsupported/rejected capability honestly. Browser proof covers a real follow notification through set/read/logout clear; no push/streaming claim is made.
- [ ] B-122 — User-directed chronological feed views and saved queries: let a user explicitly compose, name, persist, edit, and delete client-visible views from existing follows/tags/communities and supported filters, with transparent inclusion criteria and stable keyset chronology. No timeline RPC may gain `sort`/`order`; no ranking, engagement/activity recommendation, trending, votes, karma, scores, or interaction-based feed repositioning (§153, §194).
- [ ] B-124 — **Unify the two E2EE identity transcript families** (blocks B-101's session bootstrap, and therefore the `patches-franking-v1` enablement at P13-016). The same certificate/roster/prekey facts are signed twice under different encodings: the _crypto-native_ family in `packages/crypto/src/identity.ts` (which `initiateX3dh`/`respondX3dh` re-verify) and the _node-canonical_ family in `apps/server/src/modules/e2ee/e2ee.codec.ts` (mirrored client-side in `apps/tui/src/e2ee/node-transcripts.ts`), which is the only one `EnrollDevice`/`ClaimPrekeyBundles`/`GetDeviceRoster` ever store or serve. A client can fully authenticate a peer's published chain and still not build the crypto-native `PreKeyBundle`/`SignedDeviceRoster` X3DH demands, because that needs the peer's _root_ signature over the crypto-native encoding — which no other party can mint. Land the node-canonical transcript as the single family (the hoist into `@patches/domain` that `e2ee.codec.ts` already documents as a follow-up), with `@patches/crypto` owning both encode and decode so `verifyCertifiedDevice`/`verifyRosterSnapshot`/`verifyPreKeyBundle` check the served bytes _and_ re-derive the decoded view themselves — never a caller-supplied decoding. Touches reviewed crypto, its test vectors, the server codec, and enrollment, so it needs an **ADR** and a cross-client vector refresh first. Note two encoders currently share domain separators (`identity.ts` exports `CERTIFICATE_CONTEXT`/`ROSTER_CONTEXT` purely so a server test can assert disjointness) — unification should retire that hazard rather than preserve it.
- [ ] B-123 — Federated identity-customization portability: map portable, bounded identity cosmetics/Page attribution onto documented ActivityPub actor/profile fields or a namespaced extension with capability-safe fallback, while keeping local-only effects local and treating remote badges as untrusted unless server-attested. Add two-node round trips, sanitization/size limits, accessibility/plain rendering, and no remote-code or functional-gating path; reuse the existing federation actor seam and nameplate model.
- [ ] B-126 — Dead wire/DB enum after B-095: `FILTER_SCOPE_MESSAGE_REQUESTS` (`packages/proto/proto/patches/v1/filters.proto` value 7, plus the DB check constraint and `filter-enums.ts`) still exists although the message-request flow it scoped was deleted. Remove the value and **`reserved` its number and name** (spec §153 — never reuse either), drop it from the DB constraint via a migration, and sweep the client filter UIs that offer it as a choice. Found during the B-097 docs sweep.
- [ ] B-125 — **owner decision needed**: `INITIAL_VISION.md` has five sections left stale by ADR 0030 / B-095 / B-096, and the spec is authoritative, so only the owner can reconcile them — §183.1 (mandates the retired plaintext disclosure as a shipping requirement, describing a mode that can no longer exist), §183.2 (its second arm assumes an "accepted a prior message request" store that is deleted), §183.4 (assumes `ReportMessage` snapshots plaintext bodies; replaced by `ReportE2eeMessage`/`AttachReportEvidence`), §194's DM clause (the absolute ban on "encrypted"/"secure"/"private" now needs the narrow E2EE_V1 exception the code implements), and §195.1 (frames E2EE as un-authorized, moot now that it is the only mode). Docs currently cite ADR 0030 as superseding; that is a stopgap, not a spec amendment. Also flagged: ADR 0020's "coexist with LEGACY_SERVER_VISIBLE" plan is superseded in practice and wants an architect pass.
- [ ] B-127 — Flaky web tests under file parallelism: `apps/web`'s suite failed twice in this session on different files (`ThreadRoute.test.tsx`'s `mockCreatePost` `waitFor`, then a second file), each passing in isolation and on re-run. `apps/tui` already hit this class of bug and fixed it with `fileParallelism: false` in its vitest config (the `waitFor` resolves before React commits under CPU contention). Diagnose whether `apps/web` needs the same, or a narrower fix — a suite that fails ~1 run in 3 trains everyone to re-run instead of reading the failure, which is how a real regression gets waved through.
- [ ] B-128 — Misleading permanent-failure copy: `E2eeSetupUnavailableError` says "Encrypted session setup material is unavailable right now. Try again." but, until B-124 unifies the identity transcript families, session bootstrap fails **permanently** — "try again" is false. Revisit with B-124 (deliberately left unchanged by the B-101 agent rather than churning copy the §183.1 grep guard watches).

<!-- Beta issue reporter 2026-08-24 (owner directive: independent PR off main) -->

- [x] B-112 — **beta issue reporter core**: low-friction `report an issue` in TUI (palette `:report` + dedicated key) and web (button + error-state CTA); builds a redacted diagnostics bundle (app/version/node/session-handle, last-N structured events + RPC failures as status codes only, navigation breadcrumbs, capability/vault booleans, TUI last-frame capture, web display-media screenshot opt-in) and submits via the issues-ingest Worker → GitHub issue on alliecatowo/patches labeled beta-reporter; zero-input reports must still carry full automatic context; manual copy-out fallback when the endpoint is unreachable _(landed 2026-08-24 on `feat/beta-issue-reporter`: TUI `:report` palette command → IssueReportScreen (optional description, opt-in @handle default OFF) with api-client RPC-failure seam, nav breadcrumbs, stdout frame capture; web `IssueReporter` chip on RouteErrorBoundary + Settings entry with getDisplayMedia screenshot opt-in ≤1280w PNG; both POST `{description,bundle}` to the issues-ingest Worker (`PATCHES_REPORT_URL`/`VITE_PATCHES_REPORT_URL` overrides), local-file/download fallback prints the issues URL; runbook `docs/operations/issue-reporter.md`; `mise run check` green for domain/tui/web)_
- [x] B-113 — **redaction + bundle schema**: shared `@patches/domain` diagnostics-bundle schema + redaction rules — never DM bodies/plaintext, tokens, or key material (§194); size cap ~256 KiB; golden redaction tests _(landed 2026-08-24: `packages/domain/src/diagnostics.ts` — zod bundle schema v1 + `redactDiagnosticsText` (sanitize-first, then JWT/Bearer/provider-key/assignment/PEM/hex-run scrub); 256 KiB post-redaction cap shedding oldest events → frame → screenshot → notes; 28 golden/hostile-input tests; single build path used by both clients)_
- [ ] B-114 — BACKLOG: mobile shake-to-report on RN (needs react-native sensor deps decision); same bundle schema

- [x] B-105 — PR preview environments (owner ask 2026-08-23): per-PR temporary deploy via a Fly machine + a Neon branch (`fly deploy --config` with PR-suffixed app name, `neonctl branches create <pr-N>` + `DATABASE_URL` swap, migrations run on the branch), posting the preview URL on the PR; torn down on close/merge. Needs secrets strategy for R2/federation (or a degraded-feature flag set), a GitHub workflow wiring `workflow_run`/pull_request events to flyctl+neonctl, and a cost guard (machine size, single region, auto-destroy). Keep `swarm/e2ee-federation-niceties` mainline deploys untouched. **Landed 2026-08-23:** `.github/workflows/preview.yml` + `infra/preview/{fly-preview.toml,make-preview-config.mjs}` + runbook `docs/operations/preview-environments.md` (degraded env set: no federation/media/worker, autostop single machine, destroy-on-close, 7-day Neon expiry fail-safe); needs FLY_API_TOKEN/NEON_API_KEY/NEON_PROJECT_ID secrets before first real run.

<!-- Swarm wave 1, 2026-08-22 -->

- [x] B-077 — `pnpm db:generate` emits phantom drift: unrelated `ALTER TABLE filter_list_subscriptions ... SET DEFAULT` (with a malformed `down`) from un-migrated entity drift in `filter-list-subscription.entity.ts`; stripped out of the P13-008 migration — needs its own reconciliation (entity fix or dedicated migration) _(fixed 2026-08-25: root cause was TypeORM 1.x's postgres driver, not the schema — it strips `::cast` suffixes from an introspected default before comparing, and lowercases everything outside quotes on the entity side, so no `ARRAY[...]::text[]` literal can ever match what it reads back. Entity now computes a bare quoted `'{HOME,...}'` literal instead; added `FilterListSubscriptionScopesDefaultReconciliation1787658107141` to move the already-shipped column to it (same value, verified up→down→up against real Postgres). `pnpm db:generate --name=DriftCheck` now prints "No changes in database schema were found" and writes no file)_
- [x] B-078 — `E2EE_GROUP_MAX_MEMBERS` (`packages/domain/src/e2ee/`) and `DM_GROUP_MAX` (`packages/domain/src/limits.ts`) are two constants for the same spec bound of 8 — unify on one _(fixed 2026-08-25: `DM_GROUP_MAX` now aliases `E2EE_GROUP_MAX_MEMBERS` as its single source of truth in `packages/domain/src/limits.ts`. `DM_GROUP_MAX` is read only by `apps/server/src/modules/messages/` — the legacy DM surface a concurrent agent (B-095) is removing — so the name was kept and every existing call site there compiles unchanged rather than being edited)_
- [x] B-079 — federation: `unfollow`/`unlike` mint a fresh random UUID for the inner activity on `Undo` (`activitypub-federation-gateway.service.ts:134-149,162-173`) instead of naming the original Follow/Like activity id — latent interop hazard (peers can't match the Undo); P18-003 must not replicate the pattern, and Follow/Like deserve the same deterministic-id fix _(fixed 2026-08-25: added `localDeterministicActivityUri(origin, 'follow' | 'like', actorUri, objectUri)` to `activity-ids.ts` — a SHA-256 over the activity kind plus the local actor's immutable handle-derived URI and the remote object's immutable `canonicalUri`, since neither `follows` nor `likes` keeps a row past unfollow/unlike to reconstruct an id from. `followRemoteActor`/`buildLikeUndoLike` mint it; `unfollowRemoteActor`/`unlikeRemotePost` re-derive the same call so `Undo`'s object names exactly the original id. 5 new tests in `activitypub-federation-gateway.service.test.ts`; `docs/architecture/federation.md` updated)_
- [x] P13-010 — TUI E2EE UX: immutable mode labels, verification/safety-number flow, change/compromise interstitials, backup-loss disclosure, device manager, and inaccessible-history states _(landed 2026-08-22: recovered an interrupted agent's half-done slice — fixed its type errors, fake-api capability stubs, and shell wiring; screens render immutable mode labels + ADR 0027 dev-mode warning, safety-number verify (`v`, session-scoped verified set), roster-change/identity-change send pauses, vault-fault banners stating lost history as lost (`patches logout --wipe-e2ee` / device-manager wipe), group-control transcript view, and sends routed only through the vault stage→send→confirm pipeline; §194 source-scan test scoped to the LEGACY disclosure with ADR 0020 §11 carve-outs; `mise run check @patches/tui` green)_
- [x] P18-003 — Outbound reposts: `announceRemotePost`/`unannounceRemotePost` on `FederationGateway` + ActivityPub impl + `reaction.service.ts` wiring. **Deterministic Announce ids reconstructed from the repost row** — never `randomUUID()` (the current follow/like Undo path mints fresh inner-activity ids, B-079; do not replicate). Owns `federation-gateway.ts`, `activitypub-federation-gateway.service.ts`(+test), `apps/server/src/modules/reactions/reaction.service.ts` _(landed 2026-08-22: `${PUBLIC_ORIGIN}/activities/announce/<repost-id>` via new `activity-ids.ts`; Undo embeds byte-identical deterministic Announce; PUBLIC-only, blocked-domain pre-check, transactional unrepost; +10 tests. Inbound Announce is P18-004)_
- [x] B-080 — web Pages gaps: Gallery + Friends blocks unrendered (Friends is cheap — `api.socialGraph.listMutualFollows`, TUI reference `apps/tui/src/pages/render/blocks.tsx:461`), `PageTheme` (accent/background/border) unapplied, guestbook sign/report/remove web-less, pinned-posts section absent, Badges placeholder everywhere _(landed 2026-08-22: all five minus Badges — schema defines no badge fields yet; theme colors whitelist-validated into CSS vars; 120/120 web tests green)_

<!-- Owner directives 2026-08-22 (evening): DM bugs/features + legacy purge + rollout versioning -->

- [ ] B-092 — media.integration tests: 3 pre-existing S3 presigned-PUT → 403s against local MinIO (env-only; suite otherwise green) — diagnose (clock skew? stale creds in .env? signature header drift) so full `mise run verify` is green locally
- [ ] B-093 — **E2EE read receipts + presence + typing indicators** (owner request): E2EE conversations ONLY — design as encrypted control envelopes over the existing exact fanout so the node never sees the metadata in plaintext (ADR needed: control-message framing, TTLs, what "presence" means without websockets on a unary-gRPC stack, abuse/rate limits); then implement. NOTE §183's "no read receipts" bound the LEGACY server-visible mode — that mode is being removed (B-095); this ticket does not resurrect it for anything server-visible
- [ ] B-094 — rollout versioning discipline: every rollout must be reversible — each new user-facing/server behavior ships behind an explicit version/capability gate (capability RPC field, node-policy flag, or protocol version constant) with a documented un-roll path (config flip, data-shape compatibility note) in its ADR/ticket; audit existing rollouts (E2EE_V1 capability, FEDERATION_ENABLED, PASSWORD_AUTH policy are already shaped this way) and codify the rule
- [x] B-095 — **REMOVE all legacy server-visible DM surfaces — proto/database/server** (owner directive 2026-08-22: "this app has 0 users, anything legacy should be removed"; ADR 0030 binding): drop `LEGACY_SERVER_VISIBLE` from `CONVERSATION_SECURITY_MODE` (reserve the number), delete plaintext message paths (`messages.proto` RPCs, DirectMessageService plaintext send/read/delete, message_requests flow), migration to constrain/drop legacy rows, keep ReportMessage only if it serves E2EE evidence (else fold into ReportE2eeMessage), notification MESSAGE type re-pointed at E2EE arrivals (content-free) _(done 2026-08-25: enum member removed with its number `reserved`, plaintext RPCs/message-requests flow deleted, rows migrated. Re-pointing `NOTIFICATION_TYPE_MESSAGE` at E2EE arrivals is split out as B-098 — the enum survives, nothing emits it yet)_
- [x] B-096 — REMOVE legacy DM surfaces — clients: TUI MessagesScreen legacy paths + mandated §183.1 disclosure copy retirement, `cli/dm.ts`, fake-api; web MessagesRoute/MessageThreadRoute; mobile message screens — all onto the E2EE-only API (depends on B-095) _(done 2026-08-25: TUI `MessagesScreen`/`cli/dm.ts`/fake-api and web `MessagesRoute`/`MessageThreadRoute`/`ProfileRoute` migrated onto the E2EE-only API; `NewMessageDialog` deleted. Mobile had no message screens, so that scope was a no-op. The §183.1 grep guard was **kept and strengthened**, not deleted: it now strips the named true-copy exceptions by content (so a drifting mirror of `requiredConversationDisclosure` still trips it) before asserting no "encrypted/secure/private" survives — see `apps/tui/src/screens/MessagesScreen.test.tsx`)_
- [x] B-097 — docs sweep after B-095/096: privacy.md/moderation.md/user-guide.md/api.md/architecture (ADR 0017 superseded-by-0030 marker), README/site claims about server-visible DMs, §183.1 disclosure obligations retired with the mode; federation-of-DMs prohibition (ADR 0020 §13) restated unchanged _(done 2026-08-25: 16 files swept — `api.md`, `data-model.md`, `e2ee.md`, `federation.md`, `social.md`, `moderation.md`, `privacy.md`, `roadmap.md`, `user-guide.md`, `tui-design-vision.md`, `preview-environments.md`, `deployment.md`, the ADR 0017 `Superseded by 0030` marker + index row, and `.claude/rules/tui.md`'s DM rule. ADR bodies left as point-in-time records. Two real defects fell out of the sweep and are filed separately: the live `PRIVACY_NOTICE_SUMMARY` false statement (fixed here in `infra/fly/fly.toml`) and B-124's dead filter-scope enum; spec staleness is B-125)_
- [x] B-098 — BUG: **no notifications for messages** — arriving conversation messages produce no notification anywhere (NOTIFICATION_TYPE_MESSAGE exists but is unwired for E2EE; legacy wiring dies with B-095). Land content-free arrival notifications (sender + conversation id only, never body/metadata beyond membership) integrated with unread counts + moderation rules _(fanout wiring landed under B-110 before this ID was ticked — `E2eeConversationService#notifyRecipients` calls `NotificationsService.notifyMessage` post-commit for every co-member on create/send, sharing the existing block/mute/unread-count pipeline; this change adds the missing mute-suppression + content-free coverage in `e2ee-privacy-scan.integration.test.ts`)_
- [ ] B-099 — UX: **viewing your DMs is cumbersome** (owner report): reduce navigation friction — fewer keys/taps list→thread→back, unread badges surfaced in shell/drawer/radial, conversation list information density per B-088/B-090 findings; no engagement ordering ever (chronological only)
- [ ] B-100 — **message edit + delete for E2EE conversations**: delete = encrypted tombstone control envelope updating peers' transcripts (sender-side, per-device fanout already exact); edit = superseding logical message with prior-id reference rendered as edited history (mirrors PostEdits semantics, adapted to ratchet transport); needs small ADR addendum or design section in B-093's ADR covering transcript/history implications
- [ ] B-101 — **TUI E2EE runtime build-out** (the audit's biggest finding: the client half of the protocol doesn't exist yet): wire `createVaultE2eeSender`'s transport to real `SendEnvelopes`; receive path = mailbox poll → `openDeviceEnvelope` (AD verification incl. new logicalMessageId) → `ratchetDecrypt` → **franking tag verification on decrypt** → render; bootstrap via prekey-claim/session-init; verify sender cert chain + group-control signatures on receive using stored rosters; history-transfer parse+display labeling; vault lifecycle fixes (confirmSend-on-transport-error so a failed send doesn't brick the conversation; wipe evicts the live store + resets fault state; sweep wrapping-key temps). Blocks the `patches-franking-v1` profile enablement recorded at P13-016. _(**partially landed** — see `docs/architecture/e2ee.md` §9 for the current client-runtime map. **Done:** send fanout bound to the real `SendEnvelopes` (`apps/tui/src/app/e2ee-transports.ts`) with staged-commit-before-send and the confirmSend/delete-on-transport-error recovery; receive loop over the real `ListMailboxEnvelopes`/`AcknowledgeEnvelopes` with AD verification incl. `logicalMessageId` and structural franking verification inside `openDeviceEnvelope`; cert-chain (`chain.ts`) and group-control (`group-control.ts`) verification against stored rosters; history-transfer parse + display labeling; wipe through the live store with fault reset; **2026-08-25** wrapping-key temp sweep in `GuardedFileVaultKeyProvider` (a crashed rotation used to leave the key in the clear in a `.tmp`), `nowMs` actually threaded into `E2eeSessionRuntime` (the factory passed it, the option was never declared, so setup always used the wall clock), plus tampered-franking-commitment and no-plaintext/no-key-material-to-any-console regression tests. **Remaining:** session bootstrap against a peer — `claimPrekeyBundles` and peer `loadPeerRoster` still fail closed, so no two devices can actually establish a session. That is blocked on B-124's transcript-family unification, not on more TUI code; do not "fix" it by trusting a node-supplied decoding (ADR 0020 §14.2). `E2EE_APPROVED_FRANKING_PROFILES` stays `Object.freeze([])` until then.)_
- [x] B-102 — post-purge client honesty (ADR 0030 consequence): web `DmNotice` still claims "Not end-to-end encrypted — operators can read these messages" over conversations that will be E2EE-only after B-095 lands — false statement + no mode labels anywhere in web Messages routes; mobile has no DM surface to degrade. Scope decision needed from owner: terminal-only DMs until web ships a crypto runtime (large), or honest "DMs live in your terminal" messaging everywhere. _(done 2026-08-25 alongside B-096, taking the **honest-degradation** branch rather than blocking on a web crypto runtime: the false notice is gone, and web — which has no keys and therefore genuinely cannot start or read a conversation — now says so plainly instead of offering an affordance that cannot work. Profile's Message button toasts "Message @&lt;handle&gt; from the terminal client — this web view has no encryption keys to start a conversation." A web crypto runtime remains open as the larger piece; DMs are terminal-only in v0.)_

<!-- Owner UI-direction tickets (2026-08-22): "tightening up the UI … should always be in the mix" -->

- [x] B-086 — **web media/file upload broken** (owner report 2026-08-22) _(client code was correct end-to-end — verified against mobile/TUI contract and reproduced locally; ROOT CAUSE is production R2 `patches-media` having NO CORS policy, so browser PUTs die cross-origin while dev MinIO masks it. Landed: honest CORS-vs-network error copy, attachment progress/failure states, reply-submit blocked during active upload, shared MediaUploadPreview fixing an object-URL leak, regression tests. REMAINING (infra): ~~set a CORS policy on patches-media~~ **DONE 2026-08-23** — applied via `wrangler r2 bucket cors set` (origins patches-web.pages.dev + localhost:5173, methods GET/PUT, header content-type, expose etag, max-age 3600) and verified live: OPTIONS preflight → 204 with correct Access-Control-\* on the account S3 endpoint; browser uploads from the current hosted site should work immediately. Remaining is only the general stale-web redeploy, tracked by B-063; record the bucket-CORS requirement in docs/architecture/media.md + docs/operations/{web,deployment}.md)_
- [x] B-087 — compose fan-out style option: retained the stack-up default and added a true six-action radial arc plus compact ⇄ cozy density preferences. Touch targets, keyboard focus/escape, reduced motion, mobile collision geometry, persistence, and browser visual proof are covered; cosmetics gate no function (§184.3).
- [ ] B-088 — density & action-ownership audit, TUI + web: sweep every screen for (a) excess padding/space with sparse content, (b) icon/action overload — too many visible actions at once, (c) overlapping affordances or unclear ownership of functionality, (d) poor element placement; produce per-surface findings with concrete fixes, then land them
- [ ] B-089 — **thread view as flagship surface** (owner: "one of the star pieces of the show"): TUI ThreadScreen + web ThreadRoute get the full polish pass aligned with the simple throwback vision — hierarchy, focus, reply targeting, reading rhythm; no engagement ranking ever (§194)
- [x] B-090 — design research notes: `docs/research/tui-design-reference.md` (Ink/terminal UI craft) and `docs/research/web-minimal-design.md` (ultra-minimal web that reads crafted, not vibe-coded) — external sources cited + dated, findings mapped to our actual surfaces; feeds B-087/B-088/B-089 _(landed 2026-08-22; headline items: TUI — dedupe screen-local R handlers vs Ctrl+R, hint-bar caps with state-aware keys, ThreadScreen title-row reclaim, neomutt-style `── replies ──`/`↳` depth glyphs, ribbon budget freeze; WEB — 66ch reading column cap, PostCard action ownership (reply/repost/like inline only; Quote under ⋯; Share removed as duplicate), density via data-density spacing tokens (never font size), radial FAB ≤6 fixed positions with reduced-motion support, de-overlap mobile menus)_
- [ ] B-091 — BACKLOG (low): architecture-hardening pass over auth/e2ee/federation/pages seams — module boundary review, error-taxonomy consistency, observability coverage (owner: "more enterprise architecture at some point")
- [ ] B-081 — TUI E2EE vault passphrase-KDF fallback tier (ADR 0020 §4): the guarded-file tier mirrors the credential store instead; needs a passphrase→KDF option for keyring-less environments, wired through a UI seam
- [x] B-082 — mobile Pages viewer: `apps/mobile` has no way to view a user's Patches Page/wall (read-only blocks render like the TUI/web viewers; inert data only) _(landed 2026-08-22: `apps/mobile/src/pages/{document,href,view}.ts` + tests, `screens/PageScreen.tsx`, `components/PageBlocks.tsx` — Text/Markdown/AsciiArt/Hero/NowPlaying/Spacer/Links(http(s)-rechecked via RN Linking)/Image(GetMediaDownload + placeholder fallback)/Posts(read-only PostRow)/TopEight(local handles)/Guestbook read-only; entry via tappable author handle in PostRow and top-bar own handle; 44 new tests, `mise run check @patches/mobile` green)_
- [x] B-083 — ~~PARKED WIP~~ **resolved**: the NOT_FOUND was `report-evidence`/`moderation`'s uniform no-oracle membership rule — the test reported as `legacyAlice`, who was never a member of the E2EE conversation; fixed by reporting as an authenticated E2EE participant. File green (9/9), committed with P13-013.
- [ ] B-084 — mobile `PostRow` renders body only (`apps/mobile/src/components/PostRow.tsx:57`) — no media attachments; the B-082 Image-block path proves `GetMediaDownload` works on RN, so post attachment images are cheap parity follow-up
- [ ] B-085 — mobile has no ProfileScreen; when one lands it needs a "View page" affordance into the B-082 PageScreen (current entries are PostRow-handle taps and the top-bar own handle)

- [x] B-068 — web profile failure diagnostics and closed-node coverage: `ProfileRoute` now reserves missing-account copy for `NOT_FOUND`, renders `describeError` for sign-in/transport failures, and has success/not-found/sign-in-required/transport route coverage; the shared auth interceptor's closed-node bearer coverage remains green. Server images accept `PATCHES_BUILD_SHA` and report `<version>+<short-sha>` for comparison with the web footer. Source verified; the stale hosted build is still tracked by B-063.
- [x] B-067 — unblock continuous delivery source gates: deterministic protobuf formatting and the platform-sensitive TUI upgrade assertion are fixed. The final local gate passed (`mise run verify`), including proto format/lint/breaking, 2,766 unit tests, 868 database integration tests, and the migration check; `actionlint` also passed. No GitHub `main` CI or publish workflow was run in this checkout, so live delivery remains B-063 rather than evidence for this task.
- [x] B-066 — living documentation sweep: eliminated contradictory status and stale claims across `README.md`, `docs/product/roadmap.md`, `docs/architecture/` (`overview.md`, `tui.md`, `social.md`), `docs/operations/` (`database.md`, `backups.md`, `incidents.md`, `web.md`), `docs/user-guide.md`, `apps/web/README.md`, and `site/` (`site/index.md`, `site/docs/`). Shipped features accurately documented: Followers/Following list screens in TUI (`ActorListScreen`, `F`/`G`, `:followers`/`:following`) and Web (`Followers`/`Following` tabs, count pills, `ActorList`), web profile wall editing (`EditWallDialog`), inline thread reply composer, PostCard navigation, web theme switcher (`/settings/appearance`), PWA manifest and mobile safe-area insets, live Neon database and R2 media storage status, and DM retention policy honesty (0 = indefinite retention).
- [ ] B-065 — three-tier database ergonomics: local Compose plus guarded ephemeral Neon create/status/migrate/test/reset/destroy helpers and their 27-assertion test suite are implemented and documented. Still open: no real dev mirror was created, and Neon's branch-protection API returned plan-limit HTTP 422, so the production branch remains unprotected; complete the provider-side mirror/protection work before treating the local → mirror → production path as operational.
- [ ] B-064 — web becomes the full graphical peer of the TUI: the responsive shell and core profile/reporting surfaces have landed with route/component tests, but the TUI-to-web inventory still has missing surfaces and desktop/mobile breakpoints have not been visually verified against the live node. React Native parity remains explicitly out of scope.
- [ ] B-063 — delivery recovery: the live web footer still reports `0.1.0+29df763`; the source BigInt session-serialization fix (B-041), profile diagnostics/build identity (B-068), security blockers, and gated Fly/Cloudflare workflows are locally verified, but no GitHub `main` CI publish or replacement live deployment has run. Deploy, then verify live version/ping/login/profile behavior and rollback readiness before closing.
- [x] B-062 — React Native active-bug containment only (not feature investment): read `max_post_chars` from node policy instead of hardcoding 5,000, and key/reset compose draft/media state whenever the target changes so edit/reply/quote content cannot be submitted as a root post. Add non-TSX logic tests and defer broader mobile parity.
- [x] B-061 — retention honesty: `DM_RETENTION_DAYS` accepts/publishes nonzero values although no server-visible DM retention sweep exists. Until a tested deletion job lands, reject nonzero configuration and publish `0`; document that `0` means indefinite retention and add config/node-policy regression tests.
- [x] B-060 — federation abuse control: the pre-verification inbox limiter is keyed only by unsigned `activity.actor`, allowing invented-host bucket rotation and unbounded actor-document fetches. Add a trusted transport-peer budget before JSON parsing plus a verified-origin budget after signature verification; preserve useful metrics without high-cardinality attacker labels; cover rotation/spoofing tests.
- [x] B-059 — **FEDERATION SECURITY BLOCKER:** inbox signature verification covers the peer-supplied `Digest` header text but never verifies it against the exact received `rawBody` (`apps/server/src/modules/federation/services/inbox.service.ts:135`). Require `verifyDigestHeader` before actor fetch/dispatch and add a regression test that signs one body and submits another.
- [x] B-058 — verification and password-reset delivery now uses a versioned AES-256-GCM envelope with an independent rotatable server/worker keyring; the durable job payload contains only the envelope and `authCodeId`, success/dead-letter handling scrubs it, terminal auth jobs cannot replay, and migration `1787420562003-AuthCodeDeliveryEnvelopes` rejects the former top-level plaintext shape. The migration applied cleanly and a second run had no pending work; the final database suite passed 53 files / 868 tests.

### MCP backlog (planned; no product MCP endpoint is implemented)

- [ ] B-069 — MCP-01 product/security decision: turn `docs/research/model-context-protocol.md`'s verified 2026-07-28 transport research and explicit unknowns into an owner-approved scope, resource URI, tool set, threat model, and ADR where architectural choices require one.
- [ ] B-070 — MCP persistence/config foundation: add only the configuration and durable grants/approval/provenance records selected by B-069, with migrations, repositories, expiry/revocation, audit boundaries, and no DM bodies or credentials in persistence or telemetry.
- [ ] B-071 — MCP OAuth resource-server boundary: publish RFC 9728 metadata, use a distinct audience/resource and narrow scopes, validate issuer/signature/expiry/audience before dispatch, return correct challenges, and cover confused-deputy/token-forwarding failures. Do not add CIMD, DCR, or DPoP unless B-069 explicitly selects it.
- [ ] B-072 — MCP provenance and approval UI: show the requesting client, principal, scopes, arguments, and risk before any mutation; record content-minimized approvals and denials; never trust tool annotations as authorization or assume a host displayed confirmation.
- [ ] B-073 — MCP transport/application adapter: expose modern POST-only `/mcp` through Nest's transport layer using the released SDK v2 Node adapter, strict protocol/Origin/body/time budgets, authorization-filtered deterministic schemas, application-service DTOs, and no TypeORM entities.
- [ ] B-074 — MCP subscriptions: begin without dynamic change delivery; if a shipped use case requires `subscriptions/listen`, add bounded authorization-aware streams, shutdown handling, and a deliberate cross-instance event bus before enabling it on multiple replicas.
- [ ] B-075 — MCP tool catalog: implement the B-069-approved minimal tools with explicit input/output schemas, conservative annotations, shared application-service authorization, idempotency where applicable, output minimization, and per-principal/per-IP budgets.
- [ ] B-076 — MCP docs and adversarial verification: document only exercised setup and calls; add protocol-version, Origin, OAuth audience/scope, approval bypass, SSRF, replay, cancellation, subscription isolation, sensitive-output, and audit-provenance tests plus client interoperability evidence before rollout.
- [x] B-049 — `E2eeService.AttachReportEvidence` (`apps/server/src/modules/e2ee/report-evidence.ts`, `attachReportEvidence`) loads a `Report` by id/reporter but never checks `report.subjectType === 'E2EE_MESSAGE'`. Now that `ModerationService.ReportE2eeMessage` (P13-019) exists, evidence can be silently attached to a `POST`/`ACTOR`/`MESSAGE`/`GUESTBOOK_ENTRY` report too. Done = `attachReportEvidence` rejects a subject-type mismatch (reuse `AppError.validation` or a new closed-vocabulary code, reporter's own report either way — no oracle beyond what `REPORT_NOT_FOUND` already reveals), with a regression test in `report-evidence.test.ts`.

<!-- Audit sweep 2026-08-19 (P14-020: Amendment C §208 prohibition sweep + §209 checklist + whole-repo hard-rules sweep) -->

- [x] A-058 — layering (§128–129: a controller is a transport adapter; mapping between wire and persistence vocabularies belongs in a mapper): `apps/server/src/modules/communities/community.controller.ts:35` is the only controller in the tree that imports `@patches/database` (`import type { CommunityRole as DbCommunityRole } from '@patches/database'`), and `:267` defines `roleFromProto(role: CommunityRole): DbCommunityRole` inline even though `apps/server/src/modules/communities/community.mapper.ts` exists next to it. Done = `roleFromProto` moves to `community.mapper.ts` (or the service's own DTO vocabulary), the `@patches/database` import disappears from every `*.controller.ts`, and a grep for `@patches/database` under `apps/server/src/modules/*/*.controller.ts` returns nothing.
- [x] A-057 — research coverage (CLAUDE.md working agreement rule 3: "Before touching a technology, read its note in `docs/research/`. If the note is missing or wrong, fix the note"): two correctness-critical runtime dependencies have no note. (a) `tldts` (`apps/server/package.json`) backs PSL-based DOMAIN-term matching for filters and filter lists (§198.2 link-domain terms, §199.4 domain subscripts) — a wrong public-suffix reading silently makes a safety filter not match, and nothing in `docs/research/` mentions it. (b) `tar-stream` (`apps/worker/package.json`) builds the account-export `.tar.gz` (`apps/worker/src/jobs/handlers/export-account.handler.ts:32,100`), which is the entire §197.3/§204.2 portability guarantee. Done = `docs/research/tldts.md` and `docs/research/tar-stream.md` (or one `docs/research/export-archive.md`) verified against official docs, with the version pinned and the behaviours we rely on (PSL update cadence / ICANN vs private suffix handling; tar entry naming, size limits, stream backpressure) cited.
- [x] A-056 — TUI surface (§205's enumerated surface list, restated as §209's "all surfaces reachable via `,`, `:`, CLI verbs"): §205 says the `,` preferences screen "gains sections: Privacy, Filters, Lists, Labelers", but `apps/tui/src/screens/PreferencesScreen.tsx:54-55` only added a `privacy` row (`type Row = 'theme' | 'glyphs' | 'plain' | 'quiet' | 'images' | 'privacy'`) — there is no Filters/Lists/Labelers row, so `,` reaches one of the four sections §205 names. Separately §205 lists `:filter` (singular) among the new palette commands and `apps/tui/src/app/keymap.ts` registers only `:filters`. Done = three more preference rows that open `:filters`/`:lists`/`:labelers` the same way the `privacy` row opens `:privacy` (`PreferencesScreen.tsx:172`), plus a `:filter` alias; no new global single-key binding (§205's zero-new-global-keys rule is not being relaxed).
- [x] A-055 — docs (§209's last checklist item: "`docs/product/privacy.md` and `docs/product/moderation.md` describe actual, shipped behaviour — no 'planned' marker left on something that is implemented, and vice versa"): three stale claims, all describing work that has since landed. (a) `docs/product/privacy.md:198` says the export is "a single self-describing JSON archive (not yet the fuller directory-tree-plus-media-files layout §197.3 describes)" — P14-023 shipped it: `apps/worker/src/jobs/handlers/export-account.handler.ts:32,72,100,119-120` builds a gzipped tar with `account.json`/`posts.json`/`follows.json`/`messages.json`/`media/<id>.<ext>`/`manifest.json`. (b) `docs/product/privacy.md:204-206` says the purge scope is "**not yet** bookmarks, reposts, community memberships, muted tags, or filter/list/labeler subscriptions" — P14-024 shipped all of them: `apps/worker/src/jobs/handlers/purge-account.handler.ts:204,205,208,209,214,215`. (c) `docs/product/moderation.md:224`'s status parenthetical "(§201.4, P14-012; account/post entries a documented follow-up)" contradicts its own body four lines later, and the same stale claim is in code at `apps/server/src/modules/moderation/moderation.service.ts:293-296` ("Today only `patches-admin domain block` … writes rows here; `user`/`report`-driven account/post entries are a follow-up") — P14-027 shipped those writes (`apps/admin/src/commands/user.ts:151,274`, `apps/admin/src/commands/report.ts:155,175`). Done = all three corrected in the same change.
- [x] A-054 — proto/server (§209 checklist item 11: "Every new limit published via `GetNodeInfo`/`GetNodePolicy` and enforced in three layers (§204)"): none of §204's new size limits are published. `packages/proto/proto/patches/v1/node.proto`'s `NodeLimits` still carries only the eight pre-Amendment-C fields (`post_body_max_chars` … `search_query_max_chars`), and `NodePolicy` publishes only `account_deletion_grace_period_days` (field 13) and `appeal_window_days` (field 14). `filters per actor: 50`, `terms per filter: 20`, `filter lists published per actor: 10`, `entries per filter list: 2,000`, `filter list subscriptions: 100`, `per-entry exceptions per list: 200`, `labeler subscriptions per actor: 50`, `appeal statement: 2,000 chars` and `export archives kept: 1 / expires 7 days` all exist only as `packages/domain/src/limits.ts:158-204` constants that a client cannot read, so no client can render "you have 50 of 50 filters" honestly. Done = new `NodeLimits` fields (next free numbers, never reusing one) populated from `@patches/domain`, and the TUI/web filter/list/labeler screens using them instead of hardcoding or guessing.
- [x] A-053 — clients (§197.1: "When a node publishes a **material** change it increments the version, and **every client MUST show the new summary at next session start and record a fresh acknowledgement**"): no client does. `grep -rn "PRIVACY_NOTICE_NOT_ACKNOWLEDGED" apps/tui/src apps/web/src` returns zero hits, and the only place the notice is re-checked is `apps/tui/src/screens/PrivacyScreen.tsx:283-286`, which the viewer has to navigate to on purpose. Consequence: on a node with `REQUIRE_PRIVACY_ACK=true` (`apps/server/src/config/env.schema.ts:249`, enforced by `apps/server/src/common/guards/require-privacy-ack.guard.ts:57-59`) every RPC fails `FAILED_PRECONDITION`/`PRIVACY_NOTICE_NOT_ACKNOWLEDGED` and neither client routes the user anywhere they can fix it — the client is simply bricked. Done = a session-start check (client compares `GetPrivacyPrefs.privacy_notice_version` against `GetNodePolicy.privacy_notice_version` on connect) that shows the summary and offers `AcknowledgePrivacyNotice` before proceeding, in both `apps/tui` and `apps/web`, plus explicit handling of the `PRIVACY_NOTICE_NOT_ACKNOWLEDGED` error code wherever an RPC can return it.
- [x] A-052 — server config (§197.1: "A node MUST publish a **privacy notice** consisting of a short structured **summary** … served by the node, not compiled into the client, so a self-hoster publishes their own (§163)"; §197.6 for the rest): the node has no way to publish one. `apps/server/src/modules/system/node.service.ts:108,111,113,134` hardcode `privacyNoticeSummary: ''`, `termsUrl: ''`, `appealInstructions: ''` and `operatorIdentity: ''` with no env var or admin surface behind them (only `PRIVACY_NOTICE_VERSION`, `NODE_POLICY_URL`, `NODE_MODERATORS`, `DATA_LOCATION` exist in `apps/server/src/config/env.schema.ts`). Clients then render the honest-but-useless "This node publishes no privacy notice." (`apps/tui/src/screens/PrivacyScreen.tsx:298-299`, `apps/tui/src/cli/register.ts:157-158`, `apps/web/src/routes/RegisterRoute.tsx:138`), which means §209's first checklist item ("Privacy notice shown and acknowledged at registration") ships a mechanism with nothing to show — including on patches-social.fly.dev. `appeal_instructions` being empty additionally leaves §197.6's "how appeals are filed" unpublished. Done = operator-supplied config for these four fields (env pointing at a file, or an `admin node policy set` command), text-only per §197.6's "MUST NOT be able to publish … markup, scripts, or remote media", plus real values set on the live node.
- [x] A-051 — server (§198.3: `notifications` and `message_requests` are v1 filter scopes; §209 checklist item 7 "Filters: … server-evaluated at the §198.4 chokepoint"): both scopes are accepted, validated, persisted and exported but **never enforced**. `apps/server/src/modules/filters/filter-enums.ts:48,50` maps `FILTER_SCOPE_NOTIFICATIONS`/`FILTER_SCOPE_MESSAGE_REQUESTS`, and the DB CHECK in `packages/database/src/migrations/1787135113517-Phase14PrivacyAndFilters.ts` allows them, but the only callers of `loadEffectiveFilterRules` are `apps/server/src/modules/feeds/feed.service.ts:73,371` (HOME/LOCAL/TAG_FEED/COMMUNITY_FEED) and `apps/server/src/modules/posts/post.service.ts:753` (SEARCH); `grep -rn "loadEffectiveFilterRules\|filter" apps/server/src/modules/notifications/ apps/server/src/modules/messages/` returns nothing. A viewer can create a filter scoped to notifications or message requests, see it listed, and it silently does nothing — worse than not offering the scope. Done = `NotificationsService.listNotifications` and the message-request read path run the same chokepoint (with `hide` omitting server-side and keyset pagination staying correct), plus integration tests per scope; or, if either is deliberately deferred, the scope is rejected at validation with a clear error and §198.3's deviation is recorded in an ADR.
- [x] A-050 — server/admin (§201.3: "Resolution is itself delivered as a moderation notice (§201.2) and itself audited (§66)"): an appellant is never told their appeal was resolved. `apps/admin/src/commands/appeal.ts:160-162` correctly writes an `admin_audit_log` row with `action: 'appeal.resolve'`, but `apps/server/src/modules/moderation/notice-projection.ts:50,56` projects notices only from `aal.action IN ('user.suspend', 'user.delete')` and `subject_type = 'REPORT' AND aal.action = 'report.resolve'` — so `appeal.resolve` produces no notice on any surface. The outcome is reachable only if the appellant happens to re-poll `GetAppeal`/`ListMyAppeals` (`apps/server/src/modules/appeals/appeal.service.ts:91,99`). Done = `appeal.resolve` added to the notice projection (carrying outcome + `appeals.resolution_reason`, never `reports.moderator_note` per §55/§208), an integration test in `apps/server/test/appeals.integration.test.ts` asserting the appellant sees a notice after `patches-admin appeal resolve`, and `docs/product/moderation.md`'s Appeals section updated.
- [x] A-049 — server (§201.2: "Every node enforcement action that affects a specific actor … MUST generate a **moderation notice**: a `MODERATION`-type notification (§56, unchanged type, extended payload)"; §209 checklist item 10 "Moderation notices delivered on every node enforcement action"): nothing is ever _delivered_. No code path in the repo writes a notification row of type `MODERATION` — `grep -rn "type: 'MODERATION'" apps/server/src apps/admin/src apps/worker/src` returns nothing, and `packages/database/src/entities/enums.ts:51-52` states the type is "reserved — no RPC creates one yet". Notices exist only as the pull-only projection `ModerationService.ListMyModerationNotices` (`apps/server/src/modules/moderation/moderation.service.ts:341`), so a suspended actor learns why only if they go looking, and `NotificationsScreen.tsx:115`'s "moderation notice" label can never render. `docs/product/moderation.md:183` claims "Every enforcement action against you generates an in-product moderation notice", which is true only in the pull sense. Done = `patches-admin user suspend|delete` and `report resolve --action remove-post|suspend` (and, with A-050, `appeal resolve`) insert a `MODERATION` notification alongside the `admin_audit_log` row, the notification carries only the audit-log id (§201.2's "read projection … not a second source of truth", and never `reports.moderator_note`), and an integration test asserts it appears in `ListNotifications`.

<!-- Audit sweep 2026-08-18 (round 2: deployed production posture + new modules vs INITIAL_VISION.md §101-104/§128-129/§153/§158-160) -->

- [x] A-038 — deploy/worker (§159 "worker configured"): `infra/fly/fly.toml:27` sets `LOG_LEVEL = "log"` in the app-wide `[env]` block, but `apps/worker`'s schema spreads `baseEnvSchema.shape` (`apps/worker/src/config/env.schema.ts:34` -> `packages/config/src/schemas/base.ts:6`, `z.enum(['debug','info','warn','error'])`), so the worker's `validateEnv` throws `ConfigError: LOG_LEVEL: Invalid option` and `main.ts` exits 1. Verified by running the built schema: `node -e "(await import('./apps/worker/dist/config/env.schema.js')).validateEnv({NODE_ENV:'production',LOG_LEVEL:'log',...})"` -> THROWS. Fly `[env]` is app-wide (no per-process-group env), so the live `worker` Machine crash-loops: no EMAIL_SEND, media derivative, token-cleanup, stale-lease-sweep or FEDERATION_DELIVER job has ever run in production. Done = one LOG_LEVEL vocabulary shared by server and worker (or the worker accepting Nest's `log`/`verbose` too), plus a test that parses `infra/fly/fly.toml`'s `[env]` against both apps' schemas so the two can't drift again, and `flyctl status`/`logs` confirming the worker Machine stays up.
- [x] A-039 — security (§102 rate limiting): behind Fly's TLS-terminating proxy every external client shares one peer address, so all peer-keyed budgets collapse into a single bucket. `apps/server/src/common/interceptors/request-context.interceptor.ts:96-104` derives `RequestContext.peer` solely from `ServerUnaryCall.getPeer()`, and nothing anywhere reads `fly-client-ip`/`x-forwarded-for` (`grep -rn "fly-client-ip\|x-forwarded-for" apps packages` -> no hits; `docs/research/fly-io.md` has no client-IP section). Consequences in prod: the `consumePeer`/`consumeDistributedPeer` ceilings on register/login/password_reset/ssh_challenge/ssh_complete/github_* (`apps/server/src/modules/auth/auth.service.ts:160,377,503,589,672`) no longer bound anything per-attacker, and the peer-only `ReportRateLimitService` (10/hour, `apps/server/src/modules/moderation/report-rate-limit.service.ts:24`, called from `moderation.controller.ts:141,157`) becomes an instance-wide budget one client can exhaust for everyone. Done = resolve the caller IP from proxy-supplied metadata only when an explicit trust-the-proxy config flag is set (falling back to `getPeer()` otherwise, never trusting caller-supplied metadata on a direct connection), a `docs/research/fly-io.md` section verifying what Fly actually forwards to an h2c gRPC backend, and unit tests covering the spoofing case.
- [x] A-040 — server auth (§102, §45): the invite-only bootstrap check is not race-safe. `apps/server/src/modules/auth/auth.service.ts:209-226` reads `manager.getRepository(User).count()` inside `dataSource.transaction(...)` opened with no isolation level (READ COMMITTED), and the comment claims a second concurrent bootstrap "blocks on the same row locks" — a `COUNT` takes no such locks, so two simultaneous registrations with different handles both observe 0 users and both bypass the invite requirement on a fresh invite-only node. Done = serialize the bootstrap decision (advisory lock, or an `INSERT ... ON CONFLICT DO NOTHING` bootstrap marker row whose success is what authorizes the invite bypass), correct the misleading comment, and add a concurrency test that fires two bootstrap registrations at once and asserts exactly one succeeds without an invite.
- [x] A-041 — _(resolved 2026-08-18: production DATABASE_URL now points at the Neon project `patches` (shy-recipe-96135980, aws-us-east-2, TLS); data migrated from the Fly Postgres cluster, which is kept stopped as a fallback)_ deploy (§159 "Managed Postgres configured"): the live node runs a legacy self-managed `flyctl postgres create` cluster (`patches-social-db`), not Fly Managed Postgres — documented honestly in `docs/operations/deployment.md:275-281` as "a planned follow-up", but nothing on the board tracks it, and `docs/decisions/0003-typeorm-postgres.md` specifies Managed Postgres as the target. This is also what makes A-042 load-bearing: unmanaged Fly Postgres does not provide the automatic managed backups `docs/operations/backups.md` assumes. Done = either migrate to `fly mpg` (or Neon) and update `deployment.md`/ADR 0003, or write an ADR accepting the unmanaged cluster for v0.0 with its operational consequences (backups, failover, upgrades) spelled out.
- [x] A-042 — _(resolved 2026-08-18: `docs/operations/backups.md` rewritten with real mechanisms — Neon PITR/branching primary, Fly volume snapshots for the fallback cluster, `infra/scripts/db-dump.sh`/`db-restore.sh` (`mise run db:dump`/`db:restore`) secondary; a full restore drill was run — `mise run db:dump` against Neon produced a 12K dump with `users=2 posts=2`, restored into a scratch local `patches_restore_test` database with matching counts, then dropped; two throwaway Neon branches were created/connected/deleted to verify the branch-restore path; concrete RPO/RTO recorded)_ operations (§159 "backup strategy exists" + "restoration procedure is documented", §90): `docs/operations/backups.md:3-8` still reads "**Status: PLANNED.** No production database exists yet as of 2026-08-17 (Phase 0)" with backup frequency / retention / PITR all "_TBD_", while a production database has been live since 2026-08-18 and holds real accounts. §90 requires backups enabled and verified _before_ Phase 7 deploy. Done = record the actual backup mechanism and retention for the cluster A-041 settles on, set concrete RPO/RTO, and rehearse a restore into a scratch database with the real commands and their output pasted into the doc.
- [x] A-043 — deploy (§159 "production health checks work"): the only platform check is `[[services.tcp_checks]]` on the gRPC port (`infra/fly/fly.toml:78`), which passes as long as the socket accepts a connection — a Machine that boots with an unreachable database, or one that has flipped the gRPC health service to `NOT_SERVING`, still passes the check and takes traffic, and a broken release is never rolled back. The blocker cited in `infra/fly/fly.toml:71-77` and `docs/operations/deployment.md:253-265` ("apps/server does not currently expose" a hybrid app) is stale: `apps/server/src/main.ts:64` now builds a full Nest HTTP app. Done = bind a small HTTP health route unconditionally (today `app.listen(HTTP_PORT)` at `main.ts:85` only runs when `FEDERATION_ENABLED`) that reports database connectivity plus the gRPC health status, point a `[[services.http_checks]]` (or a second internal service) at it, and refresh both stale comments.
- [x] A-044 — deploy/shutdown (§124 graceful shutdown): the drain is a no-op in production. `apps/server/src/main.ts:74` calls `enableShutdownHooks()` _before_ the `SIGTERM`/`SIGINT` handlers at `main.ts:96-101`, so Nest begins closing the gRPC server in the same tick that health flips to `NOT_SERVING`, with no interval in which in-flight RPCs can finish — and nothing observes that health status anyway (see A-043). Separately, `infra/fly/fly.toml` sets no `kill_timeout`, so Fly's 5s default `SIGKILL`s the worker while `apps/worker/src/main.ts:41` is still waiting out its 10s `SHUTDOWN_TIMEOUT_MS` drain, killing in-flight jobs mid-handler on every deploy. Done = an explicit drain delay between the health flip and Nest's shutdown hooks, and a `kill_timeout` in `fly.toml` comfortably above the worker's drain budget, with the numbers documented in `docs/operations/deployment.md`'s "Graceful shutdown" section.
- [x] A-045 — TUI security (§76 "never interpolate untrusted input into process execution", §101): `apps/tui/src/pages/open-link.ts:15-21` hands a server-supplied `href` straight to `xdg-open`/`open`/`cmd /c start` and explicitly declines to validate it ("the `href` was already scheme-checked to `http(s)` at write time"), but the string the TUI opens comes from whatever node the user is connected to — a hostile or compromised node, a row written before `linkHrefSchema` existed, or a future federated page can return `file:///...`, a custom scheme with a registered handler, or a value starting with `-` that the opener parses as a flag. Done = re-validate `http(s)` scheme and reject leading `-` in `openLinkExternally` (and the cached-path branch of `openMediaExternally`) before spawning, with tests for each rejected shape.
- [x] A-046 — packaging (§159 "npm package install works"): a clean-environment `npm install -g @patches/tui` cannot work — `apps/tui/README.md:14-28` records that it depends on `@patches/proto` and `@patches/terminal-media`, both still `private`/unpublished, and that the verified install only worked via `pnpm-workspace.yaml` `overrides` pointing at local tarballs. P7-004 is ticked with this noted only in its parenthetical, so the checklist item is unmet and untracked. Done = publish both dependencies publicly or bundle them into the `@patches/tui` tarball, then verify `npm install -g @patches/tui && patches --version` in a container with no repo checkout and record the transcript. _(Bundled, closing the gap — see P9-003. `npm login` isn't available in this environment, so the verification transcript uses `pnpm pack` + `pnpm add -g <tarball>` from a shell with no repo `node_modules` on `PATH` instead of a literal `npm install -g`; both resolve the same npm-registry semver ranges from the packed `package.json`, recorded in `docs/operations/deployment.md`'s "npm packaging" section. Actual `npm publish` remains a planned owner step.)_
- [x] A-047 — CI/deploy (§159 "smoke tests pass after deploy"): the deploy + smoke workflow no-ops — `docs/operations/deployment.md:412-427` records `FLY_GRPC_HOST`/`FLY_API_TOKEN` as unset and the smoke job as "**Status: planned** — never run against a real deployment" — even though `patches-social.fly.dev` is live and was deployed by hand, so nothing re-verifies the node after a merge and the only smoke evidence is one manual `patches ping`. Done = set the repo variable/secret, run the workflow against the live app once, and paste the successful smoke output into `deployment.md`.
- [x] A-048 — docs/infra accuracy (working agreement rule 7): `infra/fly/fly.toml:5-16`'s header still says the config was "never deployed to a real Fly org from this environment", tells the reader to "treat every field as should work per current docs", and instructs whoever deploys next to replace the `app = "patches-social"` "placeholder" with a real name and delete the comment — following that instruction on the live app would point a deploy at the wrong app. Done = rewrite the header to state what was actually deployed on 2026-08-18 (app name, region, cluster) and which fields remain unverified.

<!-- Audit sweep 2026-08-18 (full codebase vs INITIAL_VISION.md §153/§128–129/§101–104/§157–160) -->

- [x] A-027 — TUI (§158 "edit profile"): `e` on the viewer's own `ProfileScreen` opens `screens/EditProfileScreen.tsx` (display name/bio/location/website, `Tab`/arrows between fields, `Ctrl+S` saves via `UpdateProfile`'s `update_mask`, `Esc` discards) plus the headless equivalent `patches profile edit [--display-name] [--bio] [--location] [--website]` (`apps/tui/src/cli/profile.ts`). Covered by `apps/tui/test/profile-edit.test.tsx` (harness) and `apps/tui/src/cli/profile.test.ts` (unit). Deviation: nameplate editing was left out of this pass — no in-app or CLI surface sets `Nameplate` fields yet; filed as a follow-up.
- [x] A-028 — TUI (§158 "verify email"): `patches verify <code>` (unauthenticated `VerifyEmail`) and `patches verify --resend` (authenticated `ResendVerification`) in `apps/tui/src/cli/verify.ts`, dispatched from `apps/tui/src/cli.tsx`; `screens/AccountsScreen.tsx` also shows an "email unverified — r resend" banner with an in-app `r` handler when `session.emailVerified` is false. Covered by `apps/tui/src/cli/verify.test.ts` and `apps/tui/test/profile-edit.test.tsx`'s "Email verification banner" tests.
- [x] A-029 — docs (§154 "don't let this drift into fiction", the doc's own preamble): `docs/product/roadmap.md` is stale — line 7 states "Phase 0 … is complete; Phase 1 … is in progress. No later phase has started", and Phases 1–8 all carry `**Status: planned**` (lines 46, 74, 83, 93, 102, 125, 135, 151, 194) while `tasks.md` ticks P1-001..P8-008; the restated §158 v0 acceptance checklist (lines ~316–351) is entirely unchecked despite most items having shipped. Done = every phase Status line resynced to reality and every §158 box ticked or annotated with what is actually missing.
- [x] A-030 — docs: `docs/architecture/auth.md:8` says "**Status: planned.** Nothing in this document is implemented yet" — password login, SSH challenge/response, GitHub device flow, credential management, refresh rotation/reuse detection and password reset are all implemented in `apps/server/src/modules/auth/`. Done = an accurate per-flow status block (implemented / partial / planned) at the top of the doc.
- [x] A-031 — docs: `docs/architecture/api.md:10–32` misstates implementation status — it says `MediaService`, `ReactionService`, `ModerationService`, `NotificationService` and `ListBookmarks` are "**planned**, not yet present in `packages/proto`", and that `BlockActor`/`UnblockActor`/`MuteActor`/`UnmuteActor` "remain planned (Phase 6) … nothing writes to them yet", but all exist in `packages/proto/proto/patches/v1/{media,reactions,moderation,notifications}.proto` with server handlers (`apps/server/src/modules/{media,reactions,moderation,notifications}/*.controller.ts`); the §1 schema-layout listing omits those four `.proto` files. Done = status paragraph + §1 listing + inline per-RPC markers resynced.
- [x] A-032 — layering (§128–129: a controller is a transport adapter, logic lives in an application service): `apps/server/src/modules/federation/http/actor.controller.ts:47–68` injects the `DataSource` and builds TypeORM query-builder / `findOne` queries against `Page` and `PageRevision` inline in the `GET /users/:handle/page` handler. Done = the lookup moves into `PagesService` (or `ActorDocumentService`), `@InjectDataSource`/`typeorm`/`@patches/database` imports are dropped from the controller, and the controller only negotiates content type and serializes.
- [x] A-033 — operations (§159 "error monitoring works or documented alternative exists"): no decision is recorded anywhere — `docs/operations/incidents.md:25` defers to "Sentry or a documented alternative — see `docs/operations/deployment.md`" and `deployment.md` has no error-monitoring section, so the cross-reference dangles. Done = pick one (Sentry vs structured-log-only + Fly alerting), document setup, DSN-as-secret handling and alert routing in `docs/operations/deployment.md`, and record the choice in an ADR if it's the log-only alternative.
- [x] A-034 — docs (§159 "user documentation exists"): there is no end-user documentation — `README.md`'s only usage sections are `## Development` and `## Repository layout` (both developer-facing), and `docs/` has no user guide (`docs/product/` is principles/roadmap/moderation/privacy). Done = a user-facing doc covering `@patches/tui` install, first run against a node, the `register`/`login`/`visit`/`keys` commands, and the TUI keybinding reference, linked from `README.md`.
- [x] A-035 — federation (§160 "Update semantics decided", pre-enable gate): `apps/server/src/modules/federation/services/inbox.service.ts:173–183` handles `Follow`/`Undo`/`Accept`/`Create`/`Delete`/`Like` and silently drops `Update`; `docs/architecture/federation.md:230` records the box unchecked but nothing on the board tracked it. Done = an ADR deciding whether `Update(Note)`/`Update(Actor)` are applied, ignored, or rejected, plus the corresponding inbox branch and an integration test.
- [x] A-036 — federation (§160 "federation telemetry exists", pre-enable gate): no federation counters/metrics exist — `grep -rn "metric\|telemetry" apps/server/src/modules/federation apps/worker/src/jobs` finds only a comment in `peer-rate-limiter.service.ts:16`; `docs/architecture/federation.md:242` records the box unchecked and untracked. Done = structured counters for inbox accept/reject/dedupe/signature-failure and outbound delivery attempt/success/retry/dead-letter, surfaced through `packages/observability`, documented in `docs/operations/federation.md`.
- [x] A-037 — TUI: no in-app or CLI surface edits `Actor.nameplate` (name color/glyph/avatar frame/profile border/status line) — split out of A-027, which only covers display name/bio/location/website. Done = `patches profile edit --name-color/--glyph/...` and/or an in-app equivalent, validated against `apps/server/src/modules/actors/validation.ts`'s nameplate schema. _(`EditProfileScreen`'s "Nameplate" section — name colour/glyph/status line/avatar frame/profile border with a live `Nameplate` preview, sent under a single `"nameplate"` `UpdateProfile` mask path — plus matching `patches profile edit --name-color/--glyph/--status-line/--avatar-frame/--profile-border` CLI flags, merging unspecified fields from the current session actor. Tests: `apps/tui/test/nameplate-edit.test.tsx`, `apps/tui/src/cli/profile.test.ts`.)_

- [x] A-001 — CI: add `actions/checkout@v4` as the first step of every job (local `./.github/actions/setup` cannot resolve before checkout)
- [x] A-002 — database: stop disabling TLS verification — CA/sslmode-driven option defaulting to verify; update tests
- [x] A-003 — testkit: refuse any TEST_DATABASE_URL whose database name does not end in `_test` before dropDatabase() (§119)
- [x] A-004 — server: validate/cap inbound `x-request-id` (≤64 chars, [A-Za-z0-9._-]) before logging/echoing (§103)
- [x] A-005 — CI/tests: add `--project testkit` to `test:integration` and set TEST_DATABASE_URL so isolation helpers actually run
- [x] A-006 — CI/tests: one database per DB-touching vitest project (or no cross-project parallelism) — `database` and `server-integration` both dropDatabase() the same DB _(mitigated via `--no-file-parallelism` on `test:integration` + `server-integration`'s own `patches_test_server` database; `database`/`testkit` still share `patches_test` by design — full per-project split tracked as B-012)_
- [x] A-007 — server: sanitize the RpcException branch of RpcExceptionsFilter (no caller-controlled status/message passthrough; correct x-patches-error-code); unit-test all branches
- [x] A-008 — server: drop unused deps (@nestjs/platform-express, @nestjs/typeorm, typeorm, pg, @patches/database) until needed
- [x] A-009 — server: replace cwd/`__dirname` probe in modules/system/server-build.ts with an injected SERVER_VERSION provider
- [x] A-010 — proto: make PROTO_DIR lazy (`getProtoDir()`) so packaging errors fail at call site
- [x] A-011 — proto: breaking.sh must use `ref=` when base resolves to `origin/<branch>`
- [x] A-012 — repo: implement `pnpm keys:generate` referenced in .env.example (JWT keypair) or remove the reference
- [x] A-013 — CI: treat `skipped` as failure in ci-ok; scope `cancel-in-progress` to pull_request only
- [x] A-014 — database: `app_meta.updated_at` needs @UpdateDateColumn (or drop it)
- [x] A-015 — build: root `pnpm test` must work on a fresh clone (tests resolve @patches/* via dist) — build first via turbo or alias to src
- [x] A-016 — tests: unit coverage for RpcExceptionsFilter, LoggingInterceptor, logger.factory, grpc-options, SystemService, tui cli/ping.ts, cli.tsx
- [x] A-017 — terminal-media: bound untrusted image input (byte-length precheck + explicit sharp limitInputPixels) before Phase 5
- [x] A-018 — server: replace the process-local fixed-window `RateLimitService` with a db-backed store (§102 wants the sensitive flows db-backed by MVP); needs a `rate_limit_buckets` table + migration in `packages/database` and a sweep job _(`rate_limit_buckets`/migration landed earlier alongside `admin_audit_log`, 63c4f0f; this task adds `DbRateLimitStore` + `RateLimitService.consumeDistributed`/`consumeDistributedPeer`, called alongside — not instead of — the existing in-memory check for register/login/password_reset/verify_email/resend_verification/ssh_challenge; lazy 1-in-50 sweep-on-write in `DbRateLimitStore.increment` rather than a dedicated worker job, since nothing in `apps/worker` currently schedules a recurring job on a timer at all — see `docs/architecture/auth.md` §9)_
- [x] A-019 — server: key rate limits on the caller's peer address as well as the subject — needs the gRPC `ServerUnaryCall` (arg index 2), which a ts-proto controller signature does not expose; likely an interceptor that stashes the peer in the request context. Done as part of A-023: `RequestContextInterceptor` now reads `ServerUnaryCall.getPeer()` off `getArgByIndex(2)` and stashes it (port stripped) on `RequestContext`; `RateLimitService.consumePeer()` checks it alongside the existing subject budget for register/login/password_reset/ssh_challenge/ssh_complete.
- [x] A-020 — proto/domain: move `buildSshChallengeBlob` (and the SSH wire helpers) into a package both `apps/server` and `apps/tui` import, so the signed-blob layout has exactly one definition (§166); today the server owns it and a client must reimplement it _(`buildSshChallengeBlob`/`encodeSshString`/`encodeSshStrings`/`SshReader`/`SshWireError` moved to `@patches/domain`'s `src/ssh/{challenge-blob,wire}.ts`, generalized to take an explicit `domainSeparator` so login and B-021 enrollment share it too; `apps/server/src/modules/auth/ssh/{challenge-blob,wire}.ts` deleted, `apps/tui/src/auth/ssh-login.ts`'s duplicate `buildSshLoginBlob` deleted; pinned-fixture parity test in the domain package plus one on each importing side)
- [x] A-021 — server: `RegisterRequest.client_request_id` is accepted but not enforced — §45 asks for idempotency on (handle_normalized, client_request_id); today only the handle unique index prevents a double-register. Schema half landed (2026-08-18) by the Phase 4/6 server task: `Actor.clientRequestId` + `UNIQUE (handle_normalized, client_request_id)` via `packages/database/src/migrations/1787059787165-ActorRegistrationIdempotency.ts`. Still open: `AuthService.register` (`apps/server/src/modules/auth/**`, out of that task's file scope) does not check this column before insert yet — needs a check-then-insert/catch-unique-violation idempotency path there, same pattern as `PostService.createPost`/`GraphService.followActor`.
- [x] A-022 — docs: add `NOT_IMPLEMENTED → UNIMPLEMENTED` to the error table in `docs/architecture/api.md` §7, and document the SSH challenge blob's wire encoding in `docs/architecture/auth.md` §4
- [x] A-023 — server auth (review blockers): (a) `beginSshLogin` limiter keyed on client-supplied fingerprint / shared `anonymous` bucket, `completeSshLogin` keyed on single-use challengeId → limits never fire; (b) no peer IP plumbed into `RateLimitService` (register/login/reset all keyed on attacker-chosen subject → unbounded Argon2id CPU); (c) `resetPassword` unthrottled and hashes before validating the code. Fix with per-peer keys (`ServerUnaryCall.getPeer()` via the 3rd handler arg) + subject keys, throttle reset, validate-then-hash
- [x] A-024 — server auth (review majors): reuse detection can miss a concurrently rotated token (`revokeFamilyOutOfBand` UPDATE snapshot) → revoke at session level; add tests for refresh reuse/family revocation and SSH challenge single-use/replay; `ARGON2_MEMORY_KIB` floor 19456; dedupe `hashCode`/`hashRefreshToken`; RSA modulus ≥2048; `prune` must not evict live victim buckets; drop `rateLimit.reset` on login success
- [x] A-025 — server auth (minors): `actor.userId ?? ""` masks null (assert instead); `claimedHandle` path unimplemented in `ssh-challenge.service.ts`; drop dead `refreshTokenHashesMatch`; do not spread `exception.context` into warn logs
- [x] A-026 — server: `GraphService.followActor` (`apps/server/src/modules/graph/**`) doesn't call `NotificationsService.notifyFollow` on a new follow — `NotificationsModule`/`NotificationsService.notifyFollow(recipientActorId, actorId)` exist and are exported (P4-003) specifically for this, but `graph/**` was out of that task's file scope. One-line fix: `GraphModule` imports `NotificationsModule`, `GraphService` injects `NotificationsService`, and `followActor` calls `notifyFollow(targetActorId, viewerActorId)` after a genuinely new follow row is inserted (mirror `ReactionsService.likePost`'s `wasNew` pattern so a no-op idempotent re-follow doesn't re-notify).

- [x] B-003 — Manually run the §74 Kitty spike checklist in Ghostty + a non-graphics terminal (`pnpm --filter @patches/terminal-media spike`), record results in `packages/terminal-media/spike/README.md`, tick the two roadmap items
- [x] B-004 — Wire `@patches/terminal-media` into `apps/tui` (PostCard inline image + fallback + `o` open externally) — Phase 5 unless earlier
- [x] B-005 — Root `eslint.config.js` `allowDefaultProject` should cover per-package `*.config.{ts,mts}` so packages don't each need tsconfig splits _(scoped to apps/server, apps/worker, packages/config, packages/database, packages/testkit, packages/terminal-media — apps/tui/packages/proto/packages/media keep their tsconfig-include split, owned elsewhere)_
- [x] B-006 — Add `@grpc/reflection` to the server (grpcurl debugging), dev-only _(behind `GRPC_REFLECTION`, default off, on in `.env.example`'s dev block; grpcurl not installed in this environment, untested-live)_
- [x] B-007 — Validate/implement tmux passthrough for Kitty graphics (currently treated as unsupported) _(`GraphicsCapabilities.tmux` + `KittyGraphicsRenderer` wraps every APC write in `wrapTmuxPassthrough`; unit-tested, unverified against a live tmux+Ghostty — see `docs/research/ink-kitty-graphics.md`)_
- [x] B-008 — lefthook pre-commit (prettier/eslint on staged) via mise
- [x] B-009 — `pnpm dev` (turbo) runs the interactive TUI inside turbo output — exclude tui from the root `dev` task or make it server-only
- [x] B-010 — TUI screenshot tool (`tools/screenshot`): tmux -> ANSI -> SVG -> PNG, `mise run screenshot`, real PNGs in `docs/media/`, one embedded in README
- [x] B-011 — ~~root vitest projects skip `tools/*`~~ _(obsolete: `tools/screenshot` was removed)_
- [x] B-012 — full per-project test database isolation: `database` and `testkit` still share `patches_test` (safe today only via `--no-file-parallelism` on `test:integration`, see A-006/docs/operations/ci.md "Why one database") — give `testkit` its own `TEST_DATABASE_URL_TESTKIT`-style override (mirroring `apps/server/vitest.integration.config.mts`) once someone owns `packages/testkit`'s vitest config _(database named `patches_testkit_test`, not `patches_test_testkit` — see doc comment in `packages/testkit/vitest.config.ts`; `--no-file-parallelism` kept on `test:integration` — tested removing it, causes unrelated server-integration failures under full parallel load, see docs/operations/ci.md)_
- [x] B-013 — worker: stale-lease sweep for jobs stuck in `PROCESSING` after a worker crash (not SIGTERM) — reset to `PENDING` after `locked_at` exceeds a lease TTL
- [x] B-014 — admin CLI: inspect/replay `DEAD` outbox jobs (`patches-admin jobs list|replay <id>`) _(`apps/admin/src/commands/jobs.ts` + `packages/database/src/repositories/outbox.ts`'s `replayOutboxJob`, conditional `UPDATE ... WHERE status = 'DEAD'`, preserves `attempts`)_
- [x] B-015 — TUI: Ink `App`/`ConnectScreen` auth integration (status bar `@handle`, inline `L` login, `g p` placeholder) + `apps/tui/test/harness.tsx` ink-testing-library frame-snapshot harness (deferred from P1-007)
- [x] B-016 — TUI: wrong password on `patches login` prints "Your session is no longer valid" — map UNAUTHENTICATED from Login/Register to "Wrong handle/email or password" (keep the uniform server response, fix the client copy)
- [x] B-017 — TUI: remaining harness snapshot tests (login flow, compose→profile timeline, profile, local-feed pagination) + open an author profile from a selected post (PostList selection → ProfileScreen actorId)
- [x] B-018 — proto: `Actor.nameplate` (§173, ≤2 KiB validated doc; the DB column exists) and a post content-warning field are missing from `patches.v1` — add via /proto-change, then surface in server mappers (done: `Nameplate` message + `Actor.nameplate`/`UpdateProfileRequest.nameplate`, `Post.content_warning`/`CreatePostRequest.content_warning`, server mappers/validation). TUI PostRow/ProfileScreen surfacing tracked separately — see B-019.
- [x] B-020 — server: `SearchActors`/`ListFollowers`/`ListFollowing` return zeroed `counts` placeholders — batch real counts (one grouped query) or leave `counts` unset; TUI now refetches on profile open, so this is a data-honesty fix
- [x] B-021 — server+proto: `AddCredential` for SSH keys has no server-verified possession proof — add a `BeginSshLogin`-shaped enrollment challenge (TUI `patches keys add` currently only self-checks via the agent) _(new `BeginSshEnrollment` RPC + `AddCredentialRequest.ssh_proof` (`SshEnrollmentProof`); authenticated challenge bound to caller `user_id` + key fingerprint, JSON-encoded into the existing `ssh_login_challenges.claimed_handle` column since a dedicated purpose/binding schema column was outside this change's owned files — see `docs/architecture/auth.md`'s "Enrollment" subsection for the deviation; verified with the same `verifySshSignature` as login, distinct `patches-ssh-enroll-v1` domain separator; missing proof → `VALIDATION_ERROR`, everything else (wrong user/key, expired, replayed, bad signature) → uniform `AUTH_INVALID_CREDENTIALS`; TUI `keys add`/`AccountsScreen` wired through)
- [x] B-022 — TUI: nameplate `avatarFrame`/`profileBorder` rendering + a plain-mode toggle that strips all decoration (§173); in-app account screen wrapping `ssh-enroll.ts`
- [x] B-019 — TUI: surface `Actor.nameplate` (name color/glyph/badges/status line) in `ProfileScreen` and `Post.content_warning` (click-to-reveal) in `PostRow`, now that both are on the wire (B-018)
- [x] B-023 — TUI Pages editor: structured block-by-block add/remove/reorder UI (P45-006 shipped the `$EDITOR` raw-JSON round trip only; the structured form editor was deferred) _(`E` on `PageScreen` opens `screens/PageBlocksEditorScreen.tsx` — `j`/`k` select, `J`/`K` reorder, `a` add via a type picker, `d` delete with `y`/`n` confirm, `Enter` edit a block's scalar/comma-list fields, `Ctrl+S` validates the whole document with `parsePageStrict` and saves, `Esc` keeps the draft via the same `draftStore` the `$EDITOR` flow uses. Field shapes mirrored by hand in `apps/tui/src/pages/block-editor-schema.ts` (limits imported from `@patches/domain`). `Links` blocks list/reorder/delete but aren't form-editable yet — deferred to the raw `$EDITOR` flow. Tests: `apps/tui/test/page-blocks-editor.test.tsx`.)_
- [x] B-024 — proto+server: a bulk "list mutual follows" RPC — `PageScreen`'s `Friends` block currently renders a documented `[friends list unavailable]` placeholder because `SocialGraphService` only has per-actor `GetRelationship`, no `ListFollowing`/`ListFollowers` _(`SocialGraphService.ListMutualFollows(actor_id, cursor, limit)` — self-join on `follows`, keyset on the caller-facing edge's `(created_at DESC, id DESC)`, backed by a new `ActorService.listMutualFollows` reused for counts/mapping; requires an authenticated session like its siblings. TUI half done: `PatchesApi.listMutualFollows` (public read) and `PageScreen`'s `Friends` block now call it instead of rendering the placeholder — see `apps/tui/src/pages/render/blocks.tsx`, tested in `apps/tui/src/pages/render/blocks.test.tsx` and `apps/tui/test/friends-resolve.test.tsx`.)_
- [x] B-025 — database: migration giving `ssh_login_challenges` dedicated `purpose`/`bound_user_id`/`bound_fingerprint` columns (B-021 currently JSON-encodes the enrollment binding into `claimed_handle`) _(`Phase9Hardening` migration; `SshChallengeService`/`consumeEnrollmentProof` read the new columns directly instead of JSON-decoding `claimed_handle`; `claimed_handle`/`sshEnrollmentBindingSchema`'s JSON-binding hack removed; all 32 existing auth integration tests stay green)_
- [x] B-026 — federation: encrypt `federation_keys` private keys at rest (server key from env), before any real federation is enabled _(AES-256-GCM, `FEDERATION_KEY_ENCRYPTION_KEY` base64 32 bytes — `packages/database/src/crypto/federation-key-cipher.ts`, shared by `apps/server`'s `KeyService` and `apps/worker`'s `FederationDeliverHandler`; required in the server env schema when `FEDERATION_ENABLED=true`, optional-but-needed on the worker (no `FEDERATION_ENABLED` flag there); `private_key_pem` replaced by `private_key_ciphertext`/`private_key_iv`/`private_key_tag` in the same `Phase9Hardening` migration; `openssl rand -base64 32` documented, no separate `pnpm fed:keygen` script added; unit round-trip tests in `packages/database` and `apps/server`)_
- [x] B-027 — federation: outbox true pagination (OrderedCollectionPage), `domain_blocks` write path (`patches-admin domain block|unblock`) and enforcement on outbound delivery _(`OutboxCollectionService.buildCollection`/`buildPage` — keyset `OrderedCollection`/`OrderedCollectionPage`, served at `GET /users/:handle/outbox[?page=…]`; `patches-admin domain block|unblock|list`, audited (`ADMIN_AUDIT_SUBJECT_TYPES` widened to add `DOMAIN`); `DeliveryService.enqueue` filters blocked-domain inbox URLs at enqueue time, `apps/worker`'s `FederationDeliverHandler` re-checks at delivery time; inbound blocking in `InboxService` verified unchanged)_
- [x] B-028 — proto+server+TUI: discover a remote actor by `acct:` (WebFinger) via gRPC (`SearchActors` for `user@domain` or a `ResolveActor` RPC) so users can follow remote accounts from the TUI _(server+proto half: `ActorService.ResolveActor(acct)` — calls `RemoteActorService.resolveByAcct` when `FEDERATION_ENABLED`, else `NOT_IMPLEMENTED`; authenticated, rate-limited per caller (`ActorResolveRateLimitService`); unit-tested with a mocked path via the two-node lab's existing coverage. TUI half done: `SearchScreen` recognizes a `user@domain`-shaped query and calls `PatchesApi.resolveActor` instead of the usual `SearchActors`, showing a single result row (`Enter` opens the profile, existing `f` follows any actor id) or a friendly message for `NOT_IMPLEMENTED`/signed-out — see `apps/tui/src/screens/SearchScreen.tsx`, tested in `apps/tui/test/friends-resolve.test.tsx`.)_
- [x] B-029 — federation: compose-based manual two-node lab (`mise run fed:lab`) mirroring the automated P8-008 test _(`infra/lab/fed-lab.sh` — builds server/worker/tui, migrates `patches_lab_a`/`patches_lab_b`, starts node A (grpc 50061/http 8081) + node B (grpc 50062/http 8082) + a worker each, `mise run fed:lab:down` kills the recorded PIDs; run end-to-end — register alice@A/bob@B, `ResolveActor` WebFinger discovery of `bob@127.0.0.1:8082`, `FollowActor` → async `Accept`, `CreatePost` on B → async delivery → visible in alice's `ListHomeFeed` on A, confirmed via `/federation/metrics` — transcript in `docs/operations/federation.md` "Manual two-node lab (B-029)")_
- [x] B-030 — worker: expose `deliveries_succeeded/failed/dead` federation counters (periodic `federation_metrics` log line like the server, or a loopback HTTP snapshot) — today only per-job structured logs _(`JobRunner.run()` logs `DeliveryMetricsRegistry.snapshot()` as a `{"event":"federation_metrics",...}` line every 60s, same interval-gate pattern as `sweepStaleLeasesIfDue`, unconditional since the worker has no `FEDERATION_ENABLED` flag to gate on)_
- [x] B-031 — deploy: R2 media credentials + Resend sending domain + verified email flow on the live node _(deployed 2026-08-18: live R2 upload/derivative path, Resend API key, verified `updates.allisons.dev` sender)_
- [x] B-032 — TUI: no client-side way to delete your own post — wire `DeletePost` to a `d` keybinding (confirm prompt) on own posts in PostList/Thread; server RPC already exists
- [x] B-045 — **blocker for P13-014.** Replace the sender-chosen franking commitment with a construction that binds to what was actually sent (committing AEAD — CTX, or Grubbs/Lu/Ristenpart CtE2/HFC). `franking_commitment` is 32 sender-chosen bytes the node only length-checks (`apps/server/src/modules/e2ee/e2ee-fanout.ts:339`), so a sender repudiates any message by sending garbage. **Needs an ADR amendment and architect input, not an implementation ticket.** See ADR 0024.
- [x] B-046 — **blocker for P13-014.** Bind `franking_commitment` into every device envelope's AEAD associated data and add it (plus `opening_ciphertext`) to `canonicalFanoutTranscript` (`packages/domain/src/e2ee/envelopes.ts:145`), so ADR 0020 §9's "each device payload authenticates the common commitment" is enforced rather than asserted. Required if B-045 keeps any sender-chosen-commitment shape.
- [x] B-047 — **blocker for P13-014.** Mandatory recipient-side franking verification on decrypt, with specified UX for a mismatch. `verifyFrankingCommitment` has no caller in `apps/tui`/`apps/web`/`apps/admin` — the half of franking that makes a report meaningful does not exist.
- [x] B-050 — `e2eeReportFrankingVerifier.verifyNodeTag` (`apps/server/src/modules/e2ee/report-evidence.ts:143`) calls raw `hmacSha256` under the node's long-term franking key over untyped `Bytes`, bypassing `verifyNodeReportTag` and enforcing no domain separator at the call site of an exported function. Take a `FrankingReportTranscript` and delegate. _(already closed alongside ADR 0025: `verifyNodeTag` now delegates to `verifyNodeReportTagOverEncodedTranscript`, which checks the `REPORT_CONTEXT` domain-separator prefix before MACing.)_
- [x] B-051 — `encodePrekeyBundleTranscript` (`apps/server/src/modules/e2ee/e2ee.codec.ts:214`) writes three unvalidated `fixed()` fields into a **signed** transcript; injective only because of database CHECK constraints two layers away. Add explicit width checks in the encoder. _(already closed by B-055: `ByteWriter#fixed`'s required-width parameter makes every `fixed()` call in `encodePrekeyBundleTranscript` self-validating; the encoder's own comment says so.)_
- [x] B-052 — Bind `franking_profile` into both `commitFranking` and `encodeReportTranscript` (`packages/crypto/src/franking.ts:48`, `:113`). Neither covers it today, so the profile is unauthenticated data that `verifyReportEvidence` branches on, and a future `patches-franking-v2` would be cross-acceptable with v1. `E2EE_ALGORITHM` is the wrong agility knob — it names the ratchet suite. _(already closed: ADR 0025's landing commit `e2f3fa8` bound `frankingProfile` into both `encodeCommitmentTranscript` and `encodeReportTranscript`.)_
- [x] B-053 — `packages/crypto/src/franking.ts:67-68` claims post-acceptance ciphertext tampering invalidates the tag. **That is false**: `ciphertext_digest` is sender-asserted and never recomputed from `ciphertext` anywhere in the server. Either verify the digest on accept or correct the comment. _(closed by verifying: added `assertCiphertextDigestsMatchCiphertexts` in `packages/domain/src/e2ee/envelopes.ts`, called from `e2ee-fanout.ts`'s accept path — the node now recomputes each envelope's digest from its ciphertext and rejects the fanout on mismatch, so a stored digest is guaranteed to match the ciphertext stored beside it. `franking.ts`'s comment corrected to say precisely that, and to say what it still does not cover — a compromised node rewriting stored bytes after acceptance, which no part of franking defends against.)_
- [x] B-054 — `attachReportEvidence` (`apps/server/src/modules/e2ee/report-evidence.ts:189`) performs no conversation-membership check on `request.conversationId`; ADR 0020 §9 requires conversation authorization before evidence is marked verified. Distinct from the subject-type gap B-049 covers. _(closed: `attachReportEvidence` now looks up a `ConversationMember` row for `(request.conversationId, actorId)` before doing anything else, same no-oracle `E2EE_CONVERSATION_NOT_FOUND` error for "no such conversation" and "you were never a member" as `e2ee-conversation.service.ts` already uses.)_
- [x] B-055 — Give `ByteWriter.fixed()` a required expected-width parameter (`packages/crypto/src/codec.ts:36`) so a transcript cannot silently become non-injective, and comment `requireKeyBytes` (`franking.ts:47`) as the load-bearing anti-GLR key-length invariant rather than ordinary input validation.
- [x] B-056 — `TRANSCRIPT_MISMATCH` (`packages/domain/src/e2ee/franking.ts:71`) is in the closed failure-code set but assigned by no code path. Add the missing check or remove the code.
- [x] B-057 — `E2eeFrankingTagView.transcriptDigest` carries the full encoded transcript at `report-evidence.ts:312` and a 32-byte digest at `e2ee-fanout.ts:472`; the proto documents it as a digest. Split into distinct fields or types.
- [ ] B-033 — mobile: native passkey login/enrollment spike (from P10-017). `docs/research/expo-react-native.md` §6: no wrapper today works inside Expo Go, `react-native-passkeys` needs a custom dev-client/EAS build plus hosted `apple-app-site-association`/`assetlinks.json` at the app's real bundle id/package name, and neither Apple/Google/Expo nor the wrapper's own docs confirm its output JSON is byte-compatible with `@simplewebauthn/server`'s expected shapes (`AuthService.BeginPasskeyLogin`/`CompletePasskeyLogin`). Needs a runtime spike (dev-client build, AASA/asset-links hosting, a real compatibility check against the server RPCs) before implementation — possibly an ADR if still prioritized.

- [x] B-001 — Decide MinIO vs R2 dev bucket for local media dev (spec §96 says either)
- [x] B-002 — `apps/server` uses `@patches/config` env schemas instead of its self-contained one; `PUBLIC_ORIGIN` requires http(s) protocol
