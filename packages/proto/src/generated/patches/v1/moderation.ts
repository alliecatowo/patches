// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: patches/v1/moderation.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { Timestamp } from '../../google/protobuf/timestamp.js';
import { Actor } from './actors.js';
import { PageInfo } from './common.js';
import { Relationship } from './social_graph.js';

export const protobufPackage = 'patches.v1';

export enum ReportReason {
  REPORT_REASON_UNSPECIFIED = 'REPORT_REASON_UNSPECIFIED',
  REPORT_REASON_SPAM = 'REPORT_REASON_SPAM',
  REPORT_REASON_HARASSMENT = 'REPORT_REASON_HARASSMENT',
  REPORT_REASON_HATE_SPEECH = 'REPORT_REASON_HATE_SPEECH',
  REPORT_REASON_ILLEGAL_CONTENT = 'REPORT_REASON_ILLEGAL_CONTENT',
  REPORT_REASON_IMPERSONATION = 'REPORT_REASON_IMPERSONATION',
  REPORT_REASON_OTHER = 'REPORT_REASON_OTHER',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

/**
 * A bounded vocabulary (spec §202's `MODERATION_LOG_REASON_CATEGORIES`), derived from
 * `docs/product/moderation.md`'s guideline list — never a report's free-text `details`, never
 * a moderator's internal note. Published on `domain_blocks` (as the public counterpart to the
 * operator's free-text `reason`) and on every `ModerationLogEntry`.
 */
export enum ModerationReasonCategory {
  MODERATION_REASON_CATEGORY_UNSPECIFIED = 'MODERATION_REASON_CATEGORY_UNSPECIFIED',
  MODERATION_REASON_CATEGORY_HARASSMENT = 'MODERATION_REASON_CATEGORY_HARASSMENT',
  MODERATION_REASON_CATEGORY_HATE = 'MODERATION_REASON_CATEGORY_HATE',
  MODERATION_REASON_CATEGORY_THREATS = 'MODERATION_REASON_CATEGORY_THREATS',
  MODERATION_REASON_CATEGORY_DOXXING = 'MODERATION_REASON_CATEGORY_DOXXING',
  MODERATION_REASON_CATEGORY_IMPERSONATION = 'MODERATION_REASON_CATEGORY_IMPERSONATION',
  MODERATION_REASON_CATEGORY_SPAM = 'MODERATION_REASON_CATEGORY_SPAM',
  MODERATION_REASON_CATEGORY_ILLEGAL_CONTENT = 'MODERATION_REASON_CATEGORY_ILLEGAL_CONTENT',
  MODERATION_REASON_CATEGORY_NCII = 'MODERATION_REASON_CATEGORY_NCII',
  MODERATION_REASON_CATEGORY_INFRASTRUCTURE_ABUSE = 'MODERATION_REASON_CATEGORY_INFRASTRUCTURE_ABUSE',
  MODERATION_REASON_CATEGORY_OTHER = 'MODERATION_REASON_CATEGORY_OTHER',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

/**
 * The node enforcement actions that generate a moderation notice (spec §201.2), plus the
 * domain-level action published in the moderation log (spec §201.4-§201.5).
 */
export enum ModerationActionType {
  MODERATION_ACTION_TYPE_UNSPECIFIED = 'MODERATION_ACTION_TYPE_UNSPECIFIED',
  MODERATION_ACTION_TYPE_WARN = 'MODERATION_ACTION_TYPE_WARN',
  MODERATION_ACTION_TYPE_SUSPEND = 'MODERATION_ACTION_TYPE_SUSPEND',
  MODERATION_ACTION_TYPE_BAN = 'MODERATION_ACTION_TYPE_BAN',
  MODERATION_ACTION_TYPE_POST_REMOVAL = 'MODERATION_ACTION_TYPE_POST_REMOVAL',
  MODERATION_ACTION_TYPE_MEDIA_TAKEDOWN = 'MODERATION_ACTION_TYPE_MEDIA_TAKEDOWN',
  MODERATION_ACTION_TYPE_DOMAIN_BLOCK = 'MODERATION_ACTION_TYPE_DOMAIN_BLOCK',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

export enum ModerationLogSubjectKind {
  MODERATION_LOG_SUBJECT_KIND_UNSPECIFIED = 'MODERATION_LOG_SUBJECT_KIND_UNSPECIFIED',
  MODERATION_LOG_SUBJECT_KIND_DOMAIN = 'MODERATION_LOG_SUBJECT_KIND_DOMAIN',
  MODERATION_LOG_SUBJECT_KIND_ACCOUNT = 'MODERATION_LOG_SUBJECT_KIND_ACCOUNT',
  MODERATION_LOG_SUBJECT_KIND_POST = 'MODERATION_LOG_SUBJECT_KIND_POST',
  MODERATION_LOG_SUBJECT_KIND_MEDIA = 'MODERATION_LOG_SUBJECT_KIND_MEDIA',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

export interface BlockActorRequest {
  actorId: string;
}

export interface BlockActorResponse {
  relationship: Relationship | undefined;
}

export interface UnblockActorRequest {
  actorId: string;
}

export interface UnblockActorResponse {
  relationship: Relationship | undefined;
}

export interface MuteActorRequest {
  actorId: string;
}

export interface MuteActorResponse {
  relationship: Relationship | undefined;
}

export interface UnmuteActorRequest {
  actorId: string;
}

export interface UnmuteActorResponse {
  relationship: Relationship | undefined;
}

export interface ListBlocksRequest {
  cursor: string;
  limit: number;
}

export interface ListBlocksResponse {
  actors: Actor[];
  page: PageInfo | undefined;
}

export interface ListMutesRequest {
  cursor: string;
  limit: number;
}

export interface ListMutesResponse {
  actors: Actor[];
  page: PageInfo | undefined;
}

export interface ReportPostRequest {
  postId: string;
  reason: ReportReason;
  /** Optional free text. Max 2,000 characters (spec §58-style bound). */
  details: string;
}

export interface ReportPostResponse {
  reportId: string;
}

export interface ReportActorRequest {
  actorId: string;
  reason: ReportReason;
  /** Optional free text. Max 2,000 characters (spec §58-style bound). */
  details: string;
}

export interface ReportActorResponse {
  reportId: string;
}

export interface ReportE2eeMessageRequest {
  /**
   * An `E2eeLogicalMessage.id` (client-generated logical id shared across every per-device
   * envelope) — never a mailbox envelope id, which is per-device.
   */
  logicalMessageId: string;
  reason: ReportReason;
  /**
   * Optional free text. Max 2,000 characters (spec §58-style bound). Never message plaintext —
   * that is disclosed separately, and only with explicit consent, via
   * `E2eeService.AttachReportEvidence` (ADR 0020 §9).
   */
  details: string;
}

export interface ReportE2eeMessageResponse {
  reportId: string;
}

/**
 * A public, anonymized transparency record (spec §201.4). Account/post/media entries never
 * carry a handle, actor id, or post id — the acted-upon actor already has the full,
 * identified version in their own `ModerationNotice`, and moderators have it in
 * `admin_audit_log`. A domain entry is fully identified because it is the node's own
 * federation decision about its own conduct, not a record of any individual's conduct.
 */
export interface ModerationLogEntry {
  id: string;
  action: ModerationActionType;
  subjectKind: ModerationLogSubjectKind;
  /** Set only when `subject_kind == MODERATION_LOG_SUBJECT_KIND_DOMAIN`. */
  subjectDomain: string;
  reasonCategory: ModerationReasonCategory;
  /** Whether this action was appealed — never the appeal's content (spec §201.3, §208). */
  appealed: boolean;
  createdAt: Timestamp | undefined;
}

export interface ListModerationLogRequest {
  cursor: string;
  limit: number;
}

export interface ListModerationLogResponse {
  entries: ModerationLogEntry[];
  page: PageInfo | undefined;
}

/**
 * A private, notified read projection of the `admin_audit_log` row that enforced an action
 * against the caller (spec §201.2) — not a second source of truth. Contrast with a label
 * (`LabelService`, spec §200.4): a labeler's judgement is never notified, an enforcement
 * action always is.
 */
export interface ModerationNotice {
  id: string;
  action: ModerationActionType;
  /** Empty unless the action concerned a specific post. */
  postId: string;
  reasonCategory: ModerationReasonCategory;
  /**
   * Purpose-written for the subject at resolution time — never `reports.moderator_note`
   * (spec §55, §201.2, §208).
   */
  explanation: string;
  createdAt: Timestamp | undefined;
  /**
   * Deadline to file an appeal (`AppealService.CreateAppeal`); unset if the appeal window has
   * already closed or no appeal applies to this action.
   */
  appealDeadline: Timestamp | undefined;
  /** True once the caller has filed an appeal against this notice. */
  appealed: boolean;
}

export interface ListMyModerationNoticesRequest {
  cursor: string;
  limit: number;
}

export interface ListMyModerationNoticesResponse {
  notices: ModerationNotice[];
  page: PageInfo | undefined;
}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * User-facing block/mute/report surface (spec §55, §61–64, Phase 6 spec §140). Moderator/admin
 * actions (resolving a report, suspending an account) are deliberately not here — spec §65
 * puts that in the admin CLI, not a user-facing gRPC service, and "no user-facing API should
 * expose internal moderator notes" (§55).
 */

export interface ModerationServiceClient {
  /**
   * Rejects a block in either direction of an existing follow by removing it (spec §62); the
   * relationship returned reflects that removal. Idempotent — blocking an already-blocked
   * actor is not an error.
   */

  blockActor(request: BlockActorRequest, metadata?: Metadata): Observable<BlockActorResponse>;

  /** Idempotent: unblocking an actor the caller has not blocked is not an error. */

  unblockActor(request: UnblockActorRequest, metadata?: Metadata): Observable<UnblockActorResponse>;

  muteActor(request: MuteActorRequest, metadata?: Metadata): Observable<MuteActorResponse>;

  /** Idempotent: unmuting an actor the caller has not muted is not an error. */

  unmuteActor(request: UnmuteActorRequest, metadata?: Metadata): Observable<UnmuteActorResponse>;

  /** The caller's own block list, most-recent first. */

  listBlocks(request: ListBlocksRequest, metadata?: Metadata): Observable<ListBlocksResponse>;

  /** The caller's own mute list, most-recent first. */

  listMutes(request: ListMutesRequest, metadata?: Metadata): Observable<ListMutesResponse>;

  reportPost(request: ReportPostRequest, metadata?: Metadata): Observable<ReportPostResponse>;

  reportActor(request: ReportActorRequest, metadata?: Metadata): Observable<ReportActorResponse>;

  /**
   * A third sibling of `ReportPost`/`ReportActor`, not a generic report RPC (ADR 0020 §9): the
   * node holds no plaintext for an E2EE logical message, so this creates an evidence-free
   * `Report` row keyed by `subject_e2ee_logical_message_id`. A follow-up
   * `E2eeService.AttachReportEvidence` call supplies the reporter-disclosed
   * plaintext/opening/franking material against this report id. `ReportMessage`, the plaintext
   * sibling this once had (snapshot-backed evidence for a server-visible DM), was removed by
   * ADR 0030 §B-095 alongside the rest of the server-visible DM machinery — `ReportE2eeMessage`
   * plus disclosed evidence is the whole moderation story for DMs now.
   */

  reportE2EeMessage(
    request: ReportE2eeMessageRequest,
    metadata?: Metadata,
  ): Observable<ReportE2eeMessageResponse>;

  /**
   * A public transparency instrument about the node's own conduct, not a public record of
   * any individual's conduct (spec §201.4). Unauthenticated. Domain entries are fully
   * identified; account/post/media entries are anonymized by construction — no handle, actor
   * id, or post id, ever.
   */

  listModerationLog(
    request: ListModerationLogRequest,
    metadata?: Metadata,
  ): Observable<ListModerationLogResponse>;

  /**
   * The caller's own moderation notices — the private, notified, appealable read projection
   * of `admin_audit_log` rows that acted on them (spec §201.2).
   */

  listMyModerationNotices(
    request: ListMyModerationNoticesRequest,
    metadata?: Metadata,
  ): Observable<ListMyModerationNoticesResponse>;
}

/**
 * User-facing block/mute/report surface (spec §55, §61–64, Phase 6 spec §140). Moderator/admin
 * actions (resolving a report, suspending an account) are deliberately not here — spec §65
 * puts that in the admin CLI, not a user-facing gRPC service, and "no user-facing API should
 * expose internal moderator notes" (§55).
 */

export interface ModerationServiceController {
  /**
   * Rejects a block in either direction of an existing follow by removing it (spec §62); the
   * relationship returned reflects that removal. Idempotent — blocking an already-blocked
   * actor is not an error.
   */

  blockActor(
    request: BlockActorRequest,
    metadata?: Metadata,
  ): Promise<BlockActorResponse> | Observable<BlockActorResponse> | BlockActorResponse;

  /** Idempotent: unblocking an actor the caller has not blocked is not an error. */

  unblockActor(
    request: UnblockActorRequest,
    metadata?: Metadata,
  ): Promise<UnblockActorResponse> | Observable<UnblockActorResponse> | UnblockActorResponse;

  muteActor(
    request: MuteActorRequest,
    metadata?: Metadata,
  ): Promise<MuteActorResponse> | Observable<MuteActorResponse> | MuteActorResponse;

  /** Idempotent: unmuting an actor the caller has not muted is not an error. */

  unmuteActor(
    request: UnmuteActorRequest,
    metadata?: Metadata,
  ): Promise<UnmuteActorResponse> | Observable<UnmuteActorResponse> | UnmuteActorResponse;

  /** The caller's own block list, most-recent first. */

  listBlocks(
    request: ListBlocksRequest,
    metadata?: Metadata,
  ): Promise<ListBlocksResponse> | Observable<ListBlocksResponse> | ListBlocksResponse;

  /** The caller's own mute list, most-recent first. */

  listMutes(
    request: ListMutesRequest,
    metadata?: Metadata,
  ): Promise<ListMutesResponse> | Observable<ListMutesResponse> | ListMutesResponse;

  reportPost(
    request: ReportPostRequest,
    metadata?: Metadata,
  ): Promise<ReportPostResponse> | Observable<ReportPostResponse> | ReportPostResponse;

  reportActor(
    request: ReportActorRequest,
    metadata?: Metadata,
  ): Promise<ReportActorResponse> | Observable<ReportActorResponse> | ReportActorResponse;

  /**
   * A third sibling of `ReportPost`/`ReportActor`, not a generic report RPC (ADR 0020 §9): the
   * node holds no plaintext for an E2EE logical message, so this creates an evidence-free
   * `Report` row keyed by `subject_e2ee_logical_message_id`. A follow-up
   * `E2eeService.AttachReportEvidence` call supplies the reporter-disclosed
   * plaintext/opening/franking material against this report id. `ReportMessage`, the plaintext
   * sibling this once had (snapshot-backed evidence for a server-visible DM), was removed by
   * ADR 0030 §B-095 alongside the rest of the server-visible DM machinery — `ReportE2eeMessage`
   * plus disclosed evidence is the whole moderation story for DMs now.
   */

  reportE2EeMessage(
    request: ReportE2eeMessageRequest,
    metadata?: Metadata,
  ):
    | Promise<ReportE2eeMessageResponse>
    | Observable<ReportE2eeMessageResponse>
    | ReportE2eeMessageResponse;

  /**
   * A public transparency instrument about the node's own conduct, not a public record of
   * any individual's conduct (spec §201.4). Unauthenticated. Domain entries are fully
   * identified; account/post/media entries are anonymized by construction — no handle, actor
   * id, or post id, ever.
   */

  listModerationLog(
    request: ListModerationLogRequest,
    metadata?: Metadata,
  ):
    | Promise<ListModerationLogResponse>
    | Observable<ListModerationLogResponse>
    | ListModerationLogResponse;

  /**
   * The caller's own moderation notices — the private, notified, appealable read projection
   * of `admin_audit_log` rows that acted on them (spec §201.2).
   */

  listMyModerationNotices(
    request: ListMyModerationNoticesRequest,
    metadata?: Metadata,
  ):
    | Promise<ListMyModerationNoticesResponse>
    | Observable<ListMyModerationNoticesResponse>
    | ListMyModerationNoticesResponse;
}

export function ModerationServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      'blockActor',
      'unblockActor',
      'muteActor',
      'unmuteActor',
      'listBlocks',
      'listMutes',
      'reportPost',
      'reportActor',
      'reportE2EeMessage',
      'listModerationLog',
      'listMyModerationNotices',
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('ModerationService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('ModerationService', method)(
        constructor.prototype[method],
        method,
        descriptor,
      );
    }
  };
}

export const MODERATION_SERVICE_NAME = 'ModerationService';
