// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: patches/v1/feeds.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { PageInfo } from './common.js';
import { Post } from './posts.js';

export const protobufPackage = 'patches.v1';

export interface ListHomeFeedRequest {
  cursor: string;
  limit: number;
}

export interface ListHomeFeedResponse {
  posts: Post[];
  page: PageInfo | undefined;
}

export interface ListLocalFeedRequest {
  cursor: string;
  limit: number;
}

export interface ListLocalFeedResponse {
  posts: Post[];
  page: PageInfo | undefined;
}

export interface ListActorPostsRequest {
  actorId: string;
  cursor: string;
  limit: number;
}

export interface ListActorPostsResponse {
  posts: Post[];
  page: PageInfo | undefined;
}

export interface ListTagFeedRequest {
  /** Tag name — matched against the canonical (NFKC-normalized, casefolded) form. */
  tag: string;
  cursor: string;
  limit: number;
}

export interface ListTagFeedResponse {
  posts: Post[];
  page: PageInfo | undefined;
}

export interface ListCommunityFeedRequest {
  communityId: string;
  cursor: string;
  limit: number;
}

export interface ListCommunityFeedResponse {
  posts: Post[];
  page: PageInfo | undefined;
}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * Chronological, fan-out-on-read feeds (spec §52, §59). Never `GetRecommendedFeed`/
 * `GetForYouFeed` — there is no engagement-ranked feed in this product (spec §153).
 */

export interface FeedServiceClient {
  /** The caller's home timeline: local + followed actors, fan-out-on-read, chronological. */

  listHomeFeed(request: ListHomeFeedRequest, metadata?: Metadata): Observable<ListHomeFeedResponse>;

  /** All local public posts, chronological. */

  listLocalFeed(
    request: ListLocalFeedRequest,
    metadata?: Metadata,
  ): Observable<ListLocalFeedResponse>;

  /** A given actor's posts, chronological. */

  listActorPosts(
    request: ListActorPostsRequest,
    metadata?: Metadata,
  ): Observable<ListActorPostsResponse>;

  /**
   * All public posts carrying a given tag, chronological. No ordering parameter — same
   * chronological-only rule as every other feed (spec §182.2).
   */

  listTagFeed(request: ListTagFeedRequest, metadata?: Metadata): Observable<ListTagFeedResponse>;

  /** A community's posts, chronological. No ordering parameter (spec §182.2). */

  listCommunityFeed(
    request: ListCommunityFeedRequest,
    metadata?: Metadata,
  ): Observable<ListCommunityFeedResponse>;
}

/**
 * Chronological, fan-out-on-read feeds (spec §52, §59). Never `GetRecommendedFeed`/
 * `GetForYouFeed` — there is no engagement-ranked feed in this product (spec §153).
 */

export interface FeedServiceController {
  /** The caller's home timeline: local + followed actors, fan-out-on-read, chronological. */

  listHomeFeed(
    request: ListHomeFeedRequest,
    metadata?: Metadata,
  ): Promise<ListHomeFeedResponse> | Observable<ListHomeFeedResponse> | ListHomeFeedResponse;

  /** All local public posts, chronological. */

  listLocalFeed(
    request: ListLocalFeedRequest,
    metadata?: Metadata,
  ): Promise<ListLocalFeedResponse> | Observable<ListLocalFeedResponse> | ListLocalFeedResponse;

  /** A given actor's posts, chronological. */

  listActorPosts(
    request: ListActorPostsRequest,
    metadata?: Metadata,
  ): Promise<ListActorPostsResponse> | Observable<ListActorPostsResponse> | ListActorPostsResponse;

  /**
   * All public posts carrying a given tag, chronological. No ordering parameter — same
   * chronological-only rule as every other feed (spec §182.2).
   */

  listTagFeed(
    request: ListTagFeedRequest,
    metadata?: Metadata,
  ): Promise<ListTagFeedResponse> | Observable<ListTagFeedResponse> | ListTagFeedResponse;

  /** A community's posts, chronological. No ordering parameter (spec §182.2). */

  listCommunityFeed(
    request: ListCommunityFeedRequest,
    metadata?: Metadata,
  ):
    | Promise<ListCommunityFeedResponse>
    | Observable<ListCommunityFeedResponse>
    | ListCommunityFeedResponse;
}

export function FeedServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      'listHomeFeed',
      'listLocalFeed',
      'listActorPosts',
      'listTagFeed',
      'listCommunityFeed',
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('FeedService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('FeedService', method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const FEED_SERVICE_NAME = 'FeedService';
