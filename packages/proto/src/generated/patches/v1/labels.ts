// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: patches/v1/labels.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { Timestamp } from '../../google/protobuf/timestamp.js';
import { Actor } from './actors.js';
import { PageInfo } from './common.js';
import { Community } from './communities.js';

export const protobufPackage = 'patches.v1';

/** The §198.3 actions plus `ignore` (spec §200.1). */
export enum LabelAction {
  LABEL_ACTION_UNSPECIFIED = 'LABEL_ACTION_UNSPECIFIED',
  LABEL_ACTION_IGNORE = 'LABEL_ACTION_IGNORE',
  LABEL_ACTION_WARN = 'LABEL_ACTION_WARN',
  LABEL_ACTION_COLLAPSE = 'LABEL_ACTION_COLLAPSE',
  LABEL_ACTION_HIDE = 'LABEL_ACTION_HIDE',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

/**
 * One value in a labeler's closed, node-published vocabulary (spec §200.2). `value` is never
 * free text — it must be one the node currently publishes, e.g. `spam`, `scam`, `nsfw`.
 */
export interface LabelVocabularyEntry {
  value: string;
  description: string;
  defaultAction: LabelAction;
  /**
   * True when the node has designated this value legally mandatory (e.g. jurisdiction-
   * required adult-content marking) — a viewer cannot set it to `LABEL_ACTION_IGNORE`
   * (spec §200.3).
   */
  mandatory: boolean;
}

export interface Labeler {
  id: string;
  /**
   * Exactly one of `actor`/`community` is set for a non-node labeler; a node's own labeler
   * sets neither and `is_node_labeler` instead (spec §202's `CHECK exactly one of
   * actor_id/community_id/is_node_labeler`).
   */
  actor: Actor | undefined;
  community: Community | undefined;
  isNodeLabeler: boolean;
  vocabulary: LabelVocabularyEntry[];
  createdAt: Timestamp | undefined;
}

export interface Label {
  id: string;
  labelerId: string;
  /** Exactly one subject is set (spec §202, mirrors `reports`). */
  subjectActorId: string;
  subjectPostId: string;
  value: string;
  createdAt: Timestamp | undefined;
  /** Unset means this label never expires. */
  expiresAt: Timestamp | undefined;
  /** Unset means not retracted. */
  retractedAt: Timestamp | undefined;
}

export interface CreateLabelerRequest {
  /**
   * Empty publishes as the caller's own labeler. Set to a community the caller moderates to
   * publish it as that community's labeler instead (spec §200.1).
   */
  communityId: string;
  vocabulary: LabelVocabularyEntry[];
}

export interface CreateLabelerResponse {
  labeler: Labeler | undefined;
}

export interface GetLabelerRequest {
  id: string;
}

export interface GetLabelerResponse {
  labeler: Labeler | undefined;
}

export interface ListLabelersRequest {
  cursor: string;
  limit: number;
}

export interface ListLabelersResponse {
  labelers: Labeler[];
  page: PageInfo | undefined;
}

export interface ApplyLabelRequest {
  labelerId: string;
  /** Exactly one of these. */
  subjectActorId: string;
  subjectPostId: string;
  value: string;
  /** Unset means this label never expires. */
  expiresAt: Timestamp | undefined;
}

export interface ApplyLabelResponse {
  label: Label | undefined;
}

export interface RetractLabelRequest {
  labelId: string;
}

export interface RetractLabelResponse {
  label: Label | undefined;
}

export interface SubscribeLabelerRequest {
  labelerId: string;
}

export interface SubscribeLabelerResponse {}

export interface UnsubscribeLabelerRequest {
  labelerId: string;
}

export interface UnsubscribeLabelerResponse {}

export interface SetLabelerSubscriptionActionRequest {
  labelerId: string;
  value: string;
  action: LabelAction;
}

export interface SetLabelerSubscriptionActionResponse {}

export interface ListLabelsOnSubjectRequest {
  /** Exactly one of these. */
  subjectActorId: string;
  subjectPostId: string;
  cursor: string;
  limit: number;
}

export interface ListLabelsOnSubjectResponse {
  labels: Label[];
  page: PageInfo | undefined;
}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * Labelers and labels (spec §200): subscriber-scoped annotation, never global truth. A label
 * is visible only to actors subscribed to that labeler; labeling someone has no effect on
 * anyone who has not opted in. Labels never affect ordering and are never aggregated into a
 * count, a reputation, or a trust score, for anyone (§200.3, §208). Label values come from a
 * closed vocabulary published by the node (`NodeService.GetNodePolicy`) — free-text values
 * are prohibited (§200.2, §208).
 */

export interface LabelServiceClient {
  createLabeler(
    request: CreateLabelerRequest,
    metadata?: Metadata,
  ): Observable<CreateLabelerResponse>;

  getLabeler(request: GetLabelerRequest, metadata?: Metadata): Observable<GetLabelerResponse>;

  listLabelers(request: ListLabelersRequest, metadata?: Metadata): Observable<ListLabelersResponse>;

  /**
   * Rate-limited (spec §204) — a labeler that can label faster than a human can consider is
   * bot moderation at scale (§200.5). The applying actor's authority stops at their own
   * labeler; operating one grants no authority over any actor, post, community, or node.
   */

  applyLabel(request: ApplyLabelRequest, metadata?: Metadata): Observable<ApplyLabelResponse>;

  /**
   * Sets `retracted_at` rather than deleting the row — retraction preserves history
   * (spec §200.1).
   */

  retractLabel(request: RetractLabelRequest, metadata?: Metadata): Observable<RetractLabelResponse>;

  subscribeLabeler(
    request: SubscribeLabelerRequest,
    metadata?: Metadata,
  ): Observable<SubscribeLabelerResponse>;

  unsubscribeLabeler(
    request: UnsubscribeLabelerRequest,
    metadata?: Metadata,
  ): Observable<UnsubscribeLabelerResponse>;

  /**
   * Per-value action override for the caller's own subscription (spec §200.1's action map).
   * A viewer may set any value to `LABEL_ACTION_IGNORE` except a value the node has
   * designated legally mandatory (`LabelVocabularyEntry.mandatory`, spec §200.3).
   */

  setLabelerSubscriptionAction(
    request: SetLabelerSubscriptionActionRequest,
    metadata?: Metadata,
  ): Observable<SetLabelerSubscriptionActionResponse>;

  /**
   * Pull-only self-inspection (spec §200.4) — an actor is never notified that they were
   * labeled; this is the only way to find out. A label with an enforcement consequence is
   * delivered separately as a moderation notice (`ModerationService.ListMyModerationNotices`).
   */

  listLabelsOnSubject(
    request: ListLabelsOnSubjectRequest,
    metadata?: Metadata,
  ): Observable<ListLabelsOnSubjectResponse>;
}

/**
 * Labelers and labels (spec §200): subscriber-scoped annotation, never global truth. A label
 * is visible only to actors subscribed to that labeler; labeling someone has no effect on
 * anyone who has not opted in. Labels never affect ordering and are never aggregated into a
 * count, a reputation, or a trust score, for anyone (§200.3, §208). Label values come from a
 * closed vocabulary published by the node (`NodeService.GetNodePolicy`) — free-text values
 * are prohibited (§200.2, §208).
 */

export interface LabelServiceController {
  createLabeler(
    request: CreateLabelerRequest,
    metadata?: Metadata,
  ): Promise<CreateLabelerResponse> | Observable<CreateLabelerResponse> | CreateLabelerResponse;

  getLabeler(
    request: GetLabelerRequest,
    metadata?: Metadata,
  ): Promise<GetLabelerResponse> | Observable<GetLabelerResponse> | GetLabelerResponse;

  listLabelers(
    request: ListLabelersRequest,
    metadata?: Metadata,
  ): Promise<ListLabelersResponse> | Observable<ListLabelersResponse> | ListLabelersResponse;

  /**
   * Rate-limited (spec §204) — a labeler that can label faster than a human can consider is
   * bot moderation at scale (§200.5). The applying actor's authority stops at their own
   * labeler; operating one grants no authority over any actor, post, community, or node.
   */

  applyLabel(
    request: ApplyLabelRequest,
    metadata?: Metadata,
  ): Promise<ApplyLabelResponse> | Observable<ApplyLabelResponse> | ApplyLabelResponse;

  /**
   * Sets `retracted_at` rather than deleting the row — retraction preserves history
   * (spec §200.1).
   */

  retractLabel(
    request: RetractLabelRequest,
    metadata?: Metadata,
  ): Promise<RetractLabelResponse> | Observable<RetractLabelResponse> | RetractLabelResponse;

  subscribeLabeler(
    request: SubscribeLabelerRequest,
    metadata?: Metadata,
  ):
    | Promise<SubscribeLabelerResponse>
    | Observable<SubscribeLabelerResponse>
    | SubscribeLabelerResponse;

  unsubscribeLabeler(
    request: UnsubscribeLabelerRequest,
    metadata?: Metadata,
  ):
    | Promise<UnsubscribeLabelerResponse>
    | Observable<UnsubscribeLabelerResponse>
    | UnsubscribeLabelerResponse;

  /**
   * Per-value action override for the caller's own subscription (spec §200.1's action map).
   * A viewer may set any value to `LABEL_ACTION_IGNORE` except a value the node has
   * designated legally mandatory (`LabelVocabularyEntry.mandatory`, spec §200.3).
   */

  setLabelerSubscriptionAction(
    request: SetLabelerSubscriptionActionRequest,
    metadata?: Metadata,
  ):
    | Promise<SetLabelerSubscriptionActionResponse>
    | Observable<SetLabelerSubscriptionActionResponse>
    | SetLabelerSubscriptionActionResponse;

  /**
   * Pull-only self-inspection (spec §200.4) — an actor is never notified that they were
   * labeled; this is the only way to find out. A label with an enforcement consequence is
   * delivered separately as a moderation notice (`ModerationService.ListMyModerationNotices`).
   */

  listLabelsOnSubject(
    request: ListLabelsOnSubjectRequest,
    metadata?: Metadata,
  ):
    | Promise<ListLabelsOnSubjectResponse>
    | Observable<ListLabelsOnSubjectResponse>
    | ListLabelsOnSubjectResponse;
}

export function LabelServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      'createLabeler',
      'getLabeler',
      'listLabelers',
      'applyLabel',
      'retractLabel',
      'subscribeLabeler',
      'unsubscribeLabeler',
      'setLabelerSubscriptionAction',
      'listLabelsOnSubject',
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('LabelService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('LabelService', method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const LABEL_SERVICE_NAME = 'LabelService';
