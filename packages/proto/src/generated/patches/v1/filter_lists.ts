// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: patches/v1/filter_lists.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { Timestamp } from '../../google/protobuf/timestamp.js';
import { Actor } from './actors.js';
import { PageInfo } from './common.js';
import { Community } from './communities.js';
import { FilterAction, FilterTermInput, FilterTermKind } from './filters.js';

export const protobufPackage = 'patches.v1';

export interface FilterListEntry {
  id: string;
  kind: FilterTermKind;
  value: string;
  createdAt: Timestamp | undefined;
}

export interface FilterList {
  id: string;
  /**
   * Exactly one of these is set (spec §202's `CHECK exactly one owner column set`) — a
   * personal list or a community-owned one, maintained by its moderators (spec §199.1).
   */
  ownerActor: Actor | undefined;
  ownerCommunity: Community | undefined;
  name: string;
  displayName: string;
  description: string;
  createdAt: Timestamp | undefined;
  updatedAt: Timestamp | undefined;
}

export interface PublishFilterListRequest {
  name: string;
  displayName: string;
  description: string;
  /**
   * Empty publishes as the caller's own list. Set to a community the caller moderates to
   * publish it as that community's list instead (spec §199.1); a community's list has no
   * authority over non-members or inside the community — it is a recommendation like any
   * other.
   */
  ownerCommunityId: string;
  /** Up to 2,000 entries per list (spec §204). */
  entries: FilterTermInput[];
}

export interface PublishFilterListResponse {
  filterList: FilterList | undefined;
}

/**
 * `update_mask` selects which of `display_name`/`description`/`entries` to apply — same
 * FieldMask pattern as `ActorService.UpdateProfile`. `name` and ownership are immutable after
 * publish.
 */
export interface UpdateFilterListRequest {
  id: string;
  displayName: string;
  description: string;
  entries: FilterTermInput[];
  updateMask: string[] | undefined;
}

export interface UpdateFilterListResponse {
  filterList: FilterList | undefined;
}

export interface DeleteFilterListRequest {
  id: string;
}

export interface DeleteFilterListResponse {}

export interface GetFilterListRequest {
  id: string;
}

export interface GetFilterListResponse {
  filterList: FilterList | undefined;
}

export interface ListFilterListsRequest {
  /**
   * Empty lists every publicly published list (spec §199.1 — "a list is public by
   * construction"). Set to an actor id to scope to lists that actor published.
   */
  ownerActorId: string;
  cursor: string;
  limit: number;
}

export interface ListFilterListsResponse {
  filterLists: FilterList[];
  page: PageInfo | undefined;
}

export interface ListFilterListEntriesRequest {
  filterListId: string;
  cursor: string;
  limit: number;
}

export interface ListFilterListEntriesResponse {
  entries: FilterListEntry[];
  page: PageInfo | undefined;
}

export interface SubscribeFilterListRequest {
  filterListId: string;
  /**
   * Server defaults to `FILTER_ACTION_COLLAPSE` when unspecified — the least destructive
   * useful action (spec §199.2).
   */
  action: FilterAction;
}

export interface SubscribeFilterListResponse {
  subscription: FilterListSubscription | undefined;
}

export interface UnsubscribeFilterListRequest {
  filterListId: string;
}

export interface UnsubscribeFilterListResponse {}

export interface FilterListSubscription {
  filterList: FilterList | undefined;
  action: FilterAction;
  createdAt: Timestamp | undefined;
}

export interface ListFilterListSubscriptionsRequest {
  cursor: string;
  limit: number;
}

export interface ListFilterListSubscriptionsResponse {
  subscriptions: FilterListSubscription[];
  page: PageInfo | undefined;
}

export interface SetFilterListEntryExceptionRequest {
  filterListId: string;
  filterListEntryId: string;
  /** True adds the exception, false removes it — idempotent either way. */
  excepted: boolean;
}

export interface SetFilterListEntryExceptionResponse {}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * Filter lists: publishable, subscribable, revocable (spec §199) — the decentralized
 * primitive, extending §111's A3 from shareable inclusion to shareable exclusion. A list is
 * data only: no code, no action, no scope, no ordering, no scores (§199.1). The subscriber
 * owns the action; the list author owns the entries — these never swap (§199.2), and a
 * subscription can never create a block (§199.2, §208). Subscriptions are evaluated live
 * against the list's current entries; entries are never copied into a subscriber's own
 * filters, so unsubscribing is instant and complete (§199.3).
 */

export interface FilterListServiceClient {
  publishFilterList(
    request: PublishFilterListRequest,
    metadata?: Metadata,
  ): Observable<PublishFilterListResponse>;

  updateFilterList(
    request: UpdateFilterListRequest,
    metadata?: Metadata,
  ): Observable<UpdateFilterListResponse>;

  deleteFilterList(
    request: DeleteFilterListRequest,
    metadata?: Metadata,
  ): Observable<DeleteFilterListResponse>;

  getFilterList(
    request: GetFilterListRequest,
    metadata?: Metadata,
  ): Observable<GetFilterListResponse>;

  /** Publicly published lists, most-recently-updated first. */

  listFilterLists(
    request: ListFilterListsRequest,
    metadata?: Metadata,
  ): Observable<ListFilterListsResponse>;

  /**
   * The full entry set, visible to any subscriber at any time — an unauditable list is a
   * black box with authority (spec §199.3).
   */

  listFilterListEntries(
    request: ListFilterListEntriesRequest,
    metadata?: Metadata,
  ): Observable<ListFilterListEntriesResponse>;

  /**
   * Applies the list's entries as filters/mutes with an action and scopes the subscriber
   * chooses, defaulting to `FILTER_ACTION_COLLAPSE` (spec §199.2). Never creates a block.
   */

  subscribeFilterList(
    request: SubscribeFilterListRequest,
    metadata?: Metadata,
  ): Observable<SubscribeFilterListResponse>;

  unsubscribeFilterList(
    request: UnsubscribeFilterListRequest,
    metadata?: Metadata,
  ): Observable<UnsubscribeFilterListResponse>;

  /**
   * The caller's own subscriptions. Subscriber counts are never published anywhere (§199.3,
   * §208) — this is the caller's own list, not an aggregate.
   */

  listFilterListSubscriptions(
    request: ListFilterListSubscriptionsRequest,
    metadata?: Metadata,
  ): Observable<ListFilterListSubscriptionsResponse>;

  /**
   * "This list is right about everything except my friend" — an exception without
   * unsubscribing and without telling the list author (spec §199.3).
   */

  setFilterListEntryException(
    request: SetFilterListEntryExceptionRequest,
    metadata?: Metadata,
  ): Observable<SetFilterListEntryExceptionResponse>;
}

/**
 * Filter lists: publishable, subscribable, revocable (spec §199) — the decentralized
 * primitive, extending §111's A3 from shareable inclusion to shareable exclusion. A list is
 * data only: no code, no action, no scope, no ordering, no scores (§199.1). The subscriber
 * owns the action; the list author owns the entries — these never swap (§199.2), and a
 * subscription can never create a block (§199.2, §208). Subscriptions are evaluated live
 * against the list's current entries; entries are never copied into a subscriber's own
 * filters, so unsubscribing is instant and complete (§199.3).
 */

export interface FilterListServiceController {
  publishFilterList(
    request: PublishFilterListRequest,
    metadata?: Metadata,
  ):
    | Promise<PublishFilterListResponse>
    | Observable<PublishFilterListResponse>
    | PublishFilterListResponse;

  updateFilterList(
    request: UpdateFilterListRequest,
    metadata?: Metadata,
  ):
    | Promise<UpdateFilterListResponse>
    | Observable<UpdateFilterListResponse>
    | UpdateFilterListResponse;

  deleteFilterList(
    request: DeleteFilterListRequest,
    metadata?: Metadata,
  ):
    | Promise<DeleteFilterListResponse>
    | Observable<DeleteFilterListResponse>
    | DeleteFilterListResponse;

  getFilterList(
    request: GetFilterListRequest,
    metadata?: Metadata,
  ): Promise<GetFilterListResponse> | Observable<GetFilterListResponse> | GetFilterListResponse;

  /** Publicly published lists, most-recently-updated first. */

  listFilterLists(
    request: ListFilterListsRequest,
    metadata?: Metadata,
  ):
    | Promise<ListFilterListsResponse>
    | Observable<ListFilterListsResponse>
    | ListFilterListsResponse;

  /**
   * The full entry set, visible to any subscriber at any time — an unauditable list is a
   * black box with authority (spec §199.3).
   */

  listFilterListEntries(
    request: ListFilterListEntriesRequest,
    metadata?: Metadata,
  ):
    | Promise<ListFilterListEntriesResponse>
    | Observable<ListFilterListEntriesResponse>
    | ListFilterListEntriesResponse;

  /**
   * Applies the list's entries as filters/mutes with an action and scopes the subscriber
   * chooses, defaulting to `FILTER_ACTION_COLLAPSE` (spec §199.2). Never creates a block.
   */

  subscribeFilterList(
    request: SubscribeFilterListRequest,
    metadata?: Metadata,
  ):
    | Promise<SubscribeFilterListResponse>
    | Observable<SubscribeFilterListResponse>
    | SubscribeFilterListResponse;

  unsubscribeFilterList(
    request: UnsubscribeFilterListRequest,
    metadata?: Metadata,
  ):
    | Promise<UnsubscribeFilterListResponse>
    | Observable<UnsubscribeFilterListResponse>
    | UnsubscribeFilterListResponse;

  /**
   * The caller's own subscriptions. Subscriber counts are never published anywhere (§199.3,
   * §208) — this is the caller's own list, not an aggregate.
   */

  listFilterListSubscriptions(
    request: ListFilterListSubscriptionsRequest,
    metadata?: Metadata,
  ):
    | Promise<ListFilterListSubscriptionsResponse>
    | Observable<ListFilterListSubscriptionsResponse>
    | ListFilterListSubscriptionsResponse;

  /**
   * "This list is right about everything except my friend" — an exception without
   * unsubscribing and without telling the list author (spec §199.3).
   */

  setFilterListEntryException(
    request: SetFilterListEntryExceptionRequest,
    metadata?: Metadata,
  ):
    | Promise<SetFilterListEntryExceptionResponse>
    | Observable<SetFilterListEntryExceptionResponse>
    | SetFilterListEntryExceptionResponse;
}

export function FilterListServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      'publishFilterList',
      'updateFilterList',
      'deleteFilterList',
      'getFilterList',
      'listFilterLists',
      'listFilterListEntries',
      'subscribeFilterList',
      'unsubscribeFilterList',
      'listFilterListSubscriptions',
      'setFilterListEntryException',
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('FilterListService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('FilterListService', method)(
        constructor.prototype[method],
        method,
        descriptor,
      );
    }
  };
}

export const FILTER_LIST_SERVICE_NAME = 'FilterListService';
