// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: patches/v1/appeals.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { Timestamp } from '../../google/protobuf/timestamp.js';
import { PageInfo } from './common.js';

export const protobufPackage = 'patches.v1';

export enum AppealStatus {
  APPEAL_STATUS_UNSPECIFIED = 'APPEAL_STATUS_UNSPECIFIED',
  APPEAL_STATUS_OPEN = 'APPEAL_STATUS_OPEN',
  APPEAL_STATUS_UPHELD = 'APPEAL_STATUS_UPHELD',
  APPEAL_STATUS_OVERTURNED = 'APPEAL_STATUS_OVERTURNED',
  APPEAL_STATUS_MODIFIED = 'APPEAL_STATUS_MODIFIED',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

/**
 * Visible only to the appellant and moderators (§201.3, §208) — the public moderation log
 * shows only that an action was appealed (a boolean), never this message's content.
 */
export interface Appeal {
  id: string;
  moderationNoticeId: string;
  /** Max 2,000 characters (spec §204). */
  statement: string;
  status: AppealStatus;
  createdAt: Timestamp | undefined;
  /** Unset until resolved. */
  resolvedAt: Timestamp | undefined;
  /**
   * Authored by the resolving moderator — never `reports.moderator_note` or any other
   * internal-only field (spec §55, §201.3, §208).
   */
  resolutionReason: string;
}

export interface CreateAppealRequest {
  moderationNoticeId: string;
  statement: string;
}

export interface CreateAppealResponse {
  appeal: Appeal | undefined;
}

export interface GetAppealRequest {
  id: string;
}

export interface GetAppealResponse {
  appeal: Appeal | undefined;
}

export interface ListMyAppealsRequest {
  cursor: string;
  limit: number;
}

export interface ListMyAppealsResponse {
  appeals: Appeal[];
  page: PageInfo | undefined;
}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * Appeals against a node moderation notice (spec §201.3). Only the acted-upon actor may
 * appeal — not a reporter, not a bystander. One appeal per moderation notice. Admin-side
 * resolution is CLI-only (`patches-admin appeal list|inspect|resolve`) — there is
 * deliberately no gRPC resolve RPC here, mirroring `report list|inspect|resolve`.
 */

export interface AppealServiceClient {
  /**
   * `moderation_notice_id` identifies the notice being appealed
   * (`ModerationService.ListMyModerationNotices`). Rejected if the caller already has an
   * appeal for that notice, or the node's published appeal window has closed
   * (`NodeService.GetNodePolicy`).
   */

  createAppeal(request: CreateAppealRequest, metadata?: Metadata): Observable<CreateAppealResponse>;

  getAppeal(request: GetAppealRequest, metadata?: Metadata): Observable<GetAppealResponse>;

  /** The caller's own appeals, most-recent first. */

  listMyAppeals(
    request: ListMyAppealsRequest,
    metadata?: Metadata,
  ): Observable<ListMyAppealsResponse>;
}

/**
 * Appeals against a node moderation notice (spec §201.3). Only the acted-upon actor may
 * appeal — not a reporter, not a bystander. One appeal per moderation notice. Admin-side
 * resolution is CLI-only (`patches-admin appeal list|inspect|resolve`) — there is
 * deliberately no gRPC resolve RPC here, mirroring `report list|inspect|resolve`.
 */

export interface AppealServiceController {
  /**
   * `moderation_notice_id` identifies the notice being appealed
   * (`ModerationService.ListMyModerationNotices`). Rejected if the caller already has an
   * appeal for that notice, or the node's published appeal window has closed
   * (`NodeService.GetNodePolicy`).
   */

  createAppeal(
    request: CreateAppealRequest,
    metadata?: Metadata,
  ): Promise<CreateAppealResponse> | Observable<CreateAppealResponse> | CreateAppealResponse;

  getAppeal(
    request: GetAppealRequest,
    metadata?: Metadata,
  ): Promise<GetAppealResponse> | Observable<GetAppealResponse> | GetAppealResponse;

  /** The caller's own appeals, most-recent first. */

  listMyAppeals(
    request: ListMyAppealsRequest,
    metadata?: Metadata,
  ): Promise<ListMyAppealsResponse> | Observable<ListMyAppealsResponse> | ListMyAppealsResponse;
}

export function AppealServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ['createAppeal', 'getAppeal', 'listMyAppeals'];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('AppealService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('AppealService', method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const APPEAL_SERVICE_NAME = 'AppealService';
