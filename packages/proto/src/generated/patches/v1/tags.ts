// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: patches/v1/tags.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { Timestamp } from '../../google/protobuf/timestamp.js';
import { PageInfo } from './common.js';

export const protobufPackage = 'patches.v1';

export interface Tag {
  id: string;
  /**
   * Canonical form: NFKC-normalized and casefolded (spec §189, §192). Max 30 characters,
   * at least one letter (spec §188).
   */
  name: string;
  /** As originally cased by whoever first used it. */
  displayName: string;
  createdAt: Timestamp | undefined;
}

export interface SearchTagsRequest {
  query: string;
  cursor: string;
  limit: number;
}

export interface SearchTagsResponse {
  tags: Tag[];
  page: PageInfo | undefined;
}

export interface MuteTagRequest {
  tagId: string;
}

export interface MuteTagResponse {}

export interface UnmuteTagRequest {
  tagId: string;
}

export interface UnmuteTagResponse {}

export interface ListMutedTagsRequest {
  cursor: string;
  limit: number;
}

export interface ListMutedTagsResponse {
  tags: Tag[];
  page: PageInfo | undefined;
}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * Hashtag search and per-actor tag mutes (spec §189–190). Deliberately no post-count/
 * popularity surface anywhere in this service (spec §181, §58) — there is no engagement
 * ranking in this product and a tag's count is not published.
 */

export interface TagServiceClient {
  /** Handle-prefix-style search over tag names (spec §112's search pattern, applied to tags). */

  searchTags(request: SearchTagsRequest, metadata?: Metadata): Observable<SearchTagsResponse>;

  /** Idempotent: muting an already-muted tag is not an error. */

  muteTag(request: MuteTagRequest, metadata?: Metadata): Observable<MuteTagResponse>;

  /** Idempotent: unmuting a tag the caller hasn't muted is not an error. */

  unmuteTag(request: UnmuteTagRequest, metadata?: Metadata): Observable<UnmuteTagResponse>;

  /** The caller's own muted tags, most-recent first. */

  listMutedTags(
    request: ListMutedTagsRequest,
    metadata?: Metadata,
  ): Observable<ListMutedTagsResponse>;
}

/**
 * Hashtag search and per-actor tag mutes (spec §189–190). Deliberately no post-count/
 * popularity surface anywhere in this service (spec §181, §58) — there is no engagement
 * ranking in this product and a tag's count is not published.
 */

export interface TagServiceController {
  /** Handle-prefix-style search over tag names (spec §112's search pattern, applied to tags). */

  searchTags(
    request: SearchTagsRequest,
    metadata?: Metadata,
  ): Promise<SearchTagsResponse> | Observable<SearchTagsResponse> | SearchTagsResponse;

  /** Idempotent: muting an already-muted tag is not an error. */

  muteTag(
    request: MuteTagRequest,
    metadata?: Metadata,
  ): Promise<MuteTagResponse> | Observable<MuteTagResponse> | MuteTagResponse;

  /** Idempotent: unmuting a tag the caller hasn't muted is not an error. */

  unmuteTag(
    request: UnmuteTagRequest,
    metadata?: Metadata,
  ): Promise<UnmuteTagResponse> | Observable<UnmuteTagResponse> | UnmuteTagResponse;

  /** The caller's own muted tags, most-recent first. */

  listMutedTags(
    request: ListMutedTagsRequest,
    metadata?: Metadata,
  ): Promise<ListMutedTagsResponse> | Observable<ListMutedTagsResponse> | ListMutedTagsResponse;
}

export function TagServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ['searchTags', 'muteTag', 'unmuteTag', 'listMutedTags'];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('TagService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('TagService', method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const TAG_SERVICE_NAME = 'TagService';
