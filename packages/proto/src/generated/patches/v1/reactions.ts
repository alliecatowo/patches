// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: patches/v1/reactions.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { Actor } from './actors.js';
import { PageInfo } from './common.js';
import { Post, PostCounts, PostViewerState } from './posts.js';

export const protobufPackage = 'patches.v1';

export interface LikePostRequest {
  postId: string;
}

export interface LikePostResponse {
  counts: PostCounts | undefined;
  viewerState: PostViewerState | undefined;
}

export interface UnlikePostRequest {
  postId: string;
}

export interface UnlikePostResponse {
  counts: PostCounts | undefined;
  viewerState: PostViewerState | undefined;
}

export interface BookmarkPostRequest {
  postId: string;
}

export interface BookmarkPostResponse {
  viewerState: PostViewerState | undefined;
}

export interface UnbookmarkPostRequest {
  postId: string;
}

export interface UnbookmarkPostResponse {
  viewerState: PostViewerState | undefined;
}

export interface ListBookmarksRequest {
  cursor: string;
  limit: number;
}

export interface ListBookmarksResponse {
  posts: Post[];
  page: PageInfo | undefined;
}

export interface ListPostLikersRequest {
  postId: string;
  cursor: string;
  limit: number;
}

export interface ListPostLikersResponse {
  actors: Actor[];
  page: PageInfo | undefined;
}

export interface RepostPostRequest {
  postId: string;
}

export interface RepostPostResponse {
  counts: PostCounts | undefined;
  viewerState: PostViewerState | undefined;
}

export interface UnrepostPostRequest {
  postId: string;
}

export interface UnrepostPostResponse {
  counts: PostCounts | undefined;
  viewerState: PostViewerState | undefined;
}

export interface ListPostRepostersRequest {
  postId: string;
  cursor: string;
  limit: number;
}

export interface ListPostRepostersResponse {
  actors: Actor[];
  page: PageInfo | undefined;
}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * Likes and bookmarks (spec §53). Bookmarks are private — never exposed on another actor's
 * view; `ListBookmarks` only ever returns the caller's own.
 */

export interface ReactionServiceClient {
  likePost(request: LikePostRequest, metadata?: Metadata): Observable<LikePostResponse>;

  /** Idempotent: unliking a post the caller has not liked is not an error. */

  unlikePost(request: UnlikePostRequest, metadata?: Metadata): Observable<UnlikePostResponse>;

  bookmarkPost(request: BookmarkPostRequest, metadata?: Metadata): Observable<BookmarkPostResponse>;

  /** Idempotent: unbookmarking a post the caller has not bookmarked is not an error. */

  unbookmarkPost(
    request: UnbookmarkPostRequest,
    metadata?: Metadata,
  ): Observable<UnbookmarkPostResponse>;

  /**
   * The caller's own bookmarks, most-recent first (spec §52 MVP note, §53). Requires an
   * authenticated session — there is no such thing as an anonymous bookmark list.
   */

  listBookmarks(
    request: ListBookmarksRequest,
    metadata?: Metadata,
  ): Observable<ListBookmarksResponse>;

  /** Actors who liked a post, most-recent first. */

  listPostLikers(
    request: ListPostLikersRequest,
    metadata?: Metadata,
  ): Observable<ListPostLikersResponse>;

  /**
   * A repost is a pointer row like a like or a bookmark (spec §190) — it never duplicates the
   * post's content.
   */

  repostPost(request: RepostPostRequest, metadata?: Metadata): Observable<RepostPostResponse>;

  /** Idempotent: unreposting a post the caller hasn't reposted is not an error. */

  unrepostPost(request: UnrepostPostRequest, metadata?: Metadata): Observable<UnrepostPostResponse>;

  /** Actors who reposted a post, most-recent first. */

  listPostReposters(
    request: ListPostRepostersRequest,
    metadata?: Metadata,
  ): Observable<ListPostRepostersResponse>;
}

/**
 * Likes and bookmarks (spec §53). Bookmarks are private — never exposed on another actor's
 * view; `ListBookmarks` only ever returns the caller's own.
 */

export interface ReactionServiceController {
  likePost(
    request: LikePostRequest,
    metadata?: Metadata,
  ): Promise<LikePostResponse> | Observable<LikePostResponse> | LikePostResponse;

  /** Idempotent: unliking a post the caller has not liked is not an error. */

  unlikePost(
    request: UnlikePostRequest,
    metadata?: Metadata,
  ): Promise<UnlikePostResponse> | Observable<UnlikePostResponse> | UnlikePostResponse;

  bookmarkPost(
    request: BookmarkPostRequest,
    metadata?: Metadata,
  ): Promise<BookmarkPostResponse> | Observable<BookmarkPostResponse> | BookmarkPostResponse;

  /** Idempotent: unbookmarking a post the caller has not bookmarked is not an error. */

  unbookmarkPost(
    request: UnbookmarkPostRequest,
    metadata?: Metadata,
  ): Promise<UnbookmarkPostResponse> | Observable<UnbookmarkPostResponse> | UnbookmarkPostResponse;

  /**
   * The caller's own bookmarks, most-recent first (spec §52 MVP note, §53). Requires an
   * authenticated session — there is no such thing as an anonymous bookmark list.
   */

  listBookmarks(
    request: ListBookmarksRequest,
    metadata?: Metadata,
  ): Promise<ListBookmarksResponse> | Observable<ListBookmarksResponse> | ListBookmarksResponse;

  /** Actors who liked a post, most-recent first. */

  listPostLikers(
    request: ListPostLikersRequest,
    metadata?: Metadata,
  ): Promise<ListPostLikersResponse> | Observable<ListPostLikersResponse> | ListPostLikersResponse;

  /**
   * A repost is a pointer row like a like or a bookmark (spec §190) — it never duplicates the
   * post's content.
   */

  repostPost(
    request: RepostPostRequest,
    metadata?: Metadata,
  ): Promise<RepostPostResponse> | Observable<RepostPostResponse> | RepostPostResponse;

  /** Idempotent: unreposting a post the caller hasn't reposted is not an error. */

  unrepostPost(
    request: UnrepostPostRequest,
    metadata?: Metadata,
  ): Promise<UnrepostPostResponse> | Observable<UnrepostPostResponse> | UnrepostPostResponse;

  /** Actors who reposted a post, most-recent first. */

  listPostReposters(
    request: ListPostRepostersRequest,
    metadata?: Metadata,
  ):
    | Promise<ListPostRepostersResponse>
    | Observable<ListPostRepostersResponse>
    | ListPostRepostersResponse;
}

export function ReactionServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      'likePost',
      'unlikePost',
      'bookmarkPost',
      'unbookmarkPost',
      'listBookmarks',
      'listPostLikers',
      'repostPost',
      'unrepostPost',
      'listPostReposters',
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('ReactionService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('ReactionService', method)(
        constructor.prototype[method],
        method,
        descriptor,
      );
    }
  };
}

export const REACTION_SERVICE_NAME = 'ReactionService';
