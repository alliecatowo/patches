// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: patches/v1/filters.proto

/* eslint-disable */
import type { Metadata } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';
import { Timestamp } from '../../google/protobuf/timestamp.js';
import { PageInfo } from './common.js';

export const protobufPackage = 'patches.v1';

/**
 * A filter-term kind (spec §198.2). Shared with filter list entries (`filter_lists.proto`) —
 * same five kinds, same matching semantics.
 */
export enum FilterTermKind {
  FILTER_TERM_KIND_UNSPECIFIED = 'FILTER_TERM_KIND_UNSPECIFIED',
  FILTER_TERM_KIND_SUBSTRING = 'FILTER_TERM_KIND_SUBSTRING',
  FILTER_TERM_KIND_WORD = 'FILTER_TERM_KIND_WORD',
  FILTER_TERM_KIND_TAG = 'FILTER_TERM_KIND_TAG',
  FILTER_TERM_KIND_ACTOR = 'FILTER_TERM_KIND_ACTOR',
  FILTER_TERM_KIND_DOMAIN = 'FILTER_TERM_KIND_DOMAIN',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

/**
 * Where a filter applies (spec §198.3). Threads and profiles are deliberately absent — "a
 * filter governs what arrives unbidden", not something a viewer opened on purpose.
 */
export enum FilterScope {
  FILTER_SCOPE_UNSPECIFIED = 'FILTER_SCOPE_UNSPECIFIED',
  FILTER_SCOPE_HOME = 'FILTER_SCOPE_HOME',
  FILTER_SCOPE_LOCAL = 'FILTER_SCOPE_LOCAL',
  FILTER_SCOPE_TAG_FEED = 'FILTER_SCOPE_TAG_FEED',
  FILTER_SCOPE_COMMUNITY_FEED = 'FILTER_SCOPE_COMMUNITY_FEED',
  FILTER_SCOPE_NOTIFICATIONS = 'FILTER_SCOPE_NOTIFICATIONS',
  FILTER_SCOPE_SEARCH = 'FILTER_SCOPE_SEARCH',
  FILTER_SCOPE_MESSAGE_REQUESTS = 'FILTER_SCOPE_MESSAGE_REQUESTS',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

/**
 * What a matched filter does to a post (spec §198.3). Reused by filter list subscriptions
 * (`filter_lists.proto`) and, with an added `ignore` value, by `LabelAction`
 * (`labels.proto`).
 */
export enum FilterAction {
  FILTER_ACTION_UNSPECIFIED = 'FILTER_ACTION_UNSPECIFIED',
  /** FILTER_ACTION_HIDE - The row is not returned to the client at all — enforced by omission, on the server. */
  FILTER_ACTION_HIDE = 'FILTER_ACTION_HIDE',
  /**
   * FILTER_ACTION_COLLAPSE - Returned, replaced client-side by one collapsed line naming the filter, expandable in
   * place.
   */
  FILTER_ACTION_COLLAPSE = 'FILTER_ACTION_COLLAPSE',
  /** FILTER_ACTION_WARN - Returned and rendered normally with a "filtered: <name>" marker. */
  FILTER_ACTION_WARN = 'FILTER_ACTION_WARN',
  UNRECOGNIZED = 'UNRECOGNIZED',
}

/** A term as supplied by the caller (create/update/import) — no `id`, the server assigns one. */
export interface FilterTermInput {
  kind: FilterTermKind;
  /**
   * A literal value the server matches against — never a user-supplied pattern (§198.2,
   * §208).
   */
  value: string;
}

export interface FilterTerm {
  id: string;
  kind: FilterTermKind;
  /**
   * Sensitive at the application layer: never logged, never shown to a moderator (a filter
   * is not evidence of anything) — spec §202.
   */
  value: string;
}

export interface Filter {
  id: string;
  /**
   * Shown when a post is collapsed by this filter. Sensitive at the application layer, same
   * as `FilterTerm.value`.
   */
  name: string;
  terms: FilterTerm[];
  scopes: FilterScope[];
  action: FilterAction;
  /** Unset means the filter never expires. */
  expiresAt: Timestamp | undefined;
  createdAt: Timestamp | undefined;
  updatedAt: Timestamp | undefined;
}

export interface CreateFilterRequest {
  name: string;
  /** Up to 20 terms per filter (spec §204). */
  terms: FilterTermInput[];
  scopes: FilterScope[];
  action: FilterAction;
  /** Unset means the filter never expires. */
  expiresAt: Timestamp | undefined;
}

export interface CreateFilterResponse {
  filter: Filter | undefined;
}

/**
 * `update_mask` selects which of `name`/`terms`/`scopes`/`action`/`expires_at` to apply —
 * same FieldMask pattern as `ActorService.UpdateProfile`. A full replacement of `terms` is
 * intentional: a filter's term set is small (up to 20, spec §204) and partial term editing
 * would need its own add/remove RPCs for no real benefit.
 */
export interface UpdateFilterRequest {
  id: string;
  name: string;
  terms: FilterTermInput[];
  scopes: FilterScope[];
  action: FilterAction;
  expiresAt: Timestamp | undefined;
  updateMask: string[] | undefined;
}

export interface UpdateFilterResponse {
  filter: Filter | undefined;
}

export interface DeleteFilterRequest {
  id: string;
}

export interface DeleteFilterResponse {}

export interface ListFiltersRequest {
  cursor: string;
  limit: number;
}

export interface ListFiltersResponse {
  filters: Filter[];
  page: PageInfo | undefined;
}

export interface ExportFiltersRequest {}

export interface ExportFiltersResponse {
  /**
   * A plain, documented JSON document (spec §198.5): `{ "filters": [...] }`, one entry per
   * `Filter` with term ids omitted (import assigns new ones). Never a binary blob, never
   * executable (§153).
   */
  json: string;
}

export interface ImportFiltersRequest {
  /** Same JSON shape `ExportFilters` produces. */
  json: string;
  /**
   * False (the default): dry run — return what would be added without writing anything, so
   * the client can render the required preview (§198.5). True: apply.
   */
  apply: boolean;
}

export interface ImportFiltersResponse {
  /** The filters that were (or, if `apply` was false, would be) added. */
  added: Filter[];
}

export const PATCHES_V1_PACKAGE_NAME = 'patches.v1';

/**
 * Bring-your-own filters (spec §198). A filter is a named, viewer-owned rule that removes or
 * conceals posts — it never adds, reorders, or scores anything (§198.1, §208). Evaluated
 * server-side at the same chokepoint blocks/mutes already flow through (§198.4): every client
 * sees the same timeline, and pagination stays correct. Filter terms are always literal —
 * user-supplied regular expressions are prohibited in v1 (§198.2, §208).
 */

export interface FilterServiceClient {
  createFilter(request: CreateFilterRequest, metadata?: Metadata): Observable<CreateFilterResponse>;

  updateFilter(request: UpdateFilterRequest, metadata?: Metadata): Observable<UpdateFilterResponse>;

  deleteFilter(request: DeleteFilterRequest, metadata?: Metadata): Observable<DeleteFilterResponse>;

  /** The caller's own filters, most-recent first. */

  listFilters(request: ListFiltersRequest, metadata?: Metadata): Observable<ListFiltersResponse>;

  /** A plain, documented JSON export (spec §198.5) — never a binary blob, never executable. */

  exportFilters(
    request: ExportFiltersRequest,
    metadata?: Metadata,
  ): Observable<ExportFiltersResponse>;

  /**
   * Additive and previewable: pass `apply = false` to see what would be added without
   * writing anything (spec §198.5).
   */

  importFilters(
    request: ImportFiltersRequest,
    metadata?: Metadata,
  ): Observable<ImportFiltersResponse>;
}

/**
 * Bring-your-own filters (spec §198). A filter is a named, viewer-owned rule that removes or
 * conceals posts — it never adds, reorders, or scores anything (§198.1, §208). Evaluated
 * server-side at the same chokepoint blocks/mutes already flow through (§198.4): every client
 * sees the same timeline, and pagination stays correct. Filter terms are always literal —
 * user-supplied regular expressions are prohibited in v1 (§198.2, §208).
 */

export interface FilterServiceController {
  createFilter(
    request: CreateFilterRequest,
    metadata?: Metadata,
  ): Promise<CreateFilterResponse> | Observable<CreateFilterResponse> | CreateFilterResponse;

  updateFilter(
    request: UpdateFilterRequest,
    metadata?: Metadata,
  ): Promise<UpdateFilterResponse> | Observable<UpdateFilterResponse> | UpdateFilterResponse;

  deleteFilter(
    request: DeleteFilterRequest,
    metadata?: Metadata,
  ): Promise<DeleteFilterResponse> | Observable<DeleteFilterResponse> | DeleteFilterResponse;

  /** The caller's own filters, most-recent first. */

  listFilters(
    request: ListFiltersRequest,
    metadata?: Metadata,
  ): Promise<ListFiltersResponse> | Observable<ListFiltersResponse> | ListFiltersResponse;

  /** A plain, documented JSON export (spec §198.5) — never a binary blob, never executable. */

  exportFilters(
    request: ExportFiltersRequest,
    metadata?: Metadata,
  ): Promise<ExportFiltersResponse> | Observable<ExportFiltersResponse> | ExportFiltersResponse;

  /**
   * Additive and previewable: pass `apply = false` to see what would be added without
   * writing anything (spec §198.5).
   */

  importFilters(
    request: ImportFiltersRequest,
    metadata?: Metadata,
  ): Promise<ImportFiltersResponse> | Observable<ImportFiltersResponse> | ImportFiltersResponse;
}

export function FilterServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      'createFilter',
      'updateFilter',
      'deleteFilter',
      'listFilters',
      'exportFilters',
      'importFilters',
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('FilterService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('FilterService', method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const FILTER_SERVICE_NAME = 'FilterService';
